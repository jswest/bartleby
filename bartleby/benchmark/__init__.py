"""The summarizer benchmark: `bartleby benchmark` command group.

Append-only, per-cell JSONL stores under `benchmarks/` — a cell is one
(provider, model, doc), or for judgments one (provider, model, doc,
judge-provider, judge-model). See benchmarks/README.md and GH-268.
"""
