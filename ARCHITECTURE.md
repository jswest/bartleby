# Bartleby architecture

Bartleby is two surfaces over one SQLite database:

1. **The CLI (`bartleby ...`)** — ingestion (`scribe`), config (`ready`), projects, sessions, embedding, audit logs.
2. **The skill (`skill/`)** — six Python scripts the agent calls: `list_documents`, `search`, `read_chunks`, `read_document`, `save_summary`, `save_finding`.

The database is the contract between them. The CLI writes; the skill reads and writes findings back. Both import from one Python package (`bartleby/`).

For the on-disk shape, read the code: `bartleby/db/schema.py` for the schema, `bartleby/commands/*` for CLI subcommands, `bartleby/skill_scripts/*` for the scripts. Each script's module docstring documents its JSON response contract, and `bartleby skill <name> --help` prints that docstring alongside the flags (the docstring is the parser's `description`), so the agent can introspect both arguments and return shape without running the script.

## Backwards compatibility

**Default position: we don't care about it.** No migration code, no compat shims, no feature-flagged old code paths. Bump `SCHEMA_VERSION`, change the code, tell users to re-ingest. The cost of preserving compat is invariably higher than the cost of re-ingest for a tool at this scale.

**The one allowed relaxation: additive-only schema upgrades.** A schema bump may ship with an entry in the upgrade chain (`bartleby/db/upgrades.py`) if — and only if — the change is purely additive: new tables, new indexes, new nullable columns. No row transformations, no column renames, no semantic shifts in existing data. Users run `bartleby project upgrade <name>` explicitly to apply the chain; the strict version check in `open_db` rejects mismatched DBs otherwise. Non-additive bumps still mean re-ingest (the chain simply has no entry for that step, and `project upgrade` refuses).

The discipline: every new bump is either additive-with-an-upgrade-function or non-additive-with-re-ingest. The codebase never branches on schema version; it always pins to `SCHEMA_VERSION` exactly. The upgrade path is one-shot at the gate, not an ongoing tax.

## Load-bearing invariants

Things that look local but aren't. Code review should catch violations.

### Polymorphic-chunks discipline

`chunks.source_id` is not a foreign key to any single table — it references `documents`, `summaries`, `findings`, or `images` depending on `source_kind`. SQLite can't enforce this; the `CHECK (source_kind IN (...))` constraint blocks typos but not kind/id mismatches.

**All writes to `chunks` go through `bartleby/db/chunks.py` typed helpers** (`insert_document_chunks`, `insert_summary_chunks`, `insert_finding_chunks`, `insert_image_chunks`, `delete_chunks_for`). The chokepoint exists so the `source_kind` is hardcoded per function. Direct `INSERT INTO chunks` anywhere else is a bug.

### Single-writer drain + per-unit resume

Ingest writes flow through **one `Writer` (`bartleby/ingest/writer.py`) that owns the WAL connection** — the sole path that persists a parse, a caption, or a summary, each in its own transaction. Parse is producer-side and pure (no DB); the Writer drains the result. The split is load-bearing for **restartability**: a document's parse (text chunks + *uncaptioned* image rows) commits atomically and is never re-parsed; each image **caption** (keyed by image byte-hash) and the **summary** commit independently, so a caption failure can't roll back the parse. Completeness is derived from DB state — `parsed ∧ every image row has `analysis_json` ∧ (summary row exists ∨ summaries disabled ∨ 0 summarizable chunks)` — and a resume runs only the missing units. An image row with `analysis_json IS NULL` is the uncaptioned marker; do not write a non-null analysis at parse time. A deterministically-failing unit is recorded in `failed_ingests` and capped at `MAX_INGEST_ATTEMPTS` (not retried forever), and surfaced at end-of-run and in `bartleby project info` — a skipped unit must never read as a completed one. There is intentionally **no stranded-partial cleanup**: atomic parse means a `documents` row implies a finished parse, so the old `_document_already_ingested` / `_cleanup_partial_document` dance is gone.

Because a resume can re-run units across separate invocations, each persisted unit also carries **provenance**: `Writer.begin_run` records the run's resolved, secret-stripped config in the `ingests` table and stamps the document / summary / chunk rows it produces with that `ingest_run_id` (nullable — NULL on the skill write path and on corpora ingested before this feature). On a re-run the producer warns — never blocks — on any config field that drifted from the last ingest. See the ingest-provenance decision-log entry.

### Image content_type discipline

Image chunks carry one of two `content_type` values: `image_ocr` (verbatim transcription, treat as primary source) or `image_description` (model interpretation of visual content, cite as interpretation). The split is enforced by `bartleby/ingest/images.py:analysis_to_chunk_inputs` and surfaced to agents via SKILL.md. Both `chunks` rows for an image point at the same `image_id` via `chunks.source_id`; the document anchor lives in `document_images` (one join row per occurrence).

### Summarizer structured-output contract

The summarizer is structured-output only across all three providers (Anthropic / OpenAI / Ollama). Even though we control the prompt, JSON enforcement keeps open-source models from drifting into "Here is your summary:" preambles, thinking tags, or stray markdown fences. The schema is rendered from a Pydantic model (`DocumentSummary`); all three providers consume `model_json_schema()`, so adding fields means one place to change.

Validation failures raise. Don't insert malformed summaries silently.

### Memory-off enforcement

A session with `memory_enabled = 0` must not see *other sessions'* findings — enforced at the **script level**, not the prompt level. Two enforcement shapes: `skill_scripts/search.py` silently excludes **all** findings from results regardless of flags (ranked retrieval is the contamination vector — don't soften this); the direct-read commands `read_finding` / `list_findings` instead narrow to the session's **own** findings (a run can read back what it wrote, never another session's). `save_finding` stays open so a memory-off run can still produce findings for later comparison.

### Truncation note

When a document exceeds `max_summarize_tokens`, the summary's `text` field gets a deterministic note appended **in code**, not via the prompt. The caveat is guaranteed, not modeled.

## Conventions

- Skill scripts print one JSON object to stdout, exit non-zero on error with `{"error", "code"}`. Prose/progress goes to stderr only.
- Embedding model: `BAAI/bge-base-en-v1.5` (768 dims, 512 token max). FTS5 tokenizer: `unicode61 remove_diacritics 2`.
- LLM provider defaults: anthropic `claude-haiku-4-5`, openai `gpt-5-mini`, ollama `qwen3-vl:30b`.
- VLM provider defaults: anthropic `claude-haiku-4-5`, openai `gpt-5-mini`, ollama `qwen3-vl:30b`.
- PDF converter (config `pdf_converter`, CLI `--pdf-converter`): `pdfplumber` (default — fast text + page-render image extraction) and `docling` (opt-in — better structural extraction at higher cost).
- HTML converter (config `html_converter`, CLI `--html-converter`): `docling` (default) and `sec2md` (opt-in — routes iXBRL EDGAR filings to sec2md by sniff, non-iXBRL HTML falls back to docling). MD always goes through docling. If you have an HTML/MD corpus, `docling` must be installed; if you set `html_converter=sec2md`, the `sec2md` extra must also be installed.
- Dependency management: `uv` (not pip/venv). Run with `uv run python`.
- **Skill flag naming (issue #111)**: id-taking flags name the *noun*; the value is its id — **no `-id` suffix** (`--document`, `--finding`, `--from`, `--into`, `--chunks`, `--around-chunk`). The internal argparse `dest` may still be `finding_id` etc.; only the user-facing flag drops the suffix. **Arity is carried by singular/plural**: a singular flag is one id (`--document <id>`, `--finding <id>`), a plural flag is a comma-list (`--documents <id,id>`, `--chunks <id,id>`). **Scope vs target are deliberately distinct** even when both are plural id-lists: `--in-documents` is a *scope filter* (narrows `search`/`scan`/`describe_corpus`/`list_documents` to that set) while `--documents` is a *target set* (the docs `assign_tag`/`unassign_tag` act on). **Merge verbs agree**: destination is `--into`, source is `--from`, on both `merge_findings` and `merge_tags`.

## Deferred (potential v2)

Things we said no to and may revisit:

- Map-reduce summarization. Currently single-shot only.
- Cross-session memory beyond "search past findings" — no automatic injection, no summarization of past sessions.
- Per-project config beyond `~/.bartleby/config.yaml`.
- Porter stemming for FTS5 (`porter unicode61 remove_diacritics 2`). Better recall, requires re-index.
- An MCP server (the skill is plain scripts).
- Differentiating agent-saved summaries from ingest-time summaries (would need a `created_by` column on `summaries`).

## Decision log

Settled judgment calls, kept here so we don't re-derive them.

- **CLI rename**: `bartleby read` → `bartleby scribe`. "Read" belongs to the agent; the CLI scribes.
- **Sequential ingestion**: `ProcessPoolExecutor` removed. Simpler, predictable, easier to debug.
- **Docling is the only converter** for PDF/HTML/MD. Handles image-PDF OCR internally. Playwright, PyMuPDF, direct Tesseract dropped. `.txt` uses a simple character chunker (Docling has no text reader).
- **Reranker dropped**: cross-encoder reranker removed; RRF over FTS+semantic replaces it.
- **`finding_citations` is chunk-level**: findings cite chunk_ids, not document_ids — preserves "show me exactly the passage" precision.
- **Sessions auto-create**: explicit `bartleby session start` is only needed for `--no-memory`. Otherwise the skill auto-creates a session on first call.
- **Query embedding**: skill shells out to `bartleby embed` via list-form `subprocess.run`. No shell escaping. No daemon.
- **`documents.token_count`**: computed via `tiktoken.cl100k_base`. Approximate across providers; acceptable for a `--force` gate.
- **Schema v2 — title/description on summaries and findings**: `summaries.{title, description}` and `findings.{title, description}` (all NOT NULL) so `list_documents` and finding browsing aren't filename-only. Summarizer returns all three fields in one structured-output call — we don't pay for the document text three times.
- **Schema v3 — image pipeline**: added `images` (one row per unique image, deduped on `file_hash`) and `document_images` (one row per occurrence, with `page_number` + `image_index_on_page`). `chunks.source_kind` gains `'image'`. New content_type values: `'ocr'` for Tesseract-recovered scanned-page text (stored under `source_kind='document'`); `'image_ocr'` and `'image_description'` for VLM outputs (stored under `source_kind='image'`). Default PDF backend swapped from Docling to pdfplumber for ~10x faster text-PDF ingest.
- **Schema v4 — `chunks.page_number`**: first-class `INTEGER` column on `chunks`. Populated for pdfplumber-extracted document chunks (the previous `section_heading = "page N"` hack is gone — section_heading is now `NULL` for pdfplumber). NULL for docling chunks (docling doesn't expose per-chunk pages in our wiring), summaries, findings, and image chunks (image page lives on `document_images`). `search` and `read_chunks` surface it directly; `save_finding` returns it on `citations[]`.
- **Tesseract owns image transcription, VLM owns description.** Each embedded image is dispositioned by Tesseract first: if it yields ≥ 300 chars at avg confidence ≥ 60, classification is `kind='text'` and the VLM is never called — Tesseract output becomes the single `image_ocr` chunk. Otherwise classification is `kind='scene'` and the VLM produces a bounded description (~200 words, never asked to transcribe) which becomes the single `image_description` chunk. One chunk per image, not two. The split exists because asking a small local VLM (gemma3, etc.) to verbatim-transcribe a text-heavy slide under JSON-grammar constraint causes per-image latency in the tens of minutes; Tesseract handles it in a second. Sparse-page renders that already failed page-level OCR pass their `OcrResult` down so the image pipeline doesn't re-OCR the same bytes. Providers' `analyze_image` returns a `VlmDescription` (description + notes only); the orchestrator merges it with the Tesseract result into the persisted `ImageAnalysis`.
- **`search --in-documents`**: scopes a search to those documents' chunks and their summaries' chunks. Findings dropped when set (they're not tied to documents).
- **`search` triage signals**: each hit carries `rank` (1-indexed) and `normalized_score` (top hit = 1.0). Raw RRF `score` is tiny by design (~`0.015–0.033`) and only comparable within one query.
- **`read_chunks --chunks <ids>`**: second mode for direct chunk lookup by id, mutually exclusive with `--document`.
- **No running citation tracker**: an agent asked for `cite <chunk_id>` + `save_finding --use-tracked-citations`. Declined — `save_finding` already gives durable storage; SKILL.md nudges agents to write interim findings when context grows long.
- **Additive-only schema upgrades allowed (relaxation of "no backwards compat")**: schema bumps may ship with an entry in `bartleby/db/upgrades.py` if the change is purely additive (new tables, new indexes, new nullable columns). Users invoke `bartleby project upgrade <name>` explicitly; the strict version check in `open_db` is unchanged and still rejects mismatched DBs without that step. Non-additive bumps still force re-ingest (no chain entry). The codebase never branches on schema version — `SCHEMA_VERSION` stays pinned. Rationale: existing users with multi-hour ingests get a graceful path for genuinely safe changes (e.g. `summaries.authored_date` from #15, `tags` table from #16) without us paying the ongoing migration-code tax.
- **Summarizer input includes image chunks**: ingest-time summarization feeds the LLM document chunks *and* image chunks (the VLM's `image_description` or Tesseract's `image_ocr`) interleaved by `(page_number, chunk_index)` rather than the raw extracted body alone. Image-heavy docs (slide decks, figure-laden papers, OCR-fallback pages) get the VLM's already-paid-for analysis folded into the summary input. Implemented in `_build_summary_input` in `bartleby/commands/scribe.py`: it builds from whatever chunks exist (document, image, or both), so an image-only doc summarizes from its real VLM/Tesseract descriptions rather than the raw `full_text`. The `full_text` fallback is reached only when a doc has *zero* chunks — and `_maybe_summarize` excludes that case outright (see the #80 entry). Decorative-image bloat is absorbed by the `max_summarize_tokens` cap (default 50k).
- **Schema v5 — `summaries.authored_date`**: nullable `TEXT` column populated by the summarizer (additional field on the `DocumentSummary` Pydantic model). One LLM call still produces title/description/text/authored_date — no extra inference cost. Strict `YYYY-MM-DD` validation lives in `bartleby.ingest.summarize.normalize_authored_date`; malformed inputs (`"Q3 2024"`, `"2024-13-01"`, partial dates) silently become NULL rather than failing the ingest. Surfaced in `list_documents` (brief mode) and accepted by `save_summary --authored-date`. Shipped with `_upgrade_v4_to_v5` in `bartleby/db/upgrades.py` so existing corpora can opt in via `bartleby project upgrade <name>`; the column stays NULL until the user re-summarizes.
- **Skill scratch lives in `~/.bartleby/tmp/`, not `/tmp`**: the skill stages finding bodies (`save_finding`/`edit_finding --body-file`) under `~/.bartleby/tmp/`, created mode `700` by `config.ensure_scratch_dir()` which `skill_runner.run()` calls best-effort on every skill invocation (so the dir exists before the agent writes). `/tmp` is world-readable (`drwxrwxrwt`) on macOS, leaking in-progress research notes to other local users on shared machines (issue #13). Scratch is durable throwaway — scripts don't delete the source file after a successful write; the canonical copy is the `body` field returned to the agent. No config knob: `--body-file` still accepts any explicit path.
- **Schema v6 — `tags` + `document_tags`**: a controlled vocabulary the user curates (human-named, human-described) with LLM-assisted assignment against document summaries. All operations live in the skill — there is no parallel CLI surface. The agent drives the workflow; humans direct through conversation. Six new skill scripts (`read_tags`, `add_tag`, `delete_tag`, `rename_tag`, `merge_tags`, `tag`) plus `--tag` filters on `list_documents` and `search`. The classifier reuses the configured summarizer provider/model (no new config knob) via a generic `Provider.classify(prompt, schema)` method. `add_tag` does its own embedding-similarity check (BGE, cosine threshold ~0.85) plus normalized-name exact-match against existing tags, and returns `{status: "conflict", similar_to: ...}` on hit instead of creating — surfaced to the human, not silently overridden. `tag --all` reuses the summarizer model once per document; SKILL.md requires human confirmation before invoking it. Shipped with `_upgrade_v5_to_v6`.
- **Converter axis split + sec2md (issue #14)**: the single `backend` config key and `--backend` CLI flag are split into orthogonal axes — `pdf_converter` (`pdfplumber` / `docling`) and `html_converter` (`docling` / `sec2md`). `sec2md` is a third converter slotted into the HTML branch for iXBRL EDGAR filings; it activates only when `html_converter=sec2md` AND a per-file iXBRL sniff (`xmlns:ix="http://www.xbrl.org/.../inlineXBRL"` in the first ~4KB) hits. Non-iXBRL HTML falls through to docling. Hard rename per the no-backwards-compat rule — users re-run `bartleby config`. No schema change: sec2md chunks land under `source_kind='document'` with new `content_type` values `'sec_text'` / `'sec_table'` (split on `Chunk.has_table`). `section_heading` maps to `Chunk.header` directly (often `None` on 8-Ks, populated when sec2md detects section context). XBRL tags, element IDs, and filing-level metadata (CIK / ticker / form type / filing date) are deliberately deferred — they want a schema bump and belong to #10. SGML `.txt` full-submission wrappers were originally out of scope here; they are now unwrapped and routed by a dedicated parser (see the #51 entry below). New optional dep: `bartleby[sec2md]`, pinned `sec2md==0.1.22` (solo dev, alpha, no tagged releases — explicit upgrade control).
- **Manual tag assignment — `assign_tag` / `unassign_tag` (issue #35)**: two skill scripts that attach/detach a single `(document, tag)` pair directly, with no LLM. The `tag` classifier only reads document *summaries*, so body-level/intrinsic categories it structurally cannot see — OCR quality, language, "contains tables" — are unreachable through it (summaries are reconstructed clean prose; the defining signal is scrubbed out). These primitives record membership the agent/human determined out-of-band (body scan, deterministic rule), which is the only path for such tags. They reuse the existing `assign()` / `unassign()` helpers in `_tags.py`; `assign_tag` is idempotent (`INSERT OR IGNORE`), `unassign_tag` is a no-op when absent and — unlike `delete_tag` — detaches one document rather than cascading the whole tag. No DB or schema change. Deliberately not built: a provenance column distinguishing manual from LLM assignment (assignments are assignments), a body-aware classification mode for `tag` (deferred — an LLM is the wrong tool for the motivating `bad_ocr` case anyway), and multi-*tag* forms (still one tag per call). The "single doc; agents loop" stance was reversed by issue #68 (see entry below) once an agent hit 63 process spawns on one tagging pass — the multi-*document* batch form is now the only form.
- **`list_findings` / `read_finding` — finding read paths (issue #40)**: two skill scripts that browse and read findings, mirroring the `list_documents` / `read_document` pair but for the agent's own memory. The data was already intact (`findings.body` holds the whole markdown; `finding_citations` maps each finding to its cited chunks) — the gap was read access: `list_documents`/`read_document` are documents-only, and `search --findings` returns ranked *fragments*, not "show me finding N" or "what findings exist." `list_findings` enumerates newest-first (`ORDER BY finding_id DESC`) with `citation_count` and the `list_documents` pagination/`hint` idiom; `read_finding` returns the full body in the same shape `save_finding`/`edit_finding` emit (so the verbatim-echo contract holds), reusing `resolve_citations`. **Memory-off behavior diverges from `search` deliberately**: `search` silently drops findings from a mixed result list (the load-bearing invariant), but these finding-only commands raise an explicit `{"code": "MEMORY_OFF"}` error via a new `require_memory_enabled` guard in `_common.py` — a direct-intent "read my findings" failing silently would be more confusing than honest, and this deliberately reveals that findings exist (accepted tradeoff). No schema change; both wired into the dispatcher `SCRIPTS` tuple. Out of scope: filtering `list_findings` by session/tag, and any change to `search --findings`.
- **`--help` prints the response shape, not just flags (issue #47)**: every skill script already documented its JSON return contract in its module docstring's `Output:` block, but the agent never saw it — it reads SKILL.md and `--help`, and `--help` showed arguments only. The contract was half-invisible at the moment it was needed (observed: an agent assumed `scan`'s hits were under `results` like `search`, hit `KeyError`, recovered by dumping keys). Fix: a shared `build_arg_parser(prog, doc)` in `skill_runner.py` that sets the module docstring as the parser `description` with `RawDescriptionHelpFormatter` (so the `Output:` JSON block isn't reflowed); all ~18 scripts construct their parser through it passing `__doc__`. The docstrings stay the single source of truth — no second contract to drift, which is exactly why the "Returns: block in SKILL.md" option was rejected. The raw-formatter+description pairing is a correctness invariant (description without the raw formatter mangles the JSON), so it's centralized in the helper rather than copied to each call site. SKILL.md now tells the agent to run `--help` before parsing a script's output for the first time and flags that top-level keys differ by script on purpose (`search`→`results`, `scan`→`matches`, `list_documents`→`documents`, `list_findings`→`findings`). The `--schema` flag option was rejected as the most code for no more benefit — it would invent a new machine-readable schema per script as a second source of truth when the docstrings already exist.
- **`scan` — full-text enumeration primitive (issue #32)**: a new skill script that *filters* rather than *ranks*. Where `search` fuses FTS+semantic via RRF and returns a ranked top-N, `scan` runs FTS5 `MATCH` only and returns *every* matching chunk corpus-wide in `(source_id, chunk_index)` order, paginated with a true `total` so termination is knowable. Documents only (`source_kind='document'`, so `source_id` *is* the `document_id` and grouping needs no join beyond `documents` for `file_name`); summaries/findings/images are never returned. Literal **phrase** match by default (the whole query wrapped as one FTS5 phrase, embedded quotes stripped); `--match-terms` switches to the implicit-AND-of-quoted-tokens behavior `search` uses. Compact by default — each hit's `text` is truncated to `--preview` chars (default 240) with `text_length`; full bodies come from `read_chunks --chunks`. `--in-documents` / `--tag` scope it exactly as in `search` (the `_intersect_tag_filter` *shape* is intentionally duplicated across the two scripts rather than hoisted — two ~6-line call sites, no speculative shared helper). **Deliberately NOT done: `--offset` on ranked `search`** — paginating a fused/semantic RRF ranking has no meaningful termination (you can't distinguish "no more matches" from "scores went low"), so enumeration got its own deterministic filter instead. Rank≈filter only holds for literal phrases on templated corpora (EDGAR/OGE/PACER); on a heterogeneous corpus `scan` degrades to `grep` and `search` is the right tool. Wired into the dispatcher `SCRIPTS` tuple in `bartleby/commands/skill.py`; no schema change.
- **Schema v7 — finding provenance (`sessions.model` / `sessions.harness`, issue #62)**: findings were attributed to a session but carried no record of *which backend* produced them, which defeats the corpus's purpose (multi-model/multi-harness report evaluation — Claude Code vs local Ollama vs Goose/Pi — stored side-by-side in `findings`). Two nullable columns land on `sessions`, not `findings`: a session is "one run under one backend," every finding it authors shares that backend, and the finding-returning scripts already join `sessions` for `session_name`, so the surfacing is additive (a new `session_provenance` helper in `_common.py` feeds `save_finding`/`edit_finding`/`read_finding`; `list_findings` adds two columns to its existing join). **Attribution is session-level, deliberately not per-finding** — per-finding columns would be redundant denormalization and push the burden onto every `save_finding` call. **Population, ranked by trust**: explicit `bartleby session start --harness --model` (authoritative); best-effort `detect_harness()` reading a small env-signature registry (`CLAUDECODE` → `claude-code` for now, more added as confirmed) when `--harness` is omitted, explicit always overriding; and `bartleby session set` to stamp/correct after the fact (supports the *blind*-comparison workflow: start unset, reveal the backend after assessing). Model rarely has an env signal, so it's explicit/post-hoc only. **NULL = unknown, never fabricated** — an unrecognized harness yields null, not a wrong label (same honesty discipline as `authored_date`). *Asking the model to self-report its own model/harness was rejected as a source of truth* (models are unreliable about their own id and blind to the wrapping harness). Shipped additive via `_upgrade_v6_to_v7` (`ALTER TABLE sessions ADD COLUMN ...`), runnable through `bartleby project upgrade` with no re-ingest; the `test_upgrade_chain_walks_*` gate extended to v7. Surfaced in each script's `--help` `Output:` block and SKILL.md per the #47 contract.
- **EDGAR full-submission `.txt` unwrap (issue #51, Phase 1)**: an EDGAR full submission is an SGML envelope (`<SEC-DOCUMENT>` / `<SEC-HEADER>` wrapping N `<DOCUMENT>` blocks). Left to the generic dispatch it fell to the plain character chunker and the agent got raw iXBRL/HTML tag soup with tables shattered cell-by-cell. New `bartleby/ingest/edgar.py` `detect()`/`parse()`/`classify()` splits the envelope and routes each inner document: HTML/iXBRL bodies → sec2md (via a new `sec2md.convert_bytes(html: bytes)` seam, since inner bodies live in memory after the split), plain-text exhibits → the character chunker, graphics (uuencoded / `<TYPE>GRAPHIC`) and XBRL data files (`.xml`/`.xsd`, `EX-101.*`) → skipped with a stderr note. `detect()` is **content-based and extension-independent** (sniffs the envelope markers in a head window), so a `.txt` is caught before the final `else`; the branch sits ahead of the existing `.htm` sec2md branch in `_process_one`. The unwrap also dissolves the issue's secondary "4 KB iXBRL sniff window misses the deep marker" bug for free — each inner body is sniffed from its own byte 0. **One-document model**: the whole submission lands as a single `documents` row (file_hash / archive / one-summary invariants untouched); inner-doc origin is recorded in `section_heading` (sec2md's header, falling back to the inner `<TYPE>`), and chunk indices run continuously across inner docs. **Routing principle — require a converter only when it's the *sole* path to usable output**: inside a submission, inner HTML always goes to sec2md regardless of `html_converter` (the alternative is tag soup, not docling — no working fallback, so the dependency is hard; missing `[sec2md]` raises the existing install hint). This is deliberately asymmetric with the standalone `.htm` path, which keeps honoring `html_converter` and the per-file iXBRL sniff because docling *is* a working fallback there. No schema change (`SCHEMA_VERSION` stays 6); `<SEC-HEADER>` metadata capture (CIK/ticker/form type/date) and the search filter layer remain Phase 2 / #10.
- **Web UI search reuses the skill scripts via subprocess (issue #65)**: the SvelteKit UI gained `/search` — ranked `search` (FTS+semantic RRF) and enumerating `scan`, with source-kind / `--tag` / `--in-documents` / `--add-context` filters and per-hit chunk expansion (`read_chunks`). The web does **not** reimplement any ranking: semantic search needs BGE query embeddings that only exist in Python (`search.py` shells out to `bartleby embed`; the "no daemon" call stands), so a JS port could only ever do FTS and would fork the logic. Instead a single server seam, `web/src/lib/server/skill.js:runSkill(name, args)`, spawns `bartleby skill <name> … --project <active>` with list-form argv (no shell), parses the JSON envelope, and surfaces `{error, code}` as a `SkillError`; routes render what the scripts already emit. Cheap browse queries still use the read-only `better-sqlite3` connection (`db.js`), so the DB is read-only *for browsing* but the search subprocesses open it read-write (session + audit rows). **Session model for a non-agent caller**: skill scripts always run inside a session, and findings visibility is gated on `memory_enabled`, but sessions were designed to track *agents*, not the human at a browser. So `runSkill` sets `BARTLEBY_SESSION_NAME=web-reader`; the runner (`skill_runner.py`) honors that env var by calling the new `session.ensure_named_session(project, name)` — a find-or-create of one durable, memory-enabled session by fixed name that **never reads or writes the `.active_session` pointer**. Net: the web always sees findings, never hijacks (or is hijacked by) the agent's active session, and its audit-log reads are attributable to a recognizable session. The env var (not a `--session` CLI flag) was chosen so the override stays off the agent-facing `--help` surface and needs no per-script parser change. Latency (BGE cold-load per query) is accepted as-is with a loading state in the UI. No schema change (`SCHEMA_VERSION` stays 6). Deliberately not built: writing from the web (save/edit findings, tagging), an embedding daemon, and deep file links for summary/image hits (the `search` output doesn't expose their `document_id`, and surfacing it would be a script change out of scope here).
- **Tags surfaced in the web UI (issue #66)**: the SvelteKit UI gained tag visibility in three places, all `web/`-only and read-only (no Python, no schema change, `SCHEMA_VERSION` stays 6). (1) **Document chips** — `listDocuments`/`getDocument` (`queries.js`) now attach a `tags` array via a shared `TAGS_JSON_SUBQUERY` (a two-level correlated `json_group_array(json_object(...))` ordered by `name COLLATE NOCASE`, parsed by a `withTags` helper); chips render under the meta line on the list and below the description on the detail page, with the tag's description as a native `title=` tooltip. Untagged documents (the common case) render nothing. (2) **`/tags` index** — a new route backed by `listAllTags()`, which mirrors the `read_tags` skill's SQL: it keeps the `description` and, unlike the pre-existing `listTags()` (kept as-is for the search filter, where empties are noise), retains zero-document tags via `LEFT JOIN` — a vocabulary entry is worth showing before anything is assigned. (3) **`/tags/[id]` detail** — `getTag()` returns the tag plus its documents in the exact `listDocuments` shape; the route reuses a new shared `DocumentList.svelte` component (extracted from the documents list, so chips render identically in both places). Chose the `/tags/[id]` shape over a `/documents?tag=` filtered view because it is self-contained and the nav's existing `isActive()` child-path match lights the Tags link for free. The `stripExt` title helper moved to `web/src/lib/format.js` (shared by the list component and the detail page).
- **Batch tag assignment — `assign_tag` / `unassign_tag --documents <id,id,...>` (issue #68)**: every `bartleby skill <name>` call is a cold `uv run` + Python boot, so a per-document operation over a corpus multiplies into N process spawns. An agent doing one tagging pass made 63 `assign_tag` calls — slow enough that the cost actively shaped its approach. Fix: both scripts take a single tag and a comma-separated **list** of document ids (`comma_int_list` from `_common.py`, the same parser `read_chunks --chunks` / `scan --in-documents` use), amortizing interpreter startup over the whole set in one boot. The singular `--document` was **replaced**, not supplemented (no-backwards-compat rule) — a one-document call is `--documents 9`; the `tag` classifier keeps its own singular `--document`/`--all` scope since it's a different operation. Per-document resolution is one `WHERE document_id IN (...)` via the new `_tags.resolve_documents()` (which also dedups and preserves input order); ids with no document land in a `not_found` list and are skipped **without aborting** the rest of the batch — the per-document analogue of `tag`'s `failed` list. The flat single-pair output (`{document_id, file_name, tag_id, tag, assigned: true}`) became `{tag_id, tag, assigned: [{document_id, file_name}, ...], not_found: [...]}` (and `unassigned` for the inverse). Idempotency is unchanged (`assign()` is still `INSERT OR IGNORE`; `unassign()` still no-ops on an absent pair). No DB or schema change. Still not built: the optional generic batch/stdin "list of arbitrary operations in one boot" from the issue — the multi-document form removes essentially all the friction the issue cites, and a general op-stream is a much larger surface for speculative benefit.
- **Content-sniffed file-type resolution (issue #78)**: ingest type detection was purely extension-based (`path.suffix.lower()` against `SUPPORTED_EXTENSIONS`), so a valid file with a missing/mangled extension was rejected (single-file) or **silently dropped** (directory `rglob` filter) — the motivating case was an NTSB docket of 16 PDFs whose scraper truncated long filenames, lopping the `.PDF` off 7 of them (6 left with empty `suffix`, 1 with junk `.1_ january` because the last dot fell inside `5800.1`). New `resolve_extension(path)` in `bartleby/ingest/chunk.py` is the single resolution seam: a **recognized extension is trusted as-is and never second-guessed from content** (a `.txt` holding PDF bytes stays text — pinned by `test_resolve_extension_does_not_override_recognized_extension`); only a missing/unrecognized extension is resolved by sniffing magic bytes via the `filetype` library (pure-Python, no system dep), mapping back to a `SUPPORTED_EXTENSIONS` member or `None`. **Sniff only when info is missing, never as a fallback to override a recognized-but-"wrong" extension** — that's the deliberate asymmetry the user asked for. `_collect_files` now returns `(sources, unidentified)` where `sources` are `(Path, resolved_ext)` pairs; the resolved ext is threaded through `_archive` (so the archived copy is named correctly) and `_process_one`'s dispatch (replacing the old in-line `path.suffix`). **Unsupported files are still skipped, but no longer silently** — directory ingest surfaces a capped list of files it couldn't identify (single-file still raises `ValueError`). This composes with the EDGAR `detect()` path below: a recognized `.txt` resolves to `.txt`, then content-routing inside `_process_one` still catches an SGML envelope. No schema change. New runtime dep `filetype>=1.2.0`. **Tangent folded in**: the `wsjpt` optional extra (git source on WSJ-internal `github.dowjones.net`) was removed from the locked dependency set — it is unreachable off-VPN and broke `uv lock`/`uv sync` for everyone; it is now an out-of-band `uv pip install 'git+ssh://…/wsjpt.git'` (the provider already imports `wsjpt` lazily with an install-hint `RuntimeError`, so nothing else changes).
- **Findings curation — `delete_finding` + `merge_findings` (issue #56)**: findings previously had a write path (`save_finding`/`edit_finding`) and read paths (`list_findings`/`read_finding`) but no *curation* path, so duplicates and zero-citation drafts accreted with no way out — the asymmetry the issue called out against tags' full lifecycle (`delete_tag`/`merge_tags`). Two new skill scripts close it. `delete_finding --finding <id>` retracts one finding: `delete_chunks_for(conn, "finding", id)` clears its body chunks (+ `chunks_fts`/`chunks_vec`) and the `findings` row delete cascades `finding_citations` via the FK. `merge_findings --from <ids> --into <id> --body-file <path>` collapses a cluster: the `--into` target is kept (its `finding_id`/provenance/authoring session survive), the agent **authors** the consolidated body (validated by the same `[^N]` rules as `save_finding`), the target's body+citations are rebuilt, and the `--from` sources are deleted. Both touch only finding rows — cited *document* chunks are never affected, because findings are derivative hints, not evidence. Mutations run inside `with conn:` for atomicity (apsw nests the helpers' own transactions via savepoints). No schema change (`SCHEMA_VERSION` stays 6). **Deliberately NOT built** (scoped out by the user, against the issue's broader menu): the dedup-on-save similarity check on `save_finding` (no `--force`), and the supersede relationship (`superseded_by` column / `list_findings --include-superseded`) — the issue floated both, but delete + author-merge cover the motivating 5/6/7 and 20–23 cluster cases without a schema change or a new soft-delete state to reason about. Mechanical concat-merge (append source bodies) was rejected in favor of the authored body: auto-concatenating four report iterations yields an ugly low-quality finding, and the agent rewriting one clean consolidation is the actual goal. Neither command gates on `memory_enabled` — they act on an explicit `finding_id` like the `save`/`edit` write siblings (only the *reveal* commands `list_findings`/`read_finding` raise `MEMORY_OFF`). The shared "read body file → validate citations" logic (duplicated across `save_finding`/`edit_finding`, and a third copy waiting in merge) was hoisted to `_common.load_finding_body` / `_common.validated_replacement` in the same change.
- **No summary for zero-chunk documents + image-only summary input (issue #80)**: the summarizer confabulated complete, plausible, *entirely fabricated* summaries for documents with no extractable content. An image-only / sub-threshold PDF (e.g. an annotated track-map scan) yields a `full_text` of a few trace tokens — page labels, empty page strings joined by `"\n\f\n"` form-feeds — while creating **0 chunks**. That trace is non-empty after `.strip()`, so the old whitespace-only guards (`_maybe_summarize`, `summarize`) waved it through, and the structured-output prompt (no "insufficient content" branch) pressured the model to fill every field → it invented a generic business doc. The summaries were indistinguishable from real ones in `list_documents`/`describe_corpus` and indexed/searchable (`search --summaries`), quietly poisoning triage. **Two coupled fixes**: (1) `_maybe_summarize` now gates on a real chunk count via `_document_chunk_count` (document chunks `source_kind='document'` **plus** image chunks joined through `document_images`) — **0 chunks → skip**, with a stderr `console.warn` so the user can spot the ingestion failure (image-only PDF needing OCR/re-ingest) instead of it masquerading as success. The old `not full_text or not full_text.strip()` check is gone — chunk count subsumes it and correctly admits image-only docs whose `full_text` is empty but which *do* have image chunks. (2) `_build_summary_input` no longer short-circuits to `full_text` when a doc has image chunks but no document chunks (the old `if not doc_rows: return fallback_text` discarded the VLM/Tesseract descriptions and fed the model the same trace garbage — the more common path once a vision provider is configured). It now interleaves whatever chunks exist (document, image, or both); `fallback_text` is reachable only with zero chunks, which (1) already excludes. With (2) in place, `_ingest_image_file`'s post-hoc `full_text` reconstruction from chunks became dead and was deleted (it returns `""`). No write-path validation added on `_write_summary` and no `list_documents`/`describe_corpus` "low extracted content" surfacing — the issue flagged both as optional polish, and skipping at the source means nothing malformed is written to validate. No schema change (`SCHEMA_VERSION` stays 7).
- **Web UI packaged so `serve` works from an installed wheel (issue #87)**: `bartleby serve` was structurally run-from-checkout-only — it located the SvelteKit UI via `Path(__file__).resolve().parents[2] / "web"` (repo root), but `web/` was a *top-level* dir excluded from the build (`packages.find` only takes `bartleby*`), so an installed wheel resolved `WEB_SRC` to a non-existent `site-packages/web` and hard-failed with `FileNotFoundError`. Every other subcommand worked from an install, making this surprising. Fix: the UI moved from `web/` → **`bartleby/web/`** (now package data, declared via `[tool.setuptools.package-data]` scoped to `web/package.json`, `web/svelte.config.js`, `web/vite.config.js`, `web/src/**/*` — recursive `**` needs `setuptools>=62.3.0`, so the build req was bumped). `node_modules`/`.svelte-kit`/`build` are deliberately **not** bundled (dev/build artifacts regenerated at runtime), nor is `package-lock.json` (untracked; npm regenerates it on first install in `~/.bartleby/serve`). `serve.py` now resolves `WEB_SRC` via `importlib.resources.files("bartleby") / "web"` (identical from a checkout or a wheel). The HMR dev loop is **conditional, not removed**: a new `_is_source_checkout()` checks for a `pyproject.toml` two levels above `WEB_SRC` (repo root in a checkout/editable install; absent under `site-packages`), and `_sync_web(..., symlink_src=...)` *symlinks* `src/` for live vite HMR from a checkout but *copies* it from an installed wheel (the packaged source is read-only — symlinking it would be pointless). Verified by installing the wheel into a throwaway venv and confirming the previously-crashing path now syncs. Deliberately NOT changed: a packaged install still runs `vite dev` (not a `vite build`/preview server) — switching the production install to a built server is a separate concern the issue scoped out. No schema change.
- **Semi-automated releases — tags as version pins, minor coupled to `SCHEMA_VERSION` (issue #100)**: real users installing from a git checkout had no way to pin a known-good version and no signal for the one update that actually hurts — a pull that bumps `SCHEMA_VERSION` and forces a re-ingest. Releases are now git tags of the form **`v0.<SCHEMA_VERSION>.<patch>`**: the **minor *is* the schema version** (not merely bumped alongside it), so the version number self-documents the schema (`v0.8.3` = "schema 8, third patch") and a user can read `SCHEMA_VERSION` out of their DB's `meta` and know which minor line their corpus belongs to. Patch increments per release at the same schema and resets on a bump; major stays a manual human call. Three pieces: (1) **`setuptools-scm`** derives the package version from the latest tag (`dynamic = ["version"]`, static `version`/`__version__` deleted; `__init__.__version__` now reads `importlib.metadata`), so the release script's *only* write is `git tag` — no two-places-to-drift. Off a tag, dev builds report `0.7.0.devN+g<sha>`, surfaced by the new `bartleby --version`. (2) **`scripts/release.py`** (a standalone maintainer chore, deliberately **not** a `bartleby` subcommand — it stays off the end-user CLI surface) computes the next version with no diffing, since the constant *is* the minor: read `SCHEMA_VERSION`, parse the last `v0.*` tag, bump. It tags, and on `--push` publishes a GitHub Release with notes auto-generated from `git log` (a re-ingest banner prepended on a minor bump). (3) A **drift guard** in the same script diffs `schema.py`'s DDL (via AST, not regex) against the last tag and **refuses to tag** when the DDL changed but `SCHEMA_VERSION` did not — the one mistake the version arithmetic can't see (a breaking change mislabeled as a safe patch). The pure functions (version arithmetic, schema parse, drift, notes) are unit-tested; git/gh side effects are not. Baseline tag is **`v0.7.0`** (schema is 7), cut from `main` post-merge by running the script (dogfooding it). **Deliberately NOT built**: a hand-maintained `CHANGELOG.md` (auto-generated GH Release notes replace it — zero upkeep) and PyPI publishing (the unreachable WSJ-internal `wsjpt` git dep and the `--with` install dance make PyPI awkward, and git-tag installs already give real pins). No schema change.
- **`--brief` triage projection on the four enumeration commands (issue #105)**: triage over a large corpus means many list/search calls, and the high-token enumeration commands returned more per row than triage needs (an agent on a 707-doc corpus hit the output cap on `list_documents --limit 200` and resorted to parsing the spilled JSON by hand). The fix **standardizes the vocabulary, not the behavior**: a `--brief` flag on `search`, `list_documents`, `scan`, and `list_findings`, each documenting *its own* skinniest-useful per-row projection — `search` → `chunk_id`/`source_kind`/`source_name`/`page_number`/`rank`/`normalized_score` + a truncated `text` preview (drops the full text, context arrays, and image locators; `--add-context` is ignored under `--brief`); `list_documents` → `id`/`file_name`/`title`; `scan` → locators only (`document_id`/`file_name`/`chunk_id`/`page_number`, dropping the snippet + `text_length`, so `--preview` is moot); `list_findings` → `finding_id`/`title`/`citation_count`. **One flag name, per-command semantics — deliberately not one shared projection helper** (the field sets have nothing in common; a shared abstraction would be indirection for its own sake). The **envelope is invariant**: pagination (`total`/`offset`/`limit`/`hint`) and status flags (`memory_excluded`/`excluded_null_dated`) are always present, and `--brief` adds **no** echo field — default output shapes stay byte-for-byte unchanged (so brief is purely additive, no compat shim needed). On `list_documents`, `--brief` is a third tier *below* the existing default, mutually exclusive with `--verbose` via an argparse `add_mutually_exclusive_group()` (exits 2 like other arg-level errors); `--verbose` stays the only "more columns" knob since it's the only command with extra columns. **Out of scope**: `read_chunks`/`read_document`/`read_finding` (full-content-by-contract — a brief tier is meaningless or contract-breaking there), and a generic `--fields a,b,c` selector (canned `--brief` is a simpler, more predictable contract for an agent; revisit only if the canned projections prove too rigid). The per-script loop shape diverges on purpose: `search`/`list_documents` use a brief-branch-with-`continue` because their loops do per-row work brief skips (context fetch / image locators; `--verbose` extension), while `scan`/`list_findings` use two list comprehensions because their loops *only* project. No schema change.
- **Web view delegates read parity to the skills — home + `/documents` (issue #110)**: the web view (`bartleby serve`) lagged the CLI's read surface — the home page was a bare doc/finding count and `/documents` dumped every row, unfiltered and unpaginated. The fix brings the two highest-value gaps to parity **by delegating to the skill scripts via `runSkill`, not re-deriving their SQL in JS** — the same seam `/search` already uses. The dividing principle (settled after weighing "always delegate"): **delegate where the skill owns semantics we'd otherwise duplicate** (ranking, filtering, date/null handling, polymorphic-chunk logic), **keep direct SQL for cheap display reads and the enrichment layer the skills can't give us**. Not "always delegate" — a Python subprocess per request buys nothing on a trivial read, and the skills emit *agent-shaped* JSON so a JS enrichment/SQL layer survives regardless (`enrichHits` is already subprocess + `titleMeta` SQL). `/` now renders `describe_corpus` (token/chunk totals, authored-date range **always paired with the undated count** so a range can't overstate coverage, year histogram, summary coverage, content mix, tag chips, largest docs), falling back to nav cards on skill error. `/documents` renders `list_documents` with authored-date filters (+ include-undated toggle, surfacing `excluded_null_dated`), tag filters, pagination, and sort. Two display gaps the agent-shaped skill doesn't cover, handled web-side: (1) **tags** — the skill returns none, so a batched `tagsByDocument()` query re-attaches chips (the `titleMeta` pattern); the now-unused `listDocuments()` SQL was deleted (no-dormant-code). (2) **sort** — the skill ordered only by `document_id` and sort must precede `LIMIT/OFFSET` (can't be fixed in JS under pagination), so a new **`--sort {id,title,date}`** flag was added to `list_documents.py` (default `id` preserves the agent-facing ingest order; web passes `title`) — a small, agent-useful addition to the skill's contract, every option breaking ties on `document_id` for stable paging. Shared `toSkillError()` (error-envelope normalization) and `clampInt()` (query-param parsing) were hoisted out of the search loader into `skill.js` / `params.js` rather than copied. The web DB stays **read-only by design** — no mutations, no session/project management. **Deliberately deferred** (parity backlog, not this issue): `read_document --full` extracted-text view and `read_chunks` chunk browse (P2); a `logs` audit-log viewer and `project info` panel (P3); any write path (findings/tags/summaries mutation, LLM `tag`), which would require dropping read-only mode and a web write/audit story. No schema change.
- **Memory-off reads scoped to own session, not blocked (issue #108)**: a memory-off (`--no-memory`) session could `save_finding` (no guard) but `read_finding`/`list_findings` hard-failed with `MEMORY_OFF` — a write-only state where a run couldn't even read back the finding it just wrote. The point of memory-off is **no-contamination** (don't bias an evaluation run with prior conclusions), and that only requires walling off *other* sessions' findings, not one's own. So the two direct-read commands now scope to the active session instead of refusing outright: `read_finding` fetches the row first (so a bad id is still `FINDING_NOT_FOUND` regardless of memory state) and raises `MEMORY_OFF` only when the finding was authored by a *different* session; `list_findings` adds `WHERE f.session_id = ?` when memory is off, with `total`/pagination reflecting that scoped set. `save_finding` is unchanged (stays open — writing is how a run produces its comparison artifact for the multi-model eval), and **`search --findings` is unchanged**: it still silently drops *all* findings in memory-off, because ranked retrieval surfacing a finding as if it were corpus evidence is the real contamination path (the load-bearing invariant). The old `_common.require_memory_enabled` hard-block (from issue #40) is **deleted**, replaced by a boolean `_common.memory_enabled(conn, session_id)` that both read commands branch on and `search.py` reuses in place of its duplicated inline `SELECT memory_enabled`. The cross-session existence leak (a memory-off caller learns another session's finding *exists* via `MEMORY_OFF` vs `FINDING_NOT_FOUND`) is the same tradeoff #40 already accepted for direct-intent reads. No schema change.
- **Aggregation/facet — `scan --count-by document` + filtered `describe_corpus` (issue #104)**: two decisive "quantify a pattern" needs an agent kept rolling Python over the existing primitives — (A) "how many *documents* match this marker?" (`scan` gave a chunk `total`, then `len(set(document_id))` by hand) and (B) "what's the content/year/tag mix of *this slice*?" (`describe_corpus` faceted beautifully but corpus-wide only). Solved as **extensions, not a new verb**. (1) `scan --count-by document` (single-valued enum, room to grow) flips to aggregate mode: instead of `matches` it returns a per-document hit histogram (`documents: [{document_id, file_name, chunk_count}]`, `ORDER BY chunk_count DESC, document_id`, paginated by document) plus two always-full rollups — `distinct_document_count` (**the headline** the agent reports in prose) and `total_chunk_count` (the old `total`). Both present so the agent never guesses which `total` meant. Conflicts with `--preview`/`--brief` (they only shape per-chunk matches) → argparse `p.error` exit-2 (chosen over silent-ignore). (2) `describe_corpus` gained the same scope flags `search`/`scan`/`list_documents` take (`--tag`, `--in-documents`, date bounds) and recomputes *every existing* facet over the slice — **not** a general GROUP-BY engine, the four dimensions are fixed. The filtered `tags` facet lists **only tags present in the slice** (INNER JOIN over slice docs) vs the corpus-wide LEFT-JOIN-all-tags; `content_mix` reaches image chunks through `document_images` since they key on `image_id`, not `document_id`; `document_count`/`token_count` come from the `documents` table (not `len(scope)`) so unresolvable `--in-documents` ids can't inflate the count; an empty slice short-circuits to an all-zero overview. (3) **Refactor — `_tags.resolve_scope`**: the date-filter logic (`--authored-after`/`-before`/`--include-nulls` validation + the `IS NULL`/`excluded_null_dated` bookkeeping) was inline in `list_documents`; hoisted into a `Scope` dataclass + `resolve_scope` that folds tag ∩ in_documents ∩ date into one resolved `document_ids` set (None = whole corpus, `[]` = matched-nothing short-circuit) and computes `excluded_null_dated`. All three scripts call it; `list_documents` switches to the resolved-id path with **byte-identical output** (the issue mandated no behavior change). This **reverses the #32 "don't hoist `intersect_tag_filter`, two call sites isn't worth a helper" stance** — now three consumers plus a date dimension justify it (`resolve_scope` wraps `intersect_tag_filter`, leaving `search`'s use of the latter untouched). **Echo consistency**: a `filters` *object* (`{tags, in_documents, authored_after, authored_before, include_nulls, excluded_null_dated}`) appears on `scan` and `describe_corpus` only when a filter is active — nested rather than top-level because `describe_corpus`'s top-level `tags` key is already the distribution facet (a flat `tags` echo would collide), and chosen across both for cross-command consistency. `scan`'s old always-present top-level `in_documents`/`tags` moved *into* `filters` (no-backwards-compat rule). `list_documents` keeps its top-level `excluded_null_dated` — the one accepted inconsistency, forced by the issue's no-behavior-change constraint. The shared `--authored-after`/`-before`/`--include-nulls` argparse trio lives in `_common.add_date_filter_args`. No schema change.
- **Displayed HTML source can't run its scripts (issue #121)**: the web viewer served `.html`/`.htm` source documents as `text/html` inline (`files/[document_id]/+server.js`) and embedded them in a bare `<iframe src="/files/<id>">`, so an ingested HTML document's `<script>` executed in the app's origin — a stored-XSS vector (source documents are evidence to *read*, never to *run*). Fixed in two complementary layers: (1) the `/files` route adds `X-Content-Type-Options: nosniff` to every response and `Content-Security-Policy: sandbox` to **HTML responses only** — the empty `sandbox` directive neutralizes scripts whether the doc is framed or opened directly in a tab, and HTML is the only script-bearing source type so PDFs/images/text are untouched; (2) both viewers (`documents/[id]`, `findings/[id]`) sandbox the iframe for **non-PDF sources only** (`sandbox={isPdf ? undefined : ""}`, `isPdf` derived from the document / active-citation `file_name`) — PDFs keep the browser's native viewer and `#page=` jumps, everything else renders inert. No `allow-scripts` anywhere; unknown/missing filename → sandboxed (safe default). Verified end-to-end against a throwaway project: an HTML doc's `/files` response carries CSP+nosniff and its `/documents` iframe SSRs `sandbox=""`, while a PDF carries nosniff only and its iframe has no `sandbox`. **Deliberately scoped out (issue #123)**: the `{@html}` markdown render paths (document summary — model-generated, hence prompt-injectable — and finding body) are a *separate* stored-XSS surface, untouched here because sanitizing them must preserve the cite-chip `<button>`s injected through that same `{@html}`. No schema change.
- **Web style standardization — design tokens + extracted primitives (issue #122)**: the SvelteKit UI's styling foundation was sound (CSS-var tokens in `app.css`, shared `.markdown-body`/`.split`/`.entity-list`) but had drifted — the same visual concept existed in 2–4 slightly-different copies (tag chip styled two ways, the error box copy-pasted across three routes, the submit button defined three times, the filter-form chrome duplicated between `/search` and `/documents`, and `SearchResult`/`ScanMatch` ~90% identical). Consolidated, `web/`-only, no Python/schema change. **Three move types, by altitude.** (1) **Tokens** — `app.css:root` gained a full scale: text/surface/danger colors, a `--space-*` ramp, a `--text-*` ramp, `--radius-*`, `--tracking-wide`, and one `--width-content`. Every route `<style>` was converted; the only raw `rem` left are *structural dimensions* (input widths, grid track sizes, fixed heights, the deliberately-wider 52rem home dashboard) — that boundary (rhythm tokenized, dimensions literal) is the rule for future work. (2) **Components — only for markup+behavior+variants**: `Button.svelte` (md/sm), `StatusBanner.svelte` (error/busy), and `ResultCard.svelte` (search/scan, merging the two deleted result components). (3) **Utility classes — for presentation-only primitives**: `.surface` (the off-light card, now *composed* via `class="entity surface"` on the document/finding/tag list items rather than re-declared), the unified `.tag` chip, the shared filter-form chrome (`.filters`/`fieldset`/`legend`/`.checks`/inputs), and the text atoms (`.meta`/`.empty`/`.summary`/`.note`/`.muted`/`.code`/`.desc`). **Deliberately not components** (the one judgment call): Surface/Chip/form-chrome are classes, not `.svelte` files — a component there adds only import noise since there's no markup or behavior to encapsulate. Net −254 lines; verified by `npm run build` (clean, no unused-selector warnings) and SSR-rendering every route against a live corpus. No schema change.
- **`scan --sort {document,date}` — chronological enumeration (issue #118)**: `scan` enumerated in a fixed `(document_id, chunk_index)` order; the one analyst move that order can't serve is "walk every match chronologically" on a templated corpus (every filing mentioning X, oldest→newest). Added a single-valued-now enum `--sort` (matching `--count-by`'s room-to-grow shape). `document` (default) **is** the prior order — output stays byte-identical, no compat concern. `date` is a **document-level reorder**: it sorts the doc *groups* by `authored_date` while keeping chunks positional within each doc (`(s.authored_date IS NULL), s.authored_date, c.source_id, c.chunk_index`), so the document-grouping contract and `total`-based termination survive. **Deliberately oldest-first — the inverse of `list_documents --sort date`'s newest-first** — because scan's use case is reading a corpus forward in time, not triaging the latest; the divergence is intentional and documented in both `--help` surfaces. `authored_date` lives on `summaries` (schema v5) and is NULL-heavy (summarizer-inferred), so both queries gained a `LEFT JOIN summaries s ON s.document_id = c.source_id` (explicit `ON`, not `list_documents`' `USING` — scan's left key is `chunks.source_id`, not `document_id`) and a leading `(authored_date IS NULL)` term floats undated docs last, with `(document_id, chunk_index)` as the deterministic tiebreaker so pagination stays stable. `--sort` applies to `--count-by document` too (the issue left this optional — taken for consistency): `date` reorders the histogram chronologically instead of by hit count. Two ORDER BY maps (`_CHUNK_ORDER_BY`, `_DOC_ORDER_BY`) rather than one because the per-chunk and histogram queries have genuinely different `document` orders (`source_id, chunk_index` vs `n DESC, source_id`) and tiebreakers — collapsing them would need conditional suffixing, more complex not less. The `scan` envelope does **not** echo `sort` (matching `list_documents`, which doesn't either). No schema change.
- **Two-tone-by-meaning surface system — the visual unify pass (issue #127)**: #122 built the token/primitive *plumbing* but deliberately left the *look* alone, so the UI still read as three apps — a polished warm home page, a wall of fully-saturated mint `.surface` cards on every list/detail page, and a third plain-white treatment for search results — with amber (`--color-token-dark`) overloaded as chip *and* link *and* citation color (amber-as-text fails contrast on white). This pass spends the #122 capital on appearance; `web/`-only, no Python/schema/behavior change. **The organizing idea: color is a signal, not decoration.** `.surface` is now the *warm* canonical card (source material the agent works over — documents, search hits, tags, the home dashboard ground); `.surface--finding` is the *mint* variant (the agent's own output — saved findings). The two are **identical but for hue** (same `--radius-lg`, padding, resting `--shadow-card`) precisely so the colour reads as meaning rather than drift; a reader learns the split on the home page (mint Findings card, warm Documents card) and it holds on `/findings` + finding-detail (mint) vs `/documents` + doc-detail + `/search` + `/tags` (warm). **Three supporting moves.** (1) **One surface, two hued variants + one interactive cue**: `.surface--interactive` (the shared border-brighten + shadow-lift hover) goes on cards that lead somewhere (list rows, home cards) and is omitted on static panels (the detail-page report, which is a page not a link); the home cards stopped re-declaring their own box and now *compose* the primitive (`class="surface surface--interactive"`). The faint resting `--shadow-card` is what gives a list figure/ground — it fixes the flat "wall of mint" without changing the palette. (2) **Contrast pass — split amber's jobs**: amber stays for *fills/borders/data* (tag chips, the cite-chip, histogram bars, Doto numerals) but is banished from text; two new tokens carry the readable cases — `--color-link` (dark sage) for navigation (markdown links, home list links, pagination, result titles) and `--color-token-text` (dark amber) for amber-*as-text* accents (`.code`, `.note`, the result `.page-hint`, search's findings-excluded note). (3) **Detail pages made symmetric**: document-detail's prose moved from bare-on-white into a warm `.surface`, mirroring finding-detail's mint panel — same two-pane object in its two colours. **Card radius unified** on `--radius-lg` (the home card's, the polished reference), retiring the `--radius-md` boxes. Sage-as-muted-text (`.meta`/`.summary` at small sizes) was left as-is — it's the established secondary-text language, not an amber-contrast offender, and recoloring it was out of scope. Verified by clean `npm run build` (no unused-selector warnings) and headless before/after screenshots of all eight routes against a live corpus. No schema change.
- **Sanitize `{@html}` markdown against XSS (issue #123)**: follow-up to #121's iframe sandboxing. The web UI renders `marked.parse(...)` through `{@html}` at two sites — the document **summary** (`documents/[id]/+page.svelte`, model-generated → prompt-injectable) and the **finding body** (`findings/[id]/+page.svelte`, agent-authored) — and `marked` passes raw HTML through, so an embedded `<script>` executed in the app origin (a stored-XSS surface distinct from the source-doc iframe). Both sites flow through the **one shared `marked` singleton** configured in `+layout.svelte` (`marked.use({ hooks: { postprocess } })`, which already rewrote `<a>` to `target=_blank rel=noopener`), so sanitization lives there once rather than per-route — `DOMPurify.sanitize(html)` from **`isomorphic-dompurify`** (runs in both SSR/node via jsdom and the browser, since the finding body is re-parsed client-side to bake the active-chip class). **Sanitize-then-link-rewrite ordering** is deliberate: `target`/`rel` are appended to the already-clean output so they don't depend on the sanitizer preserving them. Relies on **DOMPurify defaults** (no custom allowlist) — empirically verified to strip `<script>`/`on*` handlers/`javascript:` URLs while keeping the cite-chip `<button>` (`type`/`class`/`data-chunk-id`/`title`, the last via the default `ALLOW_DATA_ATTR`); the chips are injected into the markdown *before* parse (`renderChip`) but are our own trusted HTML and survive the allowlist. Verified end-to-end against a throwaway project: a summary and finding body carrying `<script>`/`<img onerror>` render with the dangerous tags stripped and the markdown (and safe links with `target/rel`) intact. No new client/per-route code — `marked.parse` callers are unchanged. No schema change.
- **Schema v8 — single-writer drain + per-unit restartable ingest (issue #164)**: ingest was parse-and-write interleaved per document, with completeness judged by a coarse `_document_already_ingested` ("any chunk ⇒ done"). That was a *data-loss bug*: a document whose captioning died after its text chunks landed was treated as complete and skipped forever, **permanently losing the missing captions**. Reworked into a producer/writer split that makes ingest resumable by what's *missing*. (1) **One `Writer` (`bartleby/ingest/writer.py`) owns the WAL connection** and is the sole path that persists a unit — parse, caption, or summary — each in its own transaction (all chunk writes still via the `db.chunks` typed helpers). Parse is producer-side and pure (convert + embed, no DB); the per-type `_ingest_*` dispatchers became `_parse_*` returning a `ParsedDocument`. (2) **Atomic per unit, not per document**: `persist_parse` commits the document row + text chunks + *uncaptioned* image rows (`analysis_json IS NULL`) + `document_images` joins as one transaction (never re-parsed); each caption (`persist_caption`, keyed by image byte-hash) and the summary (`persist_summary`) commit independently — a caption failure can't roll back the expensive parse. The VLM call moved *out* of parse: parse archives the prepared JPEG and records the row; the caption stage reads bytes from the archive (so a fresh-process resume works) or an in-process hint (preserving the sparse-page `prefetched_ocr` and sparing a re-read). (3) **Completeness is derived from DB state** — `parsed (documents row exists) ∧ every image row captioned ∧ (summary row exists ∨ summaries disabled ∨ 0 summarizable chunks)` — and `_process_one` runs only the missing stages: a 55-image doc that died at image 30 re-captions 25 and writes the summary; it never re-parses or re-captions finished images. The atomic-parse guarantee means a `documents` row implies a finished parse, so **`_document_already_ingested` and `_cleanup_partial_document` are deleted** (no stranded-partial cleanup — it can't happen). The image-byte-hash dedup is unchanged (a shared image is captioned once; `persist_caption`'s `analysis_json IS NULL` guard makes a re-apply a no-op). (4) **`failed_ingests` (additive table, the schema-v8 bump)** records a deterministically-failing unit (`file_hash`, `file_name`, `stage ∈ {parse,caption,summary}`, `error`, `attempts`, `last_attempt`, PK `(file_hash, stage)`; caption rows key on the *image* hash) so a poison unit is retried up to `MAX_INGEST_ATTEMPTS` (3) then capped — not looped forever — and **surfaced loudly** (end-of-run report + a "Failed units" row in `bartleby project info`), because a silently-skipped unit reading as a green run is the exact trap this fixes. Cleared on success (`clear_failure`). Shipped additive via `_upgrade_v7_to_v8` (just the `CREATE TABLE`) — existing corpora run `bartleby project upgrade`, no re-ingest; the `test_upgrade_chain_walks_*` gate extended to v8. **No functional change on a clean single-producer run** — same end-state; only crash/resume behavior improves. **Prerequisite for the parse pool (#165)**, which will put a real queue + dedicated writer thread in front of the Writer (it must stay the connection's sole owner — apsw connections aren't thread-safe); this PR keeps the producer→Writer handoff synchronous and inline. **Deliberately NOT built**: the queue/writer-thread itself (#165 owns it, and threading a cross-thread error path into `failed_ingests` inside a "no functional change" PR is where the bugs hide), the parse pool, and a `bartleby project failures` listing command (the table is queryable and `project info` surfaces the count).
- **Per-unit ingest provenance + config-drift warnings (issue #171)**: once #164 made ingest resumable across separate `scribe` invocations, a single corpus could be parsed / captioned / summarized by *different runs under different config* (half docling / half pdfplumber, captions from two VLMs) with **no record of which run produced which unit** — corrupting the multi-model eval workflow, and captions in particular recorded *no* model at all. This captures provenance per unit and warns on drift. (1) **`ingests` table — one row per invocation**: `run_id`, `started_at`/`finished_at`, `config_json`, `bartleby_version`, `schema_version`. `Writer.begin_run` inserts it at the top of a run (storing `self.run_id`), `finish_run` stamps `finished_at` in the `finally`. (2) **Per-unit stamping (Option B — each unit carries its producing run)**: nullable `ingest_run_id` FK columns on `documents` (parse run), `summaries` (summary run), and **`chunks`** — the chunk column covers *both* parse chunks and image-caption chunks in one column, since captions are `source_kind='image'` chunks, so it answers "which VLM captioned this figure" / "recaption everything from model X", the real gap. The `Writer.persist_*` methods thread `self.run_id` through the INSERTs and the typed `db.chunks` helpers (`insert_*_chunks` gained a nullable `ingest_run_id`; `insert_finding_chunks` deliberately did *not* — findings come from a research session, not an ingest run, so the column stays NULL there, as it does on any Writer that never opened a run). (3) **Config snapshot is secret-stripped at one chokepoint**: `config.redact_config` drops every key whose name contains `api_key`/`token`/`secret`/`password`/`credential` (match-by-name so a *newly-added* credential field is stripped by default, not leaked until denylisted) — **API keys must never land in the project DB**. The snapshot is the file config overlaid with the run's CLI-resolved converters/provider/model, serialized sorted-key so a later compare is a plain string diff. (4) **Drift is warn-only**: `config.config_drift` diffs the new snapshot against the most recent prior `ingests` row (read *before* this run's row is inserted, so a run never compares to itself) field-by-field — *every* field, not a curated subset — and `scribe` emits one `console.warn` per differing field. You knowingly continue or stop; it never blocks (a mixed-config corpus is sometimes intentional). Units ingested before this feature keep NULL provenance — it's forward-looking. **Schema handling — held at v8 for the #169 omnibus**: the `ingests` table and the three `ingest_run_id` columns are additive DDL, but `SCHEMA_VERSION` is *not* bumped here. The whole concurrent-ingestion omnibus (#169) releases as a unit, so it takes a single v8→v9 bump with one consolidated `_upgrade_v8_to_v9` (CREATE `ingests` + three `ALTER … ADD COLUMN` — SQLite permits the inline FK on ADD COLUMN because the default is NULL; columns are appended at end of each table so the fresh DDL matches the ALTER output) at release, rather than each sub-issue bumping in turn (#171→v9, #165→v10, …). Consequence during dev: a fresh DB carries these structures at v8, but an *existing* v8 corpus must re-ingest to gain them (no upgrade entry exists yet, so `project upgrade` is a no-op for them). **Deliberately NOT built**: the version bump + upgrade-chain entry (deferred to the #169 release), a per-run override of `max_workers`-style knobs (no new behavior here, just provenance), blocking-on-drift (warn-only by design), backfilling provenance onto pre-feature units (forward-looking), and surfacing `ingests` in `project info` or the web UI (the table is queryable; surfacing is a separate concern).
- **Docling backend stops double-parsing for images (issue #2)**: the docling PDF path ran a *full second parse* — a `pdfplumber_pipeline.convert` over the same file — purely to enumerate embedded images for the vision pipeline, on top of docling's already-slow pass. Embedded pictures now come out of the **same docling pass**: `convert()` gained `extract_images`, and when set the converter runs with `generate_picture_images=True` at `images_scale = 150/72` (150 DPI, parity with pdfplumber's `PAGE_RENDER_DPI`); a new `_iter_picture_images(doc)` walks `DoclingDocument.pictures`, pulls each raster via docling's own `pic.get_image(doc)`, takes the page from `pic.prov[0].page_no`, and assigns a per-page 1-based `image_index_on_page` (the image-pipeline convention) — surfaced as `DoclingResult.images: list[DoclingImage]` (png_bytes + page + index), which `_ingest_pdf_docling` maps straight to `_ImageRoute`s. **Picture-image generation is gated on need**: `extract_images = vision_provider is not None`, and `_converter` is keyed on a `with_picture_images` bool (`lru_cache(maxsize=2)`) so text-only ingests pay nothing and the two converter variants coexist without cache thrash. The now-orphaned `sparse_text_threshold` param (it only fed the deleted side-pass) is dropped from `_ingest_pdf_docling`. **Scope — embedded rasters only**: pictures docling can't rasterize (vector figures, some scanned-page art) return `None` from `get_image` and are skipped; capturing those is the separately-tracked vector-figure follow-up, so the issue's sketched pypdfium2 bbox-render fallback was deliberately *not* built. The extracted image *set* can differ slightly from the old raw-XObject enumeration (docling returns layout-detected pictures) — expected. Real docling was exercised against a generated text+image PDF (`extract_images=True` → one 150-DPI picture with png bytes; `False` → zero, no wasted work); the pytest layer keeps the repo's fake-`SimpleNamespace` docling convention (no real models in CI) and the scribe test monkeypatches `pdfplumber_pipeline.convert` to raise, failing loudly if any second parse ever returns. No schema change.
- **`bartleby ready` skill installer + `ready`→`config` rename (issue #137)**: getting the skill into `~/.claude/skills/bartleby` was a manual `rm -rf … && cp -r skill …` documented in the README — easy to forget after editing `SKILL.md`, and the `cp` nests a stale copy a level too deep if the `rm` is skipped. Now a first-class command does it. **Three coordinated moves.** (1) **The harness-facing skill relocated `skill/` → `bartleby/skill/` and ships as package-data** (`pyproject.toml`, alongside the `web/**` entries) — it was a repo-root dir absent from the wheel, so a command run after `uv tool install` couldn't find it; bundling it (same reasoning as the `bartleby/web` bundle for `serve`) lets the installer work from an installed tool *and* a checkout. Source is resolved via `Path(bartleby.__file__).parent / "skill"`, not a hardcoded path. (2) **The config wizard `bartleby ready` was hard-renamed to `bartleby config`** (`commands/ready.py` → `commands/config.py`, `tests/test_ready.py` → `tests/test_config.py`, no alias per the no-backwards-compat rule) to free the `ready` verb for "get the skill ready." (3) **The new `bartleby ready`** (`commands/ready.py`, freshly) wipes-then-copies the bundled skill into the dest so the nesting bug is structurally impossible, and reports drift. **"Latest" is a content hash over the skill files, not the version number** — `SKILL.md` is edited constantly off-tag, so two builds at the same `__version__` can carry different skills and a version compare alone would falsely report up-to-date; a hidden `.bartleby-skill` marker (excluded from the hash) carries the version only for the human-readable line. Flags: `--check` (report + exit non-zero on missing/stale, writes nothing — scriptable), `--force` (rewrite when already current), `--dest`. A guard refuses to `rmtree` a non-empty dest that isn't a skill dir (no `SKILL.md`/marker), so a stray `--dest` can't nuke an unrelated directory. README's install/upgrade/Goose sections collapse to `bartleby ready` — notably "Upgrading from a release" no longer needs a separate checkout, since the pinned `git+https` install now carries the skill. No schema change.
- **Per-chunk view + quick-links from every chunk reference (issue #156)**: a chunk id was inert text (`chunk 1234`) everywhere it appeared — no way to *look at* a chunk and its surrounding context. Added a `/chunks/[id]` route plus an open-in-context affordance beside every chunk reference; `web/`-only, read-only, **no schema change**. **The view**: `getChunk(chunkId)` (`queries.js`) returns the target chunk plus its **±2 neighbors by `chunk_index` within the same `(source_kind, source_id)`** (one `BETWEEN` query off `idx_chunks_source`, target flagged via `is_target`), clamped at source boundaries; `+page.svelte` renders the target at full `--color-text` contrast on a `--color-token-dark` rail with the neighbors muted `--color-off` as context, each non-target label linking to its own `/chunks/<id>` so you can walk the stream. **Generic over source_kind** (the answer to the issue's open question): the neighbor window is within-source regardless of kind, and the back-link branches — document/summary/image chunks resolve to the document detail page via the existing `resolveSource` (same mapping citations/search use), a finding chunk links to `/findings/<source_id>`; unknown id → `error(404)` like the document/finding routes. **The quick-links**: a shared inline-SVG `CHUNK_ICON` (`lib/icons.js`) rides beside each chunk reference → `/chunks/<id>`. On search/scan cards (`ResultCard.svelte`) it's a plain `<a>` (native SPA nav). On finding citation chips (`findings/[id]/+page.svelte`) it's a **sibling `<button>`, not an `<a>`** — the chips are raw HTML inside `{@html}` and the layout's `marked` postprocess hook rewrites *every* `<a>` to `target=_blank`, so an anchor there would break in-tab routing; instead `renderChip` emits a `<span class="cite">` wrapping the chip + a `.cite-jump` button (DOMPurify keeps the `data-chunk-id` button, same as the cite-chip), and the existing click-delegation gained a `.cite-jump` branch that calls `goto()`. The chip's own click still opens the side iframe — the jump is a *separate* affordance. **Two small reuse moves** preempted by the simplify pass: the markdown-vs-literal snippet rule was hoisted out of `ResultCard` into `isMarkdownChunk` (`format.js`), shared with the chunk view; `renderChip`'s hand-rolled `.pdf`-stripper became the shared `stripExt` (so a `.docx`/`.html` chip strips its extension too). Verified end-to-end with Playwright against a live corpus — document/finding/summary chunks, boundary clamping, 404, both quick-link sites, and the preserved chip-iframe behavior — plus a clean `npm run build`.
- **Opt-in per-stage ingest timing + benchmark mode (issue #162)**: the concurrency work that follows needs "pages/sec at N workers" and a per-stage breakdown to validate against, and one baseline question settled first — is per-doc time dominated by parse, or by the ~55 captions/doc? Added a `--timings` flag to `bartleby scribe`; **off by default the ingest path is byte-for-byte unchanged** (no `StageTimer` constructed, nothing printed to stdout). **The instrumentation reuses the existing `on_stage(label)` callback** rather than threading a new timing param through every `_ingest_*` function — that callback already fires at each phase boundary in all paths, so a `StageTimer` driven off it (in `bartleby/lib/timing.py`) gets accurate per-stage wall-clock for free; the alternative (a parallel timing param) would duplicate the same threading the callback already does. The raw labels are mapped to a canonical vocabulary (`extracting`→`parse`, `embedding`→`embed`, `analyzing image(s)`→`caption`, `summarizing`→`summarize`); time before the first mark (hash + archive copy) lands in `prep`. **Chunk writes fold into `embed`** — the `INSERT` immediately follows `embed_texts` under the same label, and the baseline question doesn't need them split, so the deliberately-coarse seam was kept rather than instrumenting the inner functions. Per-doc lines (total + split) go to **stderr**; the aggregate — `docs`, `pages`, `wall_clock_s`, `docs_per_s`, `pages_per_s`, and per-stage `{total_s, pct, mean_s}` — is one JSON object on **stdout** (the shared console is stderr-bound, so `--timings > bench.json` captures cleanly). **Rates use whole-run wall-clock** measured around the loop, not the sum of per-doc totals, so inter-doc overhead counts and runs stay comparable. **A doc that raises mid-process (e.g. a summarizer failure) is not timed** — it's caught by the loop's `except` before the record is appended — and **already-ingested files are skipped**, so a clean baseline means a fresh project. `pct` is the share of summed stage time (the parse-vs-caption answer); the empirical number itself is left to the operator's representative sample. No schema change.
