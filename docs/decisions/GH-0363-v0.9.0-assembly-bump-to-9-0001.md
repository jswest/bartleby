# v0.9.0 assembly: bump SCHEMA_VERSION to 9, activate `_upgrade_v8_to_v9`

The held-at-8 protocol is resolved. Across the v0.9.0 omnibus, `schema.py` and a
dormant `_upgrade_v8_to_v9` step both already carried the v9 columns, but
`SCHEMA_VERSION` stayed pinned at 8 so the upgrade loop never reached the v8→v9
step and the chain-walk equivalence gate (`test_upgrade_chain_walks_from_v4_through_current`)
stayed `xfail`. This single assembly commit flips the version and turns the
dormant step live.

## Decision

1. **`SCHEMA_VERSION` 8 → 9** in `bartleby/db/schema.py`. The upgrade loop now
   walks `v < 9`, so the v8→v9 step fires for the first time.

2. **`_upgrade_v8_to_v9` is the consolidated v9 bump** — confirmed complete, no
   change needed. It applies all **eight** additive (nullable, no-default)
   columns that distinguish a fresh v9 DB from a released v0.8.x DB:
   - `tags.value_type` (`CHECK IN ('number','string','date')`), `tags.pattern`
     — the #114 value-bearing-tags method.
   - `document_tags.value`, `document_tags.chunk_id` (FK → `chunks`) — #114
     extracted value + its citation anchor.
   - `documents.parent_document_id` (self-FK), `documents.anchor_id`,
     `documents.section_title`, `documents.section_order` — the #254
     anchor-splitting columns, appended to the *same* v8→v9 step so #114 + #254
     ship as one bump.

   All eight are NULL-truthful on pre-upgrade rows (an ordinary boolean tag /
   an unsplit whole-file document), so released v0.8.x corpora run
   `bartleby project upgrade <name>` rather than re-ingest.

3. **Chain-strip extended** in both upgrade tests
   (`test_upgrade_chain_walks_from_v4_through_current` and
   `test_upgrade_resumes_after_mid_chain_crash`). The strip-back that simulates
   a v4 DB now also removes the eight v9 columns before re-walking the chain —
   otherwise the now-live v8→v9 step raises `duplicate column name` (flagged by
   #355). For `tags` / `document_tags` the v9 columns are `DROP COLUMN`'d
   directly. For `documents` they are **not**: `schema.py` annotates the #254
   columns with a multi-line `--` comment block, and SQLite's `DROP COLUMN`
   re-parses the residual `CREATE` — once the annotated columns are gone the
   dangling comment yields `error ... incomplete input`. Since FK enforcement is
   OFF on the raw strip-back connection and the table is empty, `documents` is
   instead rebuilt straight to its v4 shape (no `ingest_run_id`, no #254
   columns). This is a strip-back artifact, **not** a schema divergence.

4. **`xfail` removed.** The held-at-8 marker on the chain-walk test is gone. The
   strengthened full-DDL comparison (#355: normalized table + index DDL,
   order-independent, comment-stripped) now **passes for real**, genuinely
   proving the v4→v9 chain produces a DB byte-identical in DDL to a fresh one.

## Gate

Full suite green: **817 passed, 0 xfailed, 0 failed**. The chain-walk gate is no
longer xfail and is not vacuously satisfied — it asserts true DDL equivalence
between the chain-upgraded and freshly-created DBs.

## Scope

`bartleby/db/schema.py` (version bump only), `tests/test_project.py` (strip-back
extension + xfail removal). `_upgrade_v8_to_v9` itself was already complete and
is unchanged; no earlier chain step (v4→v5 … v7→v8) was touched.
