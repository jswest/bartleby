# Decision log

Settled judgment calls, kept so we don't re-derive them. Each decision is one
file, named `GH-<issue>-<slug>-<index>.md` — the issue number four-zero-padded
(`GH-XXXX` when a decision predates the issue-tracked workflow or lands out of
band), and a per-stem `<index>` that climbs only for phases of one issue or
repeated hotfixes. A hotfix folds into the `<slug>` as `hotfix-<descriptive-slug>` —
`GH-XXXX-hotfix-embedding-oom-0003.md` out of band,
`GH-0181-hotfix-vlm-fallback-0001.md` when it mends a specific issue's regression —
never the bare `hotfix` token. The `GH-XXXX` bucket sorts last on disk, so **this
index is the source of truth for chronological order**, newest first.

Current-state architecture (invariants, conventions) lives in
[`../../ARCHITECTURE.md`](../../ARCHITECTURE.md); this folder is the *why* behind
past calls, read on demand.

- [GH-0690 — **"Save as HTML" exports a finding as one self-contained file — fonts, sources, and margin-note behavior all embedded, assembled by a server endpoint** (issue #690): `GET /findings/[id]/export.html` (a third toolbar button beside *Copy as Markdown* / *Download .md*; `Content-Disposition: attachment` drives the download) bakes the live `/findings/[id]` view into a single no-network HTML file — the three self-hosted font families as base64 `data:` `@font-face`, every cited source embedded (PDF/image as base64 data URIs, each PDF with a `download`-attribute fallback link for browsers that won't render data-URI PDFs inline; `.txt`/`.log` literal-escaped per GH-0680; `.md` through the same sandboxed-viewer rendering), and a vanilla-JS re-expression of the margin-note layout + note→viewer click wiring, no Svelte runtime; shared logic was extracted rather than forked — the citation-marker→dagger substitution became `$lib/citations.js`, the sandboxed-iframe wrapper/CSS `$lib/viewerDoc.js` (file-type regexes included), the MIME table `$lib/server/mime.js` (image extensions derived, not re-enumerated), and `slugify()` landed in `$lib/format.js` — leaving the live page behavior-identical; body sanitization uses a private `Marked` instance carrying `+layout.svelte`'s exact DOMPurify hook (a `+server.js` endpoint can't rely on the layout's module-context registration); source embeds dedupe by `doc:`/`image:` key so bytes scale with unique sources, not citation count; export-only hardening from the review pass: `[^url:…]` notes emit an `href` only when it matches `/^https?:\/\//i` (a `javascript:` URL renders inert — the file is built to be emailed to third parties; the live page's identical pre-existing gap was deliberately left alone), `[^finding:N]` and chunk-icon links are dropped as meaningless standalone, and a resolved citation whose archived file is gone from disk renders inert with a visible "source file missing at export time" hint (fonts get the opposite call — `buildFontFaceCss` still throws a loud 500 rather than silently exporting wrong fonts); the export CSS/fonts are a hand-copied snapshot of `app.css` (accepted drift trade-off — fonts already require a hand re-snapshot on change); fonts read via `process.cwd()/static/fonts` because `bartleby serve` always runs the vite dev server out of `~/.bartleby/serve`; web-only, no `SCHEMA_VERSION` bump](GH-0690-finding-html-export-0001.md)
- [GH-0683 — **`WsjptProvider` preflight-checks the installed `pydantic-ai` version and fails open when it can't tell** (issue #683): wsjpt's Vertex AI/ADC auth path (no API key) calls `GoogleProvider(vertexai=…, project=…, location=…)`, kwargs that exist only in pydantic-ai `[0.3.0, 2.0.0)` — `<0.3` lacks the unified `pydantic_ai.models.google` module (`ModuleNotFoundError`) and `2.0` removed the `vertexai` kwarg (`TypeError`); wsjpt 0.9.1 pins `pydantic-ai>=0.0.45` with no upper bound, so an unconstrained out-of-band install silently resolves an incompatible version and fails only per-document under the opaque `summary failed: …` wrapper. `WsjptProvider.__init__` now parses `pydantic_ai.__version__` right after the wsjpt import and raises a `RuntimeError` naming the `>=1,<2` pin + reinstall command when `(major, minor)` is out of range, but **fails open** (no raise) when pydantic_ai is absent or its version unparseable — pydantic_ai isn't a bartleby dependency (the venv and test suite have no real one), so "can't introspect" ≠ "incompatible". The pin can't move into `pyproject.toml` since pydantic-ai only arrives transitively through the WSJ-internal out-of-band wsjpt install. Provider + docs only, no `SCHEMA_VERSION` bump](GH-0683-wsjpt-pydantic-ai-pin-0001.md)
- [GH-0680 — **`.txt` document sources render as literal escaped text in a `<pre>`, never through `marked`** (issue #680): the `/documents/[id]` viewer fell back to the browser's unstyled native `text/plain` view for `.txt` because `SourceViewer`'s `isMarkdown` regex didn't match and it dropped through to the raw-file `<iframe src>` branch; the tempting fix (add `txt` to `isMarkdown` and route through `marked.parse`) was rejected because `marked` runs repo-wide with `breaks: false`, so parsing plain text as markdown collapses single newlines into run-on paragraphs and misreads stray `#`/`*`/`_`/`___` — mangling the line-structured metadata/log `.txt` corpora this pane must show faithfully (the same "keep extracted text literal" hazard `format.js`'s `isMarkdownChunk` and `ResultCard.svelte` already guard). Instead `SourceViewer.svelte` gained an `isText` predicate (`/\.(txt|text|log)$/i`) beside `isMarkdown`, both feeding a shared `isInline` gate that reuses the same SSR-or-`fetch` resolution and the same sandboxed `srcdoc` wrapper (GH-0567) — but the body branches: markdown → `marked.parse`, text → HTML-escaped (shared `escapeHtml` in `format.js`) into a `<pre>` that gained `white-space: pre-wrap; overflow-wrap: anywhere`. The `+page.server.js` SSR preload widened past `\.md$` to `.markdown`/`.txt`/`.text`/`.log`, and its prop renamed `markdown`→`sourceText`. Frontend-only, no `SCHEMA_VERSION` bump](GH-0680-txt-literal-render-0001.md)
- [GH-0666 — **the citation-glyph map rotates to ¶ chunk / § finding / † url-doc / ‡ gone (supersedes GH-0654 and GH-0642)** (issue #666): GH-0642 had given external `[^url:…]`/`[^doc:…]` the `§` (and chunk the `†`), and GH-0654 then gave finding→finding links `¶`; this re-maps the whole set so the glyph tracks typographic weight — pilcrow `¶` for the primary corpus chunk, section sign `§` (silcrow) for finding→finding links, dagger `†` for external url/doc, double-dagger `‡` unchanged for a gone chunk. Glyph chars change only in `sourceNote`/`findingNote`/`externalNote` (`findings/[id]/+page.svelte`); the `kind`-based `cite-ref--{source,finding,gone}` classes are untouched, so `¶` chunk and `†` url/doc deliberately share the `--source` amber (glyph alone distinguishes — no new external CSS kind). Also drops the `.cite-notes` `border-left` rule between prose and gutter (the `--space-xl` gap carries the separation; narrow-screen `border-top` retained) and gives `.margin-note__link` a persistent low-contrast underline (`color-mix` 35% at rest → full on hover) so gutter links read as links. CSS/Svelte-only, no `SCHEMA_VERSION` bump](GH-0666-marginalia-glyphs-0001.md)
- [GH-0664 — **the finding-detail header aligns to the prose column, not the gutter-inclusive width (supersedes GH-0643)** (issue #664): `.ledger-hed`/`.ledger .meta`/`.ledger-dek` were capped at `--ledger-content-width` (prose + gap + notes) per GH-0643 so the header's right edge met the margin-note gutter — but that read as a too-wide band bleeding into the marginalia; owner preference is header-aligned-to-body, so all three are now capped at `--ledger-prose-width` (the same cap as the prose beneath them). Also bumps `.cite-notes` `padding-left` `--space-lg`→`--space-xl` for breathing room between the source links and the thin rule dividing marginalia from body. CSS-only, no geometry-var/grid/JS change, no `SCHEMA_VERSION` bump](GH-0664-finding-header-width-0001.md)
- [GH-0654 — **`[^finding:N]` is an accepted unidirectional finding→finding citation marker, not a new table** (issue #654, omnibus #656): the index/omnibus finding tree had only prose edges; rather than the originally-proposed `finding_links` table, links are a marker in the finding body extending the `[^chunk:N]` grammar (GH-0624) — `extract_finding_citations`/`validate_finding_ids_exist`/`live_finding_ids` in `_common.py` validate the target exists at save/edit/merge (`UNKNOWN_FINDING_LINKS` if not), `read_finding` emits `dangling_finding_links` for targets that later vanish, and `findings/[id]/+page.svelte` renders each `[^finding:N]` as a margin-note link to `/findings/N`; mirrors the external-citation (`url`/`doc`) precedent, not the chunk-citation DB path; storage stays text, no new table, no `SCHEMA_VERSION` bump](GH-0654-finding-citation-links-0001.md)
- [GH-0653 — **`scan --body-matches '/regex/'` is a first-class regex filter that selects chunks by body match** (issue #653, omnibus #656): FTS phrase scan can't require punctuation and bare tokens like `5376` match nothing, and `--extract` only projects captures after a token query locates chunks; `--body-matches` post-filters the FTS-matched set in Python (`parse_filter_regex`/`_compile_slash_regex` in `_common.py`, sharing the slash-regex prologue with `parse_capture_regex`), giving an honest `total` (pre-pagination count) in one pass and composing with all scope flags; it is the locator `--extract` pairs with (filter then project) and is mutually exclusive with `--count-by` (rejected, not silently ignored); O(n) over FTS matches, query-path only](GH-0653-scan-regex-filter-0001.md)
- [GH-0641 — **`scan --count-by 'file_name:/regex/'` tallies a per-document filename histogram** (issue #641, omnibus #656): the generic form of a hand-rolled `list_documents | grep | uniq -c` loop — `parse_field_capture` (`_common.py`) detects the `field:/regex/` form (`file_name` only, no speculative column engine) and `_count_by_field` (`scan.py`) runs the capture once per *document* (first match, not per-chunk/per-match, so a filename matching twice doesn't double-count); reuses scan's bucket envelope but with distinct keys (`count_by_field`/`buckets`) so it can't be confused with the body-text histogram; deliberately corpus-wide (does NOT take FTS scope — a "what's in the corpus" answer; scoping would silently produce a partial result); no schema impact](GH-0641-scan-count-by-fields-0001.md)
- [GH-0652 — **`sqlite-vec` floor bumped `>=0.1.6`→`>=0.1.9` so aarch64 gets a correct-arch wheel** (issue #652, omnibus #656): the published 0.1.6 aarch64 build (both the PyPI wheel and the GitHub loadable `.so`) is mis-compiled as 32-bit ELF (`ELFCLASS32`), so `apsw` refuses to load `vec0.so` and `uv run pytest` errors across every test that opens a corpus DB; 0.1.9 ships a correct aarch64 wheel; on-disk read-compat verified empirically against a real ~4.5GB corpus (already 0.1.9-written — the lock's 0.1.6 was actually *behind* the data) and corroborated by the 0.1.7–0.1.9 changelog (DELETE/KNN/packaging fixes, no vec0 format change); existing corpora keep working, no re-ingest, no `SCHEMA_VERSION` bump](GH-0652-sqlite-vec-aarch64-floor-0001.md)
- [GH-0646 — **the web app carries prefixed `<type>:<int>` ids end-to-end and strips to a bare int only at the SQLite-binding doorstep** (issue #646, triggered by GH-0624): GH-0624's interface-layer id-tagging made every skill script emit `document:204`/`finding:12`/`chunk:15837`/`image:7`/`tag:3`/`summary:N`, but the web app was built for bare ints and broke nearly everywhere skill output fed a link or a query — route URLs became `/documents/document:204` so `parseIdParam`'s `Number(…)` → NaN → 404, and prefixed strings bound into SQL `IN(…)`/`.get()` against bare-int columns matched no rows (the "documents without summaries don't render" report was one face of a broad regression); the fix keeps the prefixed string as the app's identity *everywhere* — page URLs (`/chunks/chunk:5`, owner-approved), Map keys, hrefs, values passed between functions — and converts to a bare int **only at the moment a value is bound to SQLite**, via one pure helper `bareId(value)` (`web/src/lib/server/ids.js`; recognises the six canonical types, tolerates already-bare ints, returns NaN for anything else incl. in-body `[^chunk:N]` markers and unknown prefixes like `gpt:4`); `parseIdParam` swaps `Number(raw)` for `bareId(raw)` with an optional `expectedType` guard that 404s a wrong-type URL (`finding:12` handed to the documents route) while letting bare DB-sourced URLs through; the three `queries.js` batch helpers (`tagsByDocument`, `titleMeta`, `resolveSources`) strip for the `IN(…)` list but re-key their result Maps by the *original* (prefixed) id via a `bare→original` map so callers' `.get(prefixed)` still hits; a central walk-the-JSON normalizer was **rejected** — recognising ids by field name needs an ever-maintained whitelist and would mis-convert string fields that merely look like ids (notably the echoed search `result.query`, crashing the results page), and recognising by value-shape discards the self-describing type info GH-0624 added on purpose; web-only, no Python/standard change, no `SCHEMA_VERSION` bump](GH-0646-web-prefixed-ids-strip-at-db-0001.md)
- [GH-0649 (finding-read) — **`bartleby finding read <id>` renders a finding to the terminal with citations resolved live, and keeps two read helpers rather than one flagged path** (issue #649): `read` prints a finding as Markdown to stdout (title H1, `session · model · created_at` subtitle, body) so it composes with pagers/`--render`, with `--json` delegating wholesale to the `read_finding` skill script (memory-wall and JSON shape inherited, no duplication); citations resolve against the corpus *at read-time* — `[^chunk:N]` → `† file · p.N` when present, `‡ source no longer available` when the source is gone — and `[^url:…]`/`[^doc:…]` → `§`, all rewritten to sequential Markdown footnotes numbered in a **single left-to-right pass** combining chunk and external markers (rejected: two separate sequences with a gap, and per-type `[^e1]` markers that break `glow`), each unique marker assigned its number on first appearance and reused; **two read helpers kept** — `_read_finding_for_export` (title/description/body/resolved citations) vs `_read_finding_for_display` (adds session metadata + dangling ids), because merging them behind a flag would force a conditional return shape on every caller, while only the bare-label branch (`_citation_label`) is genuinely shared between `read`'s footnotes and `export`'s inert `[corpus: …]` markers; thin CLI wrapper over existing finding/citation machinery, no `SCHEMA_VERSION` bump and no new columns; out of scope: backing chunk *text* in footnotes, and cross-machine chunk-id remapping (read assumes the finding's home corpus)](GH-0649-finding-read-0001.md)
- [GH-0648 — **`bartleby serve` serves evidence files buffered (`fs.readFileSync`), not streamed** (issue #648): the file route `bartleby/web/src/routes/files/[document_id]/+server.js` returned a streamed `Response` body, and when a client aborted a request mid-flight (iframe reloads, navigations, an early-closing probe) SvelteKit-dev + undici double-`.close()`d the `Readable.toWeb(createReadStream)` bridge, throwing an unhandled `TypeError [ERR_INVALID_STATE]: ReadableStream is already closed` that crashed the *entire* Vite dev server — every route then 500/502'd until restart, and the crash→respawn churn spiked fds toward transient `ENFILE`; the route now reads the whole file into a `Buffer` and returns `new Response(data)`, whose in-memory WHATWG stream cancels benignly on abort (no fs-stream lifecycle to double-close), eliminating the crash; the manual `Content-Length` header is dropped (a buffered `Response` sets none — SvelteKit drains the body as a chunked response, wire-correct for browsers and the iframe) and MIME lookup / `Content-Disposition: inline` / `X-Content-Type-Options: nosniff` / the HTML `Content-Security-Policy: sandbox` branch are all untouched; trade-off is whole-file-in-memory, accepted because the corpus is small local evidence docs served by a local dev tool; the streaming-with-abort-handler alternative was rejected as more code for no benefit at this scale; web-only, no `SCHEMA_VERSION` bump, no re-ingest](GH-0648-serve-buffer-files-0001.md)
- [GH-0642 (glyph) — **external citations get the § glyph, distinct from the chunk dagger** (issue #642, second decision): the finding-detail gutter used **†** for both corpus-chunk *and* external `[^url:…]`/`[^doc:…]` citations, so a reader couldn't tell corpus-vs-external apart (only the gone state, **‡**, was distinct); external now renders **§**, completing the traditional footnote sequence `* † ‡ §` (**† chunk**, **‡ chunk gone**, **§ external**) — § reads as a document/section mark and is heavier than a superscript `*`; `url`/`doc` share the one glyph (three concepts beat four; the gutter note already shows a URL as a link vs a doc as a muted ref); glyph only — `cite-ref--source` tone, `data-note` tie-back, and click behaviour unchanged; web-only, no `SCHEMA_VERSION` bump](GH-0642-external-citation-glyph-0002.md)
- [GH-0642 — **legacy bare `[^N]` chunk citations are reconciled by a one-shot data backfill, not reader leniency** (issue #642): GH-0624's hard cutover updated every reader to require `[^chunk:N]` and made the save validators reject bare `[^N]`, but — being interface-layer-only — migrated no stored bodies; a survey found **2101 bare markers across 153 of 161 findings** (only `test631` used the new form), each rendering as inert prose with no clickable dagger and no right-pane source (the click handler and `SourceViewer` were never broken — only the chunk markers failed to render; external `[^url:…]`/`[^doc:…]` rendered fine); fixed by `scripts/backfill_chunk_citations.py`, a one-shot that rewrites bare `[^<digits>]`→`[^chunk:<digits>]` in `findings.body` across every project corpus, after which the **unchanged** reader/export/validator all resolve — no frontend, validator, or `SCHEMA_VERSION` change; reader leniency was rejected (it resurrects the grammar #624 killed as a permanent shim and only fixes the web surface, leaving `finding export/import` + `read_finding` dangling-detection still seeing bare markers), and a `bartleby` verb was rejected (a one-time fixup for a closed set — new findings can't acquire bare markers — belongs in `scripts/` beside `release.py`, not the standing CLI); safe because a bare `[^N]` was unambiguously a chunk citation pre-#624 (`finding_citations` rows are the source of truth, a stale id falls back to the existing "no longer available" note), idempotent (only `[^`+digits matches), and atomic+recoverable (dry-run default; `--write` runs one transaction per corpus after a WAL-faithful SQLite online-backup snapshot to `<name>.pre-chunk-backfill.bak`, never clobbered)](GH-0642-backfill-legacy-citation-markers-0001.md)
- [GH-0631 — **finding-detail margin notes are dagger-aligned sidenotes (JS-measured), and clicking a note opens the source in the right pane** (issue #631, revisits the gutter-geometry call of #593): two UX complaints against R4's citation gutter, both reversed at the layout layer only — the #593 ledger treatment (drop-cap, Iosevka hed/dek, †/‡ tone, the `cite-ref`/`margin-note[data-note=N]` tie-back contract) is untouched. **Geometry:** #593 deliberately stacked notes in reading order with no geometric tie to their inline daggers (rejecting JS measurement as reflow-prone); the user found a note floating far from its anchor disorienting, so a `layoutNotes()` pass now *does* the measurement #593 avoided — for each ordinal it reads the `.cite-ref[data-note=N]` top via `getBoundingClientRect`+`scrollY`, sets the matching `.margin-note` to `position:absolute; top`, then sweeps top→bottom nudging overlaps down by a fixed `NOTE_GAP` (two passes: position-then-measure-height, so `offsetHeight` is read at the final absolute width with the invariant `left/right:0` already in CSS); it re-runs after every reactive `notes`/`bodyHtml` update (via `tick()`) and on resize (a `ResizeObserver` on the prose body), and on the pre-existing `@media (max-width:56rem)` collapse it detects narrow via `window.matchMedia` (same breakpoint), clears its inline styles, and early-returns to the CSS `position:static !important` end-note flow — CSS-only was rejected because per-anchor offset only exists post-layout and `sticky`/`float` fight the grid and need one `<aside>` per note. **Click:** the corpus-chunk note's `goto('/chunks/N')` (which navigated the whole page away) splits into a primary label that calls `activate(chunkId)` — same as the inline dagger, loading the doc in the right-pane `SourceViewer` with no navigation — plus a secondary muted `↗` `<a href="/chunks/N">` (`CHUNK_ICON`, SvelteKit intercepts the same-origin link, no `on:click`); `url`/`doc`/`gone` branches unchanged; web-only, no `SCHEMA_VERSION` bump](GH-0631-marginalia-citations-0001.md)
- [GH-0632 — **`--file-like` high-cardinality scope is materialised into a connection-scoped temp table, not a Python `IN (?, …)` bind list** (issue #632, omnibus #635): a broad glob (`%__202%`, ~141k docs) crashed `search`/`scan`/`list_documents` with `too many SQL variables`, because `resolve_scope` returned matched ids as a `list[int]` that three sites (`Scope.restrict_in`, `scan.work`, `search._build_scope`) re-bound as `IN (?, …)`, each blowing SQLite's 32,766 `SQLITE_MAX_VARIABLE_NUMBER` cap; now when `--file-like` is present `resolve_scope` intersects file-like with `--in-documents`/`--tag`/`--authored-*` **entirely inside SQLite** and writes the survivors to a TEMP TABLE `_scope_file_like`, with `Scope` gaining a `temp_table` field so `restrict_in()` emits `col IN (SELECT document_id FROM _scope_file_like)` with zero binds — `list_documents` inherits it via `restrict_in`, `scan` via `(restrict_sql, restrict_params)` replacing its `restrict: list` params, `search._build_scope`/`_scope_clause` via subquery-string values alongside the existing list path; **always** temp-table (no threshold) keeps a single tested path, the PRIMARY-KEY index makes the join O(log n), and the connection lifetime (one per skill invocation) is the cleanup boundary; query-path only, no `SCHEMA_VERSION` bump and no re-ingest](GH-0632-file-like-temp-table-0001.md)
- [GH-0633 — **finding writes are self-verifying and shell-safe: responses echo `title`+`description`, and `--title-file`/`--description-file` mirror `--body-file`** (issue #633, omnibus #635): `save_finding`/`edit_finding` returned provenance + `body` but not `title`/`description`, so a title- or description-only edit needed a follow-up `read_finding` to confirm — and bare `type=str` `--title`/`--description` were a shell footgun (an intended `"…~$8M/quarter"` saved as `"…~M/quarter"` after the shell ate `$8M`, while `--body-file` came through untouched, which is exactly why that mechanism is right); both scripts now echo the **resolved/stored** title+description in every write response (the *catch* — a mangled value surfaces in the same call that wrote it) and accept file-based siblings read verbatim via `Path.read_text(encoding="utf-8")` with no shell expansion (the *prevention*), routed through a shared `read_text_arg(value, file, *, flag, error_code)` in `_common.py` (save_finding's mutually-exclusive groups are `required=True`; edit_finding's stay optional so the `NOTHING_TO_UPDATE` guard still fires); body-derivation (title-from-first-H1) was deliberately skipped under the smallest-fix rule, and §A.4 (`id` vs `finding_id` on finding rows) stays deferred behind #624; response/flag-additive only, no `SCHEMA_VERSION` bump](GH-0633-self-verifying-finding-writes-0001.md)
- [GH-0624 — **agent-facing entity id *values* are type-tagged at the interface (`chunk:<id>`/`document:<id>`/…), not in storage** (issue #624, supersedes the prompt-only #623): `chunk_id` and `document_id` are independent INTEGER PKs with overlapping ranges, so a bare int is ambiguous — in real sessions a `document_id` was silently mis-stored as a citation (it parsed, pointed at the wrong row, nothing complained); now every id a skill script emits to stdout renders `<type>:<id>` (the five canonical types `chunk`/`document`/`finding`/`image`/`tag`, plus `summary` solely for a polymorphic `source_id`), and every `--*-id` flag and the inline citation marker accept **only** the prefixed form; the whole convention lives in `bartleby/skill_scripts/_ids.py` (`format_id`/`format_source_id`, `parse_id`, the `prefixed_int`/`prefixed_int_list` argparse factories, and `format_output_ids` — a recursive walk over a fixed field→type map applied at each `work()` return, with `source_id` prefixed by `source_kind` at its sites because its type is unknowable from the key); **hard cutover** per the no-back-compat rule — a bare or wrong-typed id (a `document:` value handed to `--chunks`) fails to the runner's `USAGE_ERROR` envelope, never a colliding-row lookup; the citation marker moves `[^N]`→`[^chunk:N]`, reusing the existing `[^<scheme>:<ref>]` grammar that serves external `[^url:…]`/`[^doc:…]` with `chunk` as the internal scheme, while `[^document:N]`/`[^finding:N]` raise `WRONG_CITATION_TYPE` and bare `[^N]`/`[N]` raise `MALFORMED_CITATION` (the web finding view and `finding export/import` regexes updated to match); **interface-layer only** — storage stays integer, `finding_citations` rows are bare ints, no `SCHEMA_VERSION` bump and no re-ingest; complements #573's flag-*naming* rule (name the value vs type the value); the relevance/provenance check for a correctly-typed-but-unsupported citation is explicitly out of scope (a future issue); agent-facing **breaking change** with no alias](GH-0624-type-tagged-ids-0001.md)
- [GH-0592 — **Pixelarticons (MIT) inlined as raw SVG strings, not an icon framework** (issue #592, omnibus #589): the redesign's icon vocabulary is the 1-bit Pixelarticons set, but only the ~six glyphs in use (`search`/`sparkle`/`file-text`/`hash`/`external-link`/`bookmark`) are copied verbatim into `lib/icons.js` and stamped by a shared `icon()` helper — no `pixelarticons`/`@iconify` dependency and no `<Icon>` component, extending the pre-existing `{@html}`'d `CHUNK_ICON` pattern with zero tree-shaking ceremony; each glyph is `fill="currentColor"` (inherits nav/card/citation color) and rides a `.pixel { image-rendering: pixelated }` class; the finding-detail citation dagger gets its glyph via a `::before` CSS `mask` so R4's †/‡ tie-back markup is untouched, and nav glyphs shrink to 13px on the ≤480px row to respect B1's wrap math; web-only, no `SCHEMA_VERSION` bump](GH-0592-r3-pixelarticons-0001.md)
- [GH-0593 — **finding-detail margin-note citations are a stacked gutter grid, not Tufte per-marker float** (issue #593, omnibus #589): `/findings/[id]` gains a ledger treatment (Doto drop-cap, Iosevka hed/dek, ~62–66ch measure) where inline citation chips leave the prose for a right-hand `.cite-notes` gutter; the layout is a two-track CSS grid with notes stacked in reading order rather than each note absolutely positioned beside its marker (which would need JS measurement, fight `position: sticky`, and reflow on resize/font-load) — the inline→note tie-back is carried by markup (`<sup class="cite-ref" data-note="N">†N</sup>` ↔ `.margin-note[data-note=N]` with a `†N`/`‡N` badge), so it is reflow-free and collapses at ≤56rem to stacked end-notes with no JS branch; resolved vs missing is a tone on the note kind (amber `†` "source" vs danger `‡` "missing source"), and the `cite-ref`/`margin-note__dagger` classes are a deliberate stable contract for R3's icon splice; web-only, no `SCHEMA_VERSION` bump](GH-0593-r4-ledger-finding-0001.md)
- [GH-0590 — **the retro type foundation self-hosts all three font voices (Iosevka Term + Source Serif 4 + Doto), not Google Fonts** (issue #590, omnibus #589): R1 drops the Google `<link>` and ships woff2 under `web/static/fonts/` via `@font-face` in a new `fonts.css`, forced by two CDN gaps — Google's Source Serif subset strips the `onum` oldstyle-figures feature the design needs (the full Adobe variable font carries it, verified in the GSUB) and "Iosevka Term" specifically isn't on a clean CDN; all three are SIL OFL 1.1 (web-embedding licit, so the woff2 are committed); Iosevka ships as three static weights extracted face-0 from the local `.ttc` and subset to a Latin+punctuation+daggers set (~114 KB/face, via a `fontTools.subset.main --no-hinting` pass after an OTS-malformed first attempt that silently fell back to system mono), while Source Serif/Doto stay full variable fonts; tokens `--font-display`/`-sans`/`-serif`/`-mono` plus a `--text-display-floor` Doto size gate; `pyproject.toml` package-data gains `web/static/**/*` so an installed wheel ships the fonts; web-only, no `SCHEMA_VERSION` bump](GH-0590-r1-type-token-foundation-0001.md)
- [GH-0602 — **pi-vm gains a `--model <id>` flag on both `run.sh` and `host-agent-ollama.sh`, and the flag beats the `AGENT_MODEL` env var** (issue #602): the agent model was selectable only via `AGENT_MODEL`, which is ambient and easy to leave exported in a profile; a typed `--model` is the most explicit signal, so "explicit beats ambient" — the flag overwrites `AGENT_MODEL` unconditionally for the rest of the script and collapses into that same variable before any downstream logic, so the container pass-through and `entrypoint.sh` are unchanged and the env-var path still works when no flag is given; both `--model <id>` and `--model=<id>` parse, an empty value fails loudly instead of crashing under `set -u`, and no new config/state is introduced (issue #602)](GH-0602-pi-vm-model-flag-0001.md)
- [GH-0003 — **vector figures are captured by a shapely proximity-merge of `curves+lines+rects`, cropped from the existing page render alongside raster embeds** (issue #3): matplotlib/Illustrator charts are PDF path operators that never appear in `page.images`, so the VLM never saw them under the raster-only path; the fix buffers each vector primitive by `tol/2` and `unary_union`s them into clusters, then drops sub-`min_area` slivers **and** clusters covering ≥ `PAGE_SUBSTRATE_AREA_RATIO` (90%) of the page (full-bleed backgrounds/borders — the same substrate ceiling the raster path applies, load-bearing because `vector_ink_threshold` defaults to 0), and excludes tables by centroid-in-`find_tables()`-bbox (cheaper than IOU, an accepted v1 miss for a figure straddling a table edge), cropping each survivor from the already-rendered raster — **never** gated on `len(page.images)==0`, so a page with a chart *and* a photo yields both crops; vector crops take `image_index_on_page = 1000+cluster_index` to avoid colliding with the 1-indexed raster embeds, so `_ImageRoute`/chunk-naming/page-hash-dedup need no change; new `vector_ink_threshold` key (default 0 = never skip) threaded `ParseConfig`→`convert()`; shapely (BSD) was already transitively in-tree, rejecting AGPL PyMuPDF `cluster_drawings`; additive, no `SCHEMA_VERSION` bump, no re-ingest (issue #3, omnibus #581)](GH-0003-vector-figures-shapely-proximity-merge-0001.md)
- [GH-0223 — **a dedicated `vision_temperature` knob, defaulting to the summarize `temperature`** (issue #223): captioning previously sent *no* temperature so it always rode the model default, giving a corpus that set `temperature` for deterministic summaries no say over caption determinism; the new key resolves at the single ingest→runtime point in `commands/scribe.py` as `config.get("vision_temperature", config.get("temperature", DEFAULT_TEMPERATURE))` and threads `scribe→caption._caption_all→_analyze_image→images.analyze→provider.analyze_image`, so an unconfigured corpus behaves exactly as before (caption temp == summarize temp); `analyze_image` gained a required `temperature: float` on the `Provider` protocol, and each provider **reuses its summarize-path rules rather than inventing vision policy** (anthropic `_drops_temperature` deny-list, openai `_drop_temperature` never-send for GPT-5, ollama `options.temperature`, wsjpt accepts-and-ignores) because temperature acceptance is a model property; shared module-level once-warn flags mean a summarize drop already warned so captioning doesn't double-warn; additive, no `SCHEMA_VERSION` bump, no re-ingest (issue #223, omnibus #581)](GH-0223-vision-temperature-knob-0001.md)
- [GH-0242 — **`extraction` is a first-class benchmark cell-key dimension, not a separate doc-id** (issue #242): the cell went `(provider, model, doc)` → `(provider, model, doc, extraction)` so a Docling-extracted source for the same doc doesn't distort equal-per-doc quality weighting; run + judgment records carry an `extraction` field (default `pdfplumber`), filenames stay backward-compatible for pdfplumber and take an `_x-<extraction>` segment for named variants (`-<extraction>` for source caches via `BenchmarkRoot.source_path`), `summarize --extraction <name>` runs against the variant's pre-committed source and `leaderboard --extractions a,b` filters; the **no-mixed-`source_sha`-in-a-cell** invariant is now automatic because each extraction variant is its own cell with its own source sha; the Docling variant is **fixtured** (`benchmarks/sources/psc-order-0109-docling.txt`, produced once via the production docling converter, tracked in git) to avoid heavy CI model downloads, with `source_sha` catching drift; additive/test-only, no `SCHEMA_VERSION` bump (issue #242, omnibus #581)](GH-0242-extraction-field-widens-cell-key-0002.md)
- [GH-0242 — **image-bearing benchmark docs use pre-committed fixtures, not live VLM captioning** (issue #242): the image-bearing corpus doc (`neada-heating-0461`) exercises the full `[Image on page N]\n<text>` page-order interleave of `writer.summary_input` *without* requiring the local VLM as a benchmark precondition, because the benchmark measures **summarization** quality not caption quality — live captioning would make it non-reproducible across machines without a vision backend, confound summary scores with caption quality, and couple results to the VLM version; the fixture `benchmarks/sources/neada-heating-0461-image-fixture.txt` runs the real pdfplumber extraction + `chunk_text` page interleave and substitutes hand-written descriptions (from visual inspection of each `EmbeddedImage.png_bytes`) for the five data-table images, mirroring `summary_input()` exactly; `source_sha` pins every run/judgment to the committed text and fires the same drift-detection the pdfplumber sources use; accepted tradeoff: fixtured captions slowly drift from current VLM output (fine — a fixed input is the point); run via `--extraction image-fixture` (issue #242, omnibus #581)](GH-0242-image-chunks-are-fixtured-not-live-captioned-0001.md)
- [GH-0573 — **id-valued flags take a `-id` suffix everywhere** (*name the value you accept*), overturning #111's bare-noun convention: the enforceable rule is a biconditional — a skill/CLI flag's option string ends in `-id` **iff** its argparse `dest` ends in `_id` — motivated by **agent parity** (every command emits `finding_id`/`document_id`/`chunk_id` in its JSON, so the output→input loop should read symmetrically: `read_finding --finding-id <that value>`, not `--finding`); renamed across both surfaces with **no back-compat alias** — `--finding`→`--finding-id` (delete/edit/read_finding), `--document`→`--document-id` (save_date/save_summary/read_document/read_chunks/tag), `--chunk`→`--chunk-id` (assign_tag) — while plural/relational flags (`--documents`→`document_ids`, `--chunks`, `--from`, `--around-chunk`) and name/key/path flags stay bare; the `finding export` positional and the #518/#521 siblings needed no fix; enforced by an inverted `test_skill_flag_conventions.py` biconditional guard plus a `test_no_stale_bare_id_flag_anywhere` grep guard (bounded-token regex, allow-listing `docs/decisions/` + the guard test); agent-facing breaking change, no `SCHEMA_VERSION` bump, no re-ingest (issue #573, omnibus #574, supersedes #111)](GH-0573-id-flag-convention-suffix-supersedes-0111-0001.md)
- [GH-0575 — a containerized (pi-vm) session needs **exclusive host access to its corpus**, addressed by **document-and-warn** rather than a code fix: the cross-kernel WAL clash (host `bartleby serve` + a pi-vm agent on one `/corpus`-mounted WAL DB) is **strictly cross-kernel, not concurrency** — the `-shm` wal-index is coherent only within one kernel, while same-kernel concurrency is supported and tested (`busy_timeout` before `journal_mode=WAL`, `BEGIN IMMEDIATE` seam, embedding outside the txn, six-writer test) — and has two faces: the loud `database disk image is malformed` 500 and the quiet stale-reads-until-restart; the fix strengthens `docs/pi-vm-runbook.md` with the plain rule (while a pi-vm run touches a corpus, stop host `serve` / don't start a host session / view between runs) naming both faces, a stderr warning at pi-vm launch (`entrypoint.sh`), and one minimal `console.warn` at `serve` startup; rejected (a) in-container serve [no container in the common case], (b) reader checkpoint+reopen [leaves the malformed face], (c) non-WAL journal mode [taxes every corpus]; no schema/journal-mode change, no `SCHEMA_VERSION` bump (issue #575, omnibus #574, supersedes #558)](GH-0575-pi-vm-cross-kernel-document-and-warn-0001.md)
- [GH-0567 — untrusted `.md` source documents render in a **sandboxed `srcdoc` iframe** carrying a `default-src 'none'` CSP: the citation/source pane showed raw markdown because `/files` serves `.md` as `text/plain` into a bare iframe, and the fix renders `marked.parse(source)` (still through the global DOMPurify hook) but isolates it because source docs are **untrusted ingested content**; two threats split across two controls — **script injection** already handled repo-wide by the `+layout.svelte` DOMPurify postprocess hook, and **IP-beaconing** (a remote `<img>` from markdown `![](…)` fires an outbound request on view, leaking the host IP) stopped *only* by `default-src 'none'`, since DOMPurify keeps remote images and `sandbox=""` doesn't block subresource loads; this also closed a **pre-existing latent beacon** in `documents/[id]` (inline `{@html}`, no CSP), and both panes now share one `SourceViewer` (markdown→sandboxed srcdoc, PDF→native viewer, else sandboxed raw-file iframe); rejected inline `{@html}` rendering (carries the beacon, rests on DOMPurify alone) and global DOMPurify image-stripping (broad blast radius, weaker than CSP); frontend-only, no `SCHEMA_VERSION` bump (issue #567)](GH-0567-markdown-source-viewer-sandbox-csp-0001.md)
- [GH-0563 — **external citations are a body-marker convention, not a stored relational kind**: findings may carry a web URL or external-dataset doc ref *alongside* (never instead of) the mandatory ≥1 corpus-chunk citation, written in the body as alpha-prefixed footnote markers `[^url:<url>]` / `[^doc:<ref>]` and parsed/rendered **on read** (`extract_external_citations` in `_common.py`; surfaced by `read_finding`, `save_finding`/`edit_finding`/`merge_findings` output, and the web finding view). The plan interview resolved this *away* from a schema change: `finding_citations` earns its keep only for chunk citations (real FK + cascade to `chunks`, existence validation, location enrichment, reverse lookup) — an external ref is opaque text with none of those affordances and **already persists** in `findings.body`, so a table would duplicate the body and back no constraint. The ≥1-chunk invariant is untouched because `extract_citations` (`\[\^(\d+)\]`, digit-only) ignores the alpha-prefixed markers for free, so `NO_INLINE_CITATIONS` still counts only `[^N]` and an external-only finding is still rejected; a *separate* extractor was added rather than changing what `extract_citations` counts, so the invariant can't drift. Save-time check is well-formedness only (known scheme + non-blank ref; no network, no FK, no row). An integrator fix masks external-marker spans before the caret-less-`[N]` guard runs, so a `[N]`-shaped substring inside a ref can't false-reject a valid finding. Zero migration cost — existing corpora gain the feature with no upgrade; no `SCHEMA_VERSION` bump (issue #563, omnibus #565)](GH-0563-external-citations-body-marker-not-table-0001.md)
- [GH-0547 — a research run is bound to **one conversation** via a UUID the agent mints itself, with **zero human setup**: the agent's first call is `bartleby skill session new` (mints a `run_key`, creates the run, returns the id), then it carries `--run <uuid>` on every later call; the runner resolves a session as `BARTLEBY_SESSION_NAME` (web) → `--run` (get-or-create the run bound to that key) → the `.active_session` marker (the forgot-my-id fallback), and per-`run_key` records (a UNIQUE index, not one shared marker) are the concurrency fix so two conversations on one corpus never collide; every result echoes the current run back so the agent can recover its id and spot a wrong-run fallback; the model is **self-reported** (`session new --model`, blank when unknown) and surfaced everywhere as "Set by LLM" — a claim, not a verified fact — derived from `run_key IS NOT NULL AND model IS NOT NULL` rather than a second column; prompt-adherence is accepted as flaky (a skipped `session new` falls back to the marker, usually right) in exchange for no human friction; the findings view shows the run's model with the "Set by LLM" tag and needs no hide-empty code (a zero-finding run has nothing to list) and no runs page (sessions are group headers only); **supersedes** the [sessions-auto-create](GH-XXXX-sessions-auto-create-0001.md) decision and lands the literal `_upgrade_v9_to_v10` that [GH-0508](GH-0508-record-embedding-model-0001.md) declined; additive, bumps `SCHEMA_VERSION` 9 → 10 (`project upgrade`, no re-ingest) (issue #547)](GH-0547-run-per-conversation-0001.md)
- [GH-0543 — the `/findings` list view becomes a **hero band over a Table/Grouped index**: a constant spotlight of the two **most-recent** findings (not most-cited, not user-pinned — most-recent needs no schema and no pin UI, and self-corrects as work lands) above an index that toggles between a dense table (default) and the same findings grouped by session (client-side toggle, no persistence); the hero findings **deliberately reappear** in the index — spotlight over a complete list, not a de-dupe bug; citation count renders as a proportional amber weight bar (scaled to the page max, `min-width` sliver for low non-zero, muted/empty at zero — the fill **must** be `display:block` since an inline span renders `width:%` at 0px); **frontend-only** because `listFindings()` already returns `created_at`-DESC rows with `session_name`/`citation_count`, so no query, server, or schema change; styles component-scoped with the `/documents`+`/search` `.entity-list` alignment left to a later pass; rejected most-cited heroes (conflates size with importance), pinned/starred heroes (needs an additive flag + pin UI), and grouped-by-session as the default index (an artifact of how research happened, better as an opt-in lens); no `SCHEMA_VERSION` bump (issue #543)](GH-0543-redesign-findings-hero-index-0001.md)
- [GH-0538 — a read-only `probe_dates` skill lets the agent validate a date regex *before* prompting a human to run the backfill: it reports `match_rate` / `normalized_ok` / `normalized_invalid` / `examples` / `unmatched_examples` over filenames (full or sampled) or sampled body, reusing #536's `compile_filename_date_regex` / `extract_filename_date` / `normalize_authored_date` helpers (named `date` group enforced), and emits a `suggested_command` (the exact `scribe backfill-dates --from-filename` line) **only** at `match_rate ≥ 0.9` **and only for `--field filename`** — backfill matches file_name/file_path, never body, so a body probe must not suggest a command that dates nothing; writes nothing (DB byte-identical before/after, test-pinned); SKILL.md gains verify-then-prompt guidance stating plainly the agent cannot run the backfill itself; no `SCHEMA_VERSION` bump (issue #538)](GH-0538-probe-dates-0001.md)
- [GH-0537 — `search` gains `--authored-after` / `--authored-before` / `--include-nulls` with exact **scan parity** by routing through the same shared `resolve_scope` / `_apply_date_bound` resolver `scan` uses (no reimplemented date SQL, so the two cannot drift — pinned by `test_search_date_filter_matches_scan_on_same_bounds`), and wires the previously-stubbed `excluded_null_dated` response count to the real number of undated chunks dropped; inclusive `YYYY-MM-DD` bounds, undated excluded unless `--include-nulls`; no `SCHEMA_VERSION` bump (issue #537)](GH-0537-search-date-filters-0001.md)
- [GH-0536 — `bartleby scribe backfill-dates` bulk-sets `summaries.authored_date` from a named-capture filename regex, so large templated corpora get a temporal axis **without re-ingest and without a schema bump** (dates stay on `summaries`): a human-run CLI admin op (deliberately off the agent surface, alongside `scribe`/`project upgrade`), fill-NULL-only unless `--overwrite`, invalid captures counted-not-written, `--dry-run` mutates nothing, idempotent; where no summary row exists it inserts an honest `model='backfill'` **stub** (empty title/description/text, **no chunks**) — including #254 section rows, which inherit the parent's date via the shared `file_name` — and the read side keeps stubs from lying: excluded from `summary_coverage`, `list_documents.has_summary` (false), and the `describe_corpus` #254 container tally, and `read_document --summary` returns null for them while `read_document` still exposes their `authored_date`; sentinel `BACKFILL_MODEL` in `bartleby/lib/consts.py`, filename helpers beside `normalize_authored_date` in `bartleby/ingest/summarize.py`; no `SCHEMA_VERSION` bump (issue #536)](GH-0536-backfill-dates-0001.md)
- [GH-0534 — findings carry a **plain-language contract** in `bartleby/skill/SKILL.md`, enforced at the prompt level only — a deliberate, scoped exception to the "enforce at script level" instinct, which applies where violations are mechanically detectable but not to a judgment over prose: write for an educated non-specialist, gloss terms of art on first use (never ban them), spell out acronyms, prefer plain syntax, all explicitly subordinate to the citation/honesty contracts; the rule covers the delivered reply for free by riding the verbatim-delivery contract (same bytes); rejected — readability lint (brittle on technical findings), an LLM judge/rewrite pass (a provider call per `save_finding`), a hard readability gate (rejects valid findings on a heuristic); docs-only, no `SCHEMA_VERSION` bump (issue #534)](GH-0534-plain-language-contract-0001.md)
- [GH-0528 — a consistent `--yes` vs `--force` flag convention across all of bartleby: two senses, two flags, never crossed — **`--yes`** = "proceed with the irreversible/data-losing action" (gates `project delete` and `project import` overwrite, each prompting unless passed), **`--force`** = "override a guard or skip that is *not* data loss" (`ready` reinstall, `read_document` past-budget, `tag` re-sweep — all unchanged). The lone rename is `project import --force → --yes` (overwrite drops local findings, so it's destructive); the three non-destructive `--force`s keep their name, so **no agent-facing skill flag changed and SKILL.md needed no edit**. No short forms anywhere — `project delete` loses its lone `-y`, uniform long-forms only. `import` now **prompts before overwriting unless `--yes`** (parity with `delete`), which **amends GH-0526's** rejection of an interactive confirm: the prompt is *additive to* the flag (unattended callers pass `--yes` and never reach it), so the no-silent-overwrite contract is preserved while interactive use gets a friendlier confirm than a bare error. The library `import_project(force=...)` keeps its `force` primitive; only the CLI flag/handler renamed. Rename-only, `SCHEMA_VERSION` unchanged (issue #528)](GH-0528-flag-convention-0001.md)
- [GH-0530 — `ship` / `ultraship` become vendored copies pressed from a local skill drawer rather than hand-authored here: one drawer source (`~/Code/workflows/skills/`) is the truth, `signet` presses a byte-identical copy into `.claude/skills/<skill>/`, and every bartleby-specific value the skills need (test command, gate agent, worktree scheme, decision paths, omnibus version scheme, live-corpus redline, playwright settings) moves into per-repo `.claude/ship.toml` / `.claude/ultraship.toml` that the press never touches; three repo-side bits keep it honest — the configs are git-tracked via `!.claude/ship.toml`/`!.claude/ultraship.toml` whitelists against the `.claude/*` blanket ignore (else a fresh clone routes into the wizard with an empty `player_guardrails`), the machine-local `.stamp` is gitignored (`.claude/skills/*/.stamp`), and `tests/test_skill_drift.py` runs `signet press <skill> --into . --check` on the normal `uv run pytest` gate (fails on drift, skips when signet/drawer absent — the clone/CI case) instead of a separate commit hook; `release` left repo-local (no second consumer); generalizing the SKILL.md content is drawer-side work, out of scope here; docs/tooling only, `SCHEMA_VERSION` unchanged (issue #530)](GH-0530-vendor-pressed-skills-0001.md)
- [GH-0526 — `bartleby project import` requires `--force` to overwrite an existing project (omnibus #494): the GH-0510 first cut overwrote a same-named project unconditionally once its compatibility gates passed — `rmtree`ing its `archive/` and replacing its `bartleby.db` with no confirmation, silently destroying a real corpus *and its local findings* (which a findings-free artifact can't restore); now a same-name collision is a hard refuse (clean non-zero exit, points at `--force`, existing project byte-untouched) unless `--force` is passed, decided **up front** before any download so a refusal costs no round-trip and can't perturb the existing project; mirrors `project delete --yes`, keeps idempotent re-import available as an explicit `--force` op (the encoded idempotency test now passes `--force`); rejected refuse-always (breaks idempotency), an interactive confirm (hostile to unattended import), and same-corpus auto-detect (over-built for a rare case); **`--force` governs only the overwrite — it never relaxes the schema/embedding compatibility gates** (still unconditional hard refuses); additive, `SCHEMA_VERSION` stays 9 (issue #526)](GH-0526-import-force-overwrite-guard-0001.md)
- [GH-0511 — critic-pass-1 fixes closing the publish→import contract (omnibus #494): four corrections from the post-assembly critic — (1) `bartleby project upgrade` no longer early-returns at `current == SCHEMA_VERSION`, so the GH-0508 `embedding_model` backfill is actually reachable for the existing-v9 corpora that need it (the unit tests had bypassed the CLI guard by calling `upgrades.upgrade()` directly); (2) import's `_land_files` stops swallowing every exception — only a genuinely-absent object refuses as a corrupt artifact, any other S3/disk error aborts, `write_bytes` moved inside the guarded region, and a freshly-created project is torn down on failure while a pre-existing one is never deleted; (3) `publish` refuses up front (pointing at `project upgrade`) when the source `meta` lacks `embedding_model`, so the publisher — the only party who can fix it — sees the problem instead of every importer; (4) non-SQLite blobs (`apsw.Error`) and boto3 client errors map to clean non-zero refusals, not tracebacks; no schema change (omnibus #494)](GH-0511-critic-pass1-fixes-0001.md)
- [GH-0510 — `bartleby project import <name> --from <s3-url>` adopts a published corpus as a fresh local project (omnibus #494): pulls the `.db`+content-addressed files from S3 and **hard-refuses** (non-zero exit, no `--force`) on `SCHEMA_VERSION` mismatch, embedding-model mismatch, or a missing `embedding_model` key — verified on a scratch copy *before* the project dir exists, so a refusal leaves nothing behind — then adopts the self-contained `.db` with **no id rekey**, lands files by `file_hash` (re-import idempotent), and supports `--without-tags`; reads the source `meta` directly rather than through `open_db` since the project isn't registered yet; no schema change (issue #520)](GH-0510-project-import-s3-0001.md)
- [GH-0509 — `bartleby project publish <name> --to <s3-url>` ships a clean, findings-free corpus copy to S3 (omnibus #494): a read-only `VACUUM INTO` makes a checkpointed single-file copy (never a live `cp` of a WAL DB), the session layer is stripped **on the copy** (delete `findings`/`sessions`/`finding_citations`, the `source_kind='finding'` chunks and their `chunks_vec` rows via new `bartleby.db.chunks` typed helpers `delete_chunks_of_kind`/`rebuild_fts`, then an FTS rebuild; finding-anchored `document_tags.chunk_id` nulled *before* the delete to respect the no-cascade FK), originals are gathered content-addressed by `file_hash`, and the `.db`+files upload over a thin boto3 seam (`bartleby/share/s3.py`, stubbed in tests — no moto); the source corpus is byte-identical after publish; no schema change (issue #519)](GH-0509-project-publish-s3-0001.md)
- [GH-0508 — the pinned embedding model (`EMBEDDING_MODEL`) is recorded in `meta['embedding_model']` so a transported corpus can be checked for embedding-space match, **without a `SCHEMA_VERSION` bump** (omnibus #494): new corpora already wrote the key at `init_db`, and existing v9 corpora are backfilled by an idempotent `INSERT OR IGNORE` riding the always-runs tail of `upgrades.upgrade()` (reachable via `bartleby project upgrade` once GH-0511 unblocked the CLI path), so the bundle stays additive and `v0.9.8` holds — a literal `_upgrade_v9_to_v10` chain entry was rejected because it would force the schema to 10; import-side verification lives in #520 (issue #517)](GH-0508-record-embedding-model-0001.md)
- [GH-0507 — `bartleby skill …` is blessed as the sanctioned human path for tag/finding curation with **one README sentence**, not a new CLI alias (omnibus #508): the schema table called `tags` "a controlled vocabulary the user curates" but never said *how*, and the CLI deliberately surfaces no `tag`/`finding` command because that work lives on the skill surface (`bartleby skill add_tag`, `assign_tag`, `save_finding`); a `bartleby tag …` alias was considered and rejected — it would duplicate a complete, tested surface, double maintenance, and blur the CLI-writes / skill-curates split — so the honest fix points the human at the existing path rather than growing a second; `SKILL.md` needed no change; docs-only, no `SCHEMA_VERSION` bump (issue #507)](GH-0507-bless-skill-curation-0001.md)
- [GH-0506 — test-suite hygiene: vacuous assertions made falsifiable and `test_embed` stops loading the real BAAI model (omnibus #508): an overlap test keyed on a periodic input (held even at zero overlap) → unique monotonic digit windows; an RRF-order assert admitting either top-two id → the full deterministic order pinned; a summarize/caption lane assert that read the lane key back and compared it to itself → exact `threading.get_ident()` tuples under `workers=1`; an `os.environ` offline-flag leak in `test_quiet.py` (written via `setdefault`, untracked by monkeypatch) → an autouse snapshot/restore fixture; and the real-model embed subprocess test → an in-process monkeypatch of `embed_texts` asserting list-form argv passes the query verbatim (chosen over a `slow` marker, which has no convention in `pyproject.toml`); dead imports/params removed; suite green, wall time ~2min→~18s; no production code, no schema change (issue #506)](GH-0506-test-suite-hygiene-0001.md)
- [GH-0505 — a failed `gh release create` after a pushed tag stashes the notes and prints the exact resume command instead of vanishing in a traceback (omnibus #508): the publish path pushes the tag *before* shelling out to `gh`, so a `gh` failure left a remote tag with no Release and the auto-generated notes lost in the `CalledProcessError`; the call is now wrapped to write the notes to the gitignored `REPO_ROOT/.claude/scratch/release-notes-<tag>.md` and print the ready-to-paste `gh release create … --notes-file <path>` to stderr before exiting non-zero — chosen over a re-run-idempotent path (which would re-run `git tag`+`push` and need remote-tag probing) as the smallest fix that still makes the operator notice; the catch covers both `CalledProcessError` and a missing-`gh` `FileNotFoundError` (the #508 critic's F3); no schema change (issue #505)](GH-0505-gh-publish-recovery-after-pushed-tag-0001.md)
- [GH-0504 — the Anthropic temperature/effort gates are **deny-lists of the older models**, so current and future models default to the safe path (omnibus #508; **supersedes [GH-0250](GH-0250-unconditional-temperature-drop-opus-47plus-0001.md)** on how the gates are keyed): GH-0250's closed *allowlists* were fail-unsafe for any model released later — `claude-fable-5` matched neither, so it kept `temperature` (a 400, the #250 regression) and silently lost its effort knob; inverting to deny-list the pre-4.7 temperature-acceptors and the pre-effort 400-ers makes Fable 5 and future models default safe with no per-release edit, and **explicitly NOT** a `model→params` table (triaged out as over-engineering); the #508 critic loop then extended both tuples to the full "and earlier" range — pre-4.5 models (`opus-4-0/4-1`, `sonnet-4-0`, `claude-3*`) and the dated snapshot IDs — which the first cut had dropped; separately `_extract_tool_input` now checks `stop_reason` so a `max_tokens` truncation names itself; code-only, no schema change (issue #504)](GH-0504-anthropic-denylist-sampling-params-0001.md)
- [GH-0503 — an ingest phase with no work shows `0/0` (finished), not `—` (omnibus #508): the run-of-show header read `—` as "this phase never ran", but caption and summarize early-return *before* `phase.start()` when there's nothing to do (all-text ingest has no images; a resumed run owes no summaries), so every plain-text ingest ended reading "skipped" when the phase had in fact run and found nothing; the fix calls `phase.start(0)` on those early-return branches (`ScribeProgress._start` already maps a zero total to a finished `0/0`), while the *vision-disabled* branch instead reveals and advances the real denominator to finish `N/N`; no new progress API, no schema change (issue #503)](GH-0503-empty-phase-shows-zero-of-zero-0001.md)
- [GH-0502 — `ready --dest` rmtrees only directories carrying our `.bartleby-skill` marker, not any with a `SKILL.md` (omnibus #508): `bartleby ready` wipes its destination before copying the packaged skill, but the guard accepted *either* a `SKILL.md` or the marker — and `SKILL.md` is the universal filename for any Claude skill, so pointing `--dest` at a foreign skill silently deleted it; the marker (stamped by `_install` on every copy) is the only real ownership signal, so `_looks_like_skill_dir` becomes `_is_ours` keyed solely on it, with a hard refusal (no `--force`, matching the prior guard) and a "not ours" message; empty and marked dirs still install/refresh; no schema change (issue #502)](GH-0502-ready-dest-rmtree-marker-only-guard-0001.md)
- [GH-0501 — local write robustness: a failed `init_db` unlinks its partial DB, a missing-`meta`-table gets the friendly recreate message, and the active-session pointer is written atomically (omnibus #508): `init_db` wraps its DDL+populate in `except BaseException` (incl. `KeyboardInterrupt`) to close the connection, `unlink(missing_ok=True)` the half-built file (which otherwise wedged the next `create` on the `FileExistsError` guard), and re-raise; `open_db` funnels the no-`meta`-table case into the same recreate `RuntimeError` as the missing-row case instead of leaking a raw `SQLError`; and `write_active_session_id` writes a sibling `.tmp` then `os.replace()`s it so the constantly-polling skill reader never sees a truncated pointer; three unrelated mechanisms, one theme (a failed/raced local write must not leave the next caller worse off), no schema change (issue #501)](GH-0501-local-write-robustness-init-cleanup-atomic-session-pointer-0001.md)
- [GH-0491 — CLI error-handling & UX consistency: one convention enforced end-to-end — every expected user error exits 1 with a one-line message on **stderr** (never a traceback), success/tabular output stays on stdout, and wizard prompts validate their own bounds; eight additive fixes — scribe's expected failures (no-active-project `RuntimeError`, bad converter / typo'd `--only` `ValueError`, missing-path `FileNotFoundError`) wrapped at the `cli._scribe` dispatcher and `project upgrade`'s bare `validate_project_name` mirrored on create's try/except; `project`/`session`/`logs` error-exits rerouted through `bartleby.lib.console.error` (the shared stderr console) while their success tables stay on the stdout `_console`; `logs --limit` rejects non-positive values (SQLite reads a negative LIMIT as unbounded → dumped every row); a new `_prompt_bounded_int` bounds `ocr_min_confidence` to 0–100 (the old `_prompt_positive_int` rejected the meaningful `0` and accepted `250`, left untouched to avoid rippling its other callers); `_resolve_llm_provider` warns when `--provider/--model` are silently discarded because summaries are off; dead wizard `config.pop` calls removed (`main` rebuilds config from `{}`); `_prompt_api_key`'s wsjpt fallback help fixed to name `GEMINI_API_KEY` via a new `_api_key_help`; no schema bump (issue #491)](GH-0491-cli-error-handling-ux-0001.md)
- [GH-0489 — `reasoning_effort` is validated at the scribe config-read chokepoint and **hard-fails** (director call 2026-06-11): an out-of-enum value exits 1 on stderr naming the field, the bad value, and the allowed set (`minimal, low, medium, high`) — never a bare `KeyError`, never a silent fall-back to the default that would mask a config typo; the single seam sits *before any provider call* so it covers anthropic's `_EFFORT_MAP` lookup and the OpenAI/wsjpt/ollama forwards at once; the enum + default moved to the import-light `bartleby/lib/consts.py` so scribe and the wizard can't drift, and the anthropic provider's raw lookup became `_map_effort` (a `.get` raising a named `ValueError`) as a defense-in-depth backstop for direct callers; no schema change (issue #489)](GH-0489-validate-reasoning-effort-0001.md)
- [GH-0490 — `ensure_provider_env` stops clobbering an exported `OLLAMA_API_BASE`: the ollama branch assigned the env var unconditionally (even with no `ollama_base_url` configured), overwriting a user-exported remote URL with the localhost default and contradicting the docstring + README that the four other branches honor; director call (2026-06-11) took the code fix over a doc-only line — set `OLLAMA_API_BASE` only when not already present, mirroring the api-key branches, so precedence is exported-env > configured `ollama_base_url` > localhost (what `OllamaProvider.__init__` already assumes); README/docstring stay accurate as written; additive, no schema bump (issue #490)](GH-0490-ollama-api-base-no-clobber-0001.md)
- [GH-0488 — project names are validated at the resolve/open **chokepoint** to close a path-traversal gap: `validate_project_name` was enforced only by create/delete/info/upgrade, so the hot path (`resolve_project_name → project_db_path`, `set_active_project`, the skill runner's agent-supplied `--project`) built `PROJECTS_DIR / name` for any string incl. `../../foo` — opening arbitrary `bartleby.db` files or mkdir/writing at traversed paths; the fix validates at the two true funnels (`resolve_project_name`, which open_db/skill-runner/scribe/session/logs all pass through, and `set_active_project`, the only `project use` path) *before* any path build or mkdir, raising the existing invalid-name `ValueError` with no filesystem touch — not at the pure path builders, which legitimately handle already-trusted on-disk names; the `connection.py` validation import is lazy to avoid a circular import; creating a valid new name still works; input-validation only, no schema change (issue #488; serve-side `_override_project` gap closed as an omnibus #492 integrator commit)](GH-0488-validate-project-name-chokepoint-0001.md)
- [GH-0487 — `bartleby project info --verify` is a read-only corpus integrity audit over one shared check module: counts said nothing about structural consistency — `chunks`/`chunks_fts`/`chunks_vec` drift, orphaned `source_id`, missing tables, or a stamped `schema_version` the on-disk shape didn't match surfaced only as a raw apsw traceback mid-query, if at all; `--verify` runs four checks (**tri_table_sync** via FTS5's `'integrity-check'` two-arg `rank=1` command — the one-arg form is a no-op for content/index drift; **no_orphan_chunks** per `source_kind`; **schema_matches_stamp** as a DDL-subset of `sqlite_master` so vec0/FTS5 shadow tables don't false-positive; **failed_ingests_sanity**), always the full battery, exiting 0 all-pass / non-zero on any FAIL with a per-check line, never mutating the corpus; the tri-table logic was lifted out of `tests/_skill_fixtures.py` into a new shippable `bartleby/integrity.py` consumed by both command and suite; the verify path opens its own raw connection (no version gate) so it can *report* the very schema mismatch `open_db` raises on, and each check is guarded so a corrupt DB FAILs rather than tracebacks (the per-check guard added as an omnibus #492 integrator commit); the polymorphic-chunks chokepoint guard now exempts only the FTS5 *command* form (`chunks_fts(chunks_fts, rank …)`), pinned by positive+negative assertions (issue #487)](GH-0487-project-info-verify-0001.md)
- [GH-0467 — `save_summary` carries `authored_date` forward on replace: the full-row REPLACE deleted the prior summary and, because `--authored-date` defaulted to `None` and was normalized straight into the INSERT, *re-saving* a dated summary without re-passing the date silently wrote NULL — dropping the doc out of every authored-date scope (`--authored-after`/`--authored-before`, the date `list_documents`/`scan`/`search` report); a data-loss footgun for the common "fix the title, keep everything else" re-save; the issue offered (a) a doc-only warning or (b) a carry-forward behavior fix and the director took the behavior flip (2026-06-11, the silent drop is a correctness bug not a doc gap, and the replace path already SELECTs the prior row), so the prior-row SELECT now also fetches `authored_date` and binds it into the INSERT when `--authored-date` is omitted **and** a prior row exists; three cases — replace+no-date carries forward, brand-new+no-date stays NULL (never fabricates), explicit always overwrites (still normalized; malformed still stores NULL, and `save_date` with its `--clear` flag stays the dedicated date-only path); bounded to the write-path, no schema change, no new clear flag on `save_summary` (issue #467)](GH-0467-carry-authored-date-forward-on-replace-0001.md)
- [GH-0464 — `scan` + `search` restrict the FTS `MATCH` to the `text` column: `chunks_fts` indexes both `text` and `section_heading`, so the bare `chunks_fts MATCH (<expr>)` on both surfaces also fired on heading-only hits — a chunk whose snippet never contains the term — inflating `scan` grep-totals/`--count-by` histograms and injecting heading-only chunks into `search`'s full-text RRF leg, so the two surfaces disagreed on what "matches" meant and neither agreed with the snippet the agent reads; both `MATCH` sites now column-qualify to the body text via FTS5's `{text} : (<expr>)` filter syntax, wrapped **only when the expression is non-empty** (an empty token set stays `""`, never a malformed `{text} : ()`), through one shared `text_qualified_fts` helper in `_common.py` applied at the param site *after* each surface's existing empty/no-match short-circuit; query-level only — no schema bump, no re-index (`chunks_fts` still indexes both columns, we just stop asking it to match the heading); cross-surface parity was the deciding call (2026-06-11: move both to text-only together rather than fix one and leave the other ambiguous) and deliberate heading recall relocates to the tools built for it — `scan`'s `--heading-like` (#367) and `search`'s vector leg (unaffected by the FTS column restriction); inverse tests assert a heading-only term yields no `scan` match and no `search` FTS-leg hit, with positive controls (issue #464)](GH-0464-scan-search-fts-match-text-column-only-0001.md)
- [GH-0420 — `scan --extract '/regex/'` (repeatable) emits, per matching document chunk, one row of `chunk_id`/`document_id`/`file_name` plus every pattern's capture group(s) as columns — a **tidy table for the agent to post-process, explicitly not a query engine** (the #48/#10 boundary: no predicates/joins/aggregation); it shares one capture primitive (`CaptureSpec` + `parse_capture_regex` in `_common.py`) with `--count-by '/regex/'`, which becomes extract-then-group-and-count over that primitive; column naming is fixed at parse time — a named group `(?P<amount>...)` → column `amount`, bare groups → positional `g1`/`g2`/... over `re`'s 1-based indices; a non-matching pattern yields a **null cell, never a dropped row** (a row is a chunk not a match, which is what makes several `--extract` patterns composable and keeps it a projection not a filter); only the **first match per chunk** is captured (`--extract` is the one-row-per-chunk projection, `--count-by` is the per-match fold — the two split on cardinality but share the compile/validate/column machinery); multiple patterns must produce distinct columns (`EXTRACT_COLUMN_COLLISION`, rejecting a silent clobber); the runaway rails shared by both modes were renamed `COUNT_BY_*` → `CAPTURE_*` and `--extract` can't combine with `--count-by`/`--preview`/`--brief`/`--returning` (its dynamic capture columns *are* its output contract); additive, no schema change (issue #420)](GH-0420-scan-extract-capture-columns-0001.md)
- [GH-0419 — `search`/`scan`/`read_chunks`/`list_documents` gain `--returning chunk_id,document_id,...` projecting each row to exactly the named top-level fields in order through **one** shared keystone helper `project_row(full, requested, whitelist)` in `_common.py` (plus `add_returning_arg` and the `comma_field_list` argparse type): the caller builds the full whitelisted row then `project_row` returns `None` (no flag → caller keeps its own default/brief shape untouched, purely additive, default output byte-for-byte unchanged) or `{field: full[field]}` in requested order, with an unknown field raising the `UNKNOWN_RETURNING_FIELD` JSON envelope naming `valid_fields` (not argparse's exit-2); the whitelist is **per-script** (a module-level `*_FIELDS` constant — the four scripts' rows have nothing structural in common, mirroring `--brief`'s GH-0105 per-command-semantics precedent) but the validate-and-select *mechanism* genuinely is shared so the single helper earns its keep; `chunk_id` and `document_id` lead **every** whitelist so a citable id is recoverable in one call instead of a re-read — incl. `scan --count-by document` which exposes a representative `MIN(chunk_id)` folded into the existing GROUP BY (selectable but not in the default histogram row; `--count-by '/regex/'` instead rejects `--returning` as `RETURNING_NOT_APPLICABLE` since a captured value spans documents), with `document_id` honest-null off non-document chunks; `--returning` overrides `--brief`; additive, no schema change (issue #419; eager-validation hardening in the omnibus #472 integrator commit so a typo'd field errors even on a zero-row query)](GH-0419-returning-projection-helper-0001.md)
- [GH-0442 — the caption (#166) and summarize (#188) ingest passes now drive their progress through the `ScribeProgress` **phase handle directly** instead of a two-way adapter that existed only to translate between two shapes nobody else consumed: `_caption_all`/`_summarize_all` had wrapped a `_ProgressTally` that turned `advance()` into `on_progress(done, total)`, which `scribe.py` then routed through `_Phase.on_progress` **back** into `start(total)`/`set_completed(done)` — a pure round-trip onto the same three primitives, with `set_completed`/`ScribeProgress._set_completed` existing solely to serve it and a second `on_lane` callback that was just `phase.lane`; the only production consumer of the `(done, total)` contract was `ScribeProgress` itself; fix: the two helpers now take `phase: _Phase | None` and call `phase.start(len(pending))` once + `phase.advance()` per item (every call guarded `phase is not None` so the headless/test path is a clean no-op), `scribe.py` passes `progress.phase("caption")`/`progress.phase("summarize")` directly, and `_ProgressTally`/`_Phase.on_progress`/`_Phase.set_completed`/`ScribeProgress._set_completed` are deleted — the parse phase already drove its handle this way, so all three phases are now uniform; rendered progress output is bit-for-bit unchanged (the reveal `start`, per-item `_advance(name, 1)`, and lanes all land on the same primitives), tests pass a `_StubPhase` and exercise the bar via `advance()`; no schema change (issue #442)](GH-0442-drop-progress-tally-0001.md)
- [GH-0441 — `_resolve_caption_workers` and `_resolve_summarize_workers` in `bartleby/ingest/resolve.py` (two near-identical ~71-line functions implementing the **same algorithm** — read a configured count with a default, let `--timings` force 1 with a warning only when configured > 1, clamp an Ollama provider to 1 with a warning only on an explicit non-default count per #243, else `max(1, configured)`) collapse into one parameterized helper `_resolve_io_workers(config, *, key, default, provider, verb, unit, timings)`; they differed only in config key, default constant, provider source, and two warning nouns, and had moved in lockstep through #243/#314 (their docstrings even cross-referenced each other); `scribe` calls it twice, threading `config.get('vision_provider')` for captions and the already-computed #314 effective provider for summaries (so the override-folded-provider distinction lives in the call site, not a per-function branch), `verb`/`unit` shape the two warning strings as f-string templates reproducing the prior text exactly; all three load-bearing behaviors (`--timings` clamp, Ollama explicit-value clamp re-reading raw `config[key]`, `max(1, n)` floor) are preserved byte-for-byte, tests route through small `_caption_workers`/`_summarize_workers` wrappers; no schema change (issue #441)](GH-0441-merge-worker-resolvers-0001.md)
- [GH-0439 — the `EMPTY_NAME` guard is removed from `add_tag` and `rename_tag`, leaving the single `normalize_name` guard: both scripts ran two sequential guards on the same already-`.strip()`-ed input (`if not name: raise EMPTY_NAME` then `if not normalize_name(name): raise EMPTY_NORMALIZED_NAME`), but `normalize_name` deletes every non-`[a-z0-9]` character (`_NAME_NORMALIZE_RE = r"[^a-z0-9]+"`), so any empty/whitespace-only name normalizes to `""` (falsy) and is *always* caught by the second guard — there is no input that fires `EMPTY_NAME` where `EMPTY_NORMALIZED_NAME` would not, dead code by definition; a blank/whitespace-only name now reports `EMPTY_NORMALIZED_NAME` ("must contain at least one alphanumeric character"), a truthful description of the blank case too, and nothing in the repo (tests, SKILL.md, other scripts — repo-wide grep confirms no surviving `EMPTY_NAME` reference) distinguished the two codes; `add_tag`'s unrelated `EMPTY_DESCRIPTION` guard stays; pure deletion, no schema change (issue #439)](GH-0439-drop-empty-name-guard-0001.md)
- [GH-0440 — the per-format parse helpers in the ingest parse module stop **exploding `ParseConfig` into seven scalars** and take the frozen dataclass itself as a positional `config` argument: `ParseConfig` (already picklable and already crossing the spawn boundary into `_parse_request`) was being disassembled into `pdf_converter`/`html_converter`/`sparse_text_threshold`/`ocr_min_confidence`/`vision_enabled`/`vision_max_dimension`/`vision_min_dimension`/`archive_root` and re-threaded by hand through `_parse_document`, `_parse_pdf_pdfplumber`, `_parse_pdf_docling`, `_parse_image_file`, and `_parse_image_routes` (the same keyword block repeated across six signatures and four call sites, `_parse_document` alone a 17-line signature); now each helper reads `config.vision_max_dimension` etc. directly, `_parse_request` passes `config` through unexploded, and no helper's signature exceeds `(path, config, identity, callbacks)`; every value still originates from the same dataclass, the helpers stay DB-free, no new pickling surface is created (the config is already present in the worker process) — the only thing given up is per-helper signature documentation of which settings each consumes; tests build a `ParseConfig` via a small `_parse_config(...)` factory; behavior unchanged, no schema change (issue #440)](GH-0440-parseconfig-down-parsers-0001.md)
- [GH-0438 — `validate_value_type` in `_tags.py` is **deleted** and the `INVALID_VALUE_TYPE` code removed from the vocabulary because argparse's `choices=` already guards it: the function's only repo-wide caller was `add_tag.work()`, and `add_tag` declares `--value-type` with `choices=list(VALUE_TYPES)` (`"number"/"string"/"date"`), which argparse enforces during `parse_args` — and `parse_args` runs *before* the runner's `work()` (the #402/GH-0402 seam) — so by the time `validate_value_type(args.value_type)` would have run, the value is provably `None` or a member of `VALUE_TYPES` and the `raw not in VALUE_TYPES` branch can never fire (a dead identity check guarding an unreachable code); an invalid `--value-type` now surfaces as the `USAGE_ERROR` envelope per GH-0402 (stderr usage naming number/string/date), the same surface as every other malformed flag (missing required, non-integer ids, `list_documents --order`, `scan --group-by`), and no test or SKILL.md referenced `INVALID_VALUE_TYPE`; `add_tag.work()` now reads `args.value_type` directly; net shrink, no schema change (issue #438)](GH-0438-delete-validate-value-type-0001.md)
- [GH-0437 — finishing the residue #432 left after inlining `intersect_tag_filter`/`intersect_file_like_filter` into `resolve_scope`, this folds `Scope.filters_dict` (the 18-line "build the self-describing `filters` echo, or `None` when unfiltered" method) inline into its **sole caller** `Scope.echo_into` (guarded directly by `if self.active:`) and corrects two docstrings advertising consumers that no longer exist: `resolve_tag_names` no longer claims it's used by `list_documents`/`search`/"any future consumer of `--tag`" (its only caller is `resolve_scope`, through which all four scope scripts route their `--tag`), and the `Scope` class docstring's "self-describing via `filters_dict`" now points at `echo_into`; no call sites changed (the four consumers `search`/`scan`/`describe_corpus`/`list_documents` already called `echo_into`), the emitted `filters` dict is byte-for-byte identical (same keys/order/`if self.active` gate), and the memory wall plus the `document_ids` filter-set resolution are untouched — only the single-caller echo-formatting indirection collapsed, the seam to be re-extracted when a second caller actually arrives; no schema change (issue #437)](GH-0437-collapse-scope-filter-chain-0001.md)
- [GH-0445 — two scribe-level e2e tests that re-prove behaviors living entirely inside `summarize.summarize()` (and already unit-tested more strongly in `tests/test_summarize.py`) are cut: `test_scribe_truncation_note_in_summary` drove an absurdly small `max_summarize_tokens` through the full pipeline only to re-assert the truncation-note strings that `test_summarize_long_doc_truncates_input_and_appends_note` already pins *more* strongly (it also verifies the provider received `<= N` tokens), so it's deleted outright; `test_scribe_drops_malformed_authored_date` (fed `"Q3 2024"` e2e, asserted persisted `summaries.authored_date` came out `NULL`) is **not** a pure cut — the existing parametrized unit test calls `normalize_authored_date(raw)` directly and would still pass if the `authored_date=normalize_authored_date(...)` call site regressed to a raw pass-through, so it's swapped for a tighter `test_summarize_drops_malformed_authored_date_through_summarize` that exercises the call site *through `summarize()`* (a `FakeProvider` returning `"Q3 2024"` → `result.authored_date is None`), preserving the one genuinely-unique assertion since `summaries.authored_date` is assumed strict-ISO by date-filter tags, the `describe_corpus` year histogram, and scan/list date sorts; the incidental config-threading coverage (`max_summarize_tokens` reads from config and threads to `summarize()`) is intentionally dropped as a literal benign kwarg pass-through still exercised for `model`/`temperature` by `test_scribe_writes_summary_when_provider_configured`; net ~71 e2e lines removed, ~9-line unit added, full suite green, no schema change (issue #445)](GH-0445-drop-scribe-summarize-retests-0001.md)
- [GH-0444 — the summarizer prompt stops carrying a **hand-maintained restatement** of the `DocumentSummary` field definitions: `SUMMARY_INSTRUCTIONS` (in `bartleby/providers/prompt.py`) is now built at import time from a one-line framing sentence plus a per-field bullet rendered from `DocumentSummary.model_fields[name].description`, collapsing two hand-synced copies into one since those Field descriptions in `base.py` were already the enforced source of truth for every provider's structured-output channel (Anthropic tool-use `input_schema`, OpenAI `response_format`, Ollama `format=`); the duplication had already drifted exactly as `benchmark/judging.py`'s framing-only `JUDGE_INSTRUCTIONS` pattern warns against — the old prompt's `description` bullet silently lost the `never exceed 200 characters` ceiling the schema and judge rubric impose — and the only ability lost is for prompt text and schema to disagree, which was only ever a (realized) drift hazard; the rules had to render **into the constant** not just the message builder because `wsjpt.py` consumes `SUMMARY_INSTRUCTIONS` directly as `custom_instructions` without calling `build_summary_messages` (else the wsjpt/Gemini channel would lose all four bullets) and Ollama's grammar-constrained `format=` never sees the schema descriptions as prompt text — which is why the constant keeps full per-field content rather than shrinking to a framing line; `summarize.prompt_sha()` takes a one-time provenance break (the regime change the leaderboard's heterogeneity warning surfaces), no test asserted the old text, `SCHEMA_VERSION` stays 9 (issue #444)](GH-0444-render-summary-instructions-from-model-fields-0001.md)
- [GH-0443 — the hand-maintained 23-name `SCRIPTS` tuple in `bartleby/commands/skill.py` is replaced by a **derivation over the `bartleby.skill_scripts` package** (`tuple(sorted(m.name for m in pkgutil.iter_modules(skill_scripts.__path__) if not m.name.startswith("_")))`): the literal was, element for element, the directory listing minus its two underscore-convention shared helpers (`_common.py`/`_tags.py`), and every new script previously required a parallel tuple edit or silently failed dispatch with `UNKNOWN_SKILL`; the empty `__init__.py` means no extra import cost, the `UNKNOWN_SKILL`/`MISSING_SKILL` envelopes and `_print_help` are byte-for-byte unchanged (only the name *source* moved from literal to directory), and dispatch remains an **allowlist** (a name not in the derived tuple still gets `UNKNOWN_SKILL`, so no arbitrary module/dotted-path injection — underscore exclusion plus "real module in this one package" is the whole gate); trades away the ability to exclude a dropped-in module by registry omission (now requires the underscore prefix) and the hand-grouped help ordering (now alphabetical, weightless since SKILL.md is the agent reference); the #411 guard test now imports `SCRIPTS` from the dispatcher and `test_dispatcher_roster_derives_from_package` asserts the derivation *property* rather than mirroring a frozen 23-name snapshot; no schema change (issue #443)](GH-0443-derive-scripts-registry-0001.md)
- [GH-0413 — omnibus v0.9.2 integrator critic-loop fixes, three patch-level corrections applied directly on the omnibus branch: (1) **narrow the runner's `USAGE_ERROR` arm** — #402 had moved `parse_args` inside `skill_runner.run`'s main `try` with an `except SystemExit` that would mislabel *any* `SystemExit` (a future `work()` or a library `sys.exit`) as `USAGE_ERROR`; now only the `parse_args(argv)` call carries its own `try/except SystemExit` (non-zero → `USAGE_ERROR` envelope + exit 1, clean 0 → re-raise so `--help` still exits 0), and a `SystemExit` from `work()` is a `BaseException` the `INTERNAL_ERROR` catch-all never swallows; (2) **pin the #411 guard roster to the dispatcher** — `test_skill_flag_conventions.py` now asserts `set(SCRIPTS) == set(bartleby.commands.skill.SCRIPTS)` so a newly-registered script can't silently escape the convention guard; (3) **`positive_int` on the remaining id flags** (`save_date`/`save_summary` `--document`, `delete_finding`/`edit_finding`/`read_finding` `--finding`, `assign_tag` `--chunk`, `tag` `--document`, `merge_findings` `--into`) since ids are autoincrement `>= 1`, leaving comma-int lists and the already-#403 `--limit`/`--offset` alone; no schema change (omnibus #413 integrator commit)](GH-0413-integrator-critic-fixes-0001.md)
- [GH-0411 — a parser-introspection guard test (`tests/test_skill_flag_conventions.py`) makes the #111 flag conventions **structurally enforced** rather than reviewer-vigilance-only (the very drift #403/#405/#408 each fixed after the fact): it introspects each skill script's argparse parser — patching the final `parser.parse_args` to raise with the parser as payload so the build runs without per-script valid argv — and asserts exactly four conventions with deliberately *literal* rosters (not generalized inference, which would be a brittle spec-mirror): #403 (`--limit`→`positive_int`, `--offset`→`nonneg_int` by identity), #405 (a single-existing-tag selector is named `--tag` across `delete_tag`/`assign_tag`/`unassign_tag`/`extract`/`tag`, excluding `add_tag`'s create-`--name` and the two-tag relational scripts), #408 (every scope script exposes `--in-documents`), and #111 (no redundant `-id` flag suffix); a non-conforming script shape is skipped, not silently passed; tests-only, no schema change (issue #411)](GH-0411-flag-convention-guard-0001.md)
- [GH-0410 — dispatcher + runner JSON-contract tests fill the genuine gaps after sibling issues (#339/#401/#402/#407) already pinned most of it: the runner's **`NO_ACTIVE_PROJECT`** arm (no `--project`, no active project → envelope + exit 1, asserting `open_db` is *never* called since the SkillError fires before the DB opens, so no conn to leak and no audit row) was the one untested error arm; plus dispatcher **routing + remaining-argv passthrough** (a known name routes to `skill_scripts.<name>.main`, name consumed, rest of argv verbatim) and **exit-code propagation** (a script's `sys.exit(2)` surfaces 2 unchanged — the dispatcher doesn't swallow `SystemExit`); explicitly *did not* build the fragile "induce a failure in all ~23 scripts" sweep (per-script envelopes are pinned by their own tests); tests-only, no schema change (issue #410)](GH-0410-dispatcher-runner-tests-0001.md)
- [GH-0409 — six new tests pin `read_chunks --around-chunk` **window mechanics** against a freshly-seeded 9-chunk document (`_wide_document` helper on `seeded_project` + the typed `insert_document_chunks`): default window (`window == 3`, symmetric ±3), explicit `--window 1` (echo + ±1), `--window 0` (target alone), clamping at document start (no negative indexes) and end (no overrun, asserted by observed `BETWEEN`-range behavior), and an unknown id → canonical `CHUNK_NOT_FOUND`; the pre-existing around-mode tests only exercised the memory wall, leaving the window arithmetic and not-found path uncovered (the issue body was stale, asking for tests that already exist — the real gaps were filled instead of duplicating); tests-only, no production change (issue #409)](GH-0409-read-chunks-around-coverage-0001.md)
- [GH-0408 — `list_documents` gains the `--in-documents` scope flag, closing the lone gap among the scoping scripts (`search`/`scan`/`describe_corpus` carried it per #111); purely additive and the plumbing already existed — `resolve_scope` in `_tags.py` accepts `in_documents` and `Scope` echoes it into `filters`, `list_documents` simply never defined the flag — so the fix copies `scan`'s arg block verbatim (`comma_int_list("document_id")` `--in-documents`, `dest=in_documents`) and threads `args.in_documents` into the existing `resolve_scope` call; default unscoped behavior unchanged (no flag → `None` → no scope, `filters` echo absent); SKILL.md's row updated; no schema change (issue #408)](GH-0408-list-documents-in-documents-0001.md)
- [GH-0407 — `skill_runner.run` now **closes the conn on every opened path**, not just resolved sessions: the teardown gated *both* `conn.close()` and the audit `log_call` on one `if conn is not None and session_id is not None:`, conflating a resource obligation with the audit write's need for an FK target — so when `open_db` succeeded but session resolution then raised (a `BARTLEBY_SESSION_NAME` lookup or `ensure_active_session` failure), `session_id` stayed `None`, the guard was false, and the open conn leaked; the fix nests the guards (`if conn is not None:` always closes; `log_call` stays under `if session_id is not None:`), and the docstring's unconditional "one audit row per invocation" claim is corrected to "once a session is resolved"; envelope and exit codes unchanged; no schema change (issue #407)](GH-0407-skill-runner-conn-close-0001.md)
- [GH-0406 — `read_chunks` **documents** (does not enforce) that flags belonging to an unselected mode are silently ignored: its three mutually-exclusive modes (`--document`/`--chunks`/`--around-chunk`) leave out-of-mode flags like `--window`/`--offset`/`--limit` simply unread — *inert, not unsafe* — so the fix is a one-sentence caveat in the module docstring (which also feeds `--help` via `build_arg_parser`), not a `p.error()` leg that would require fragile explicit-vs-default flag detection (over-building for an inert condition, and the issue's acceptance criteria accept the docstring route); behavior unchanged, no schema change (issue #406)](GH-0406-read-chunks-mode-doc-0001.md)
- [GH-0405 — `delete_tag`'s tag-identifying flag is renamed `--name` → `--tag`, aligning it with `assign_tag`/`unassign_tag` which already use `--tag` to name an *existing* tag; `--name` is reserved across the tag scripts for the tag being *created* (`add_tag --name … --description …`), so `delete_tag` taking `--name` for an existing tag was a genuine inconsistency, while `rename_tag` (`--old`/`--new`) and `merge_tags` (`--from`/`--into`) name two tags and keep their relational flags; per the repo's no-back-compat default the old `--name` is deleted outright (no alias/deprecation); SKILL.md row + the cascade test updated; no schema change (issue #405)](GH-0405-delete-tag-flag-rename-0001.md)
- [GH-0404 — `rename_tag` now applies `add_tag`'s **normalized-name duplicate check**: `add_tag` collapses `"NYSEG"`/`"nyseg"`/`"ny-seg"` to one key via `find_similar_tag`, but `rename_tag` had only an *exact*-match lookup (`get_tag_by_name`), so renaming `"ny-seg"` → `"NYSEG"` onto an existing `"NYSEG"` slipped past and created a normalized-duplicate pair; the fix runs the same normalized-equality check through a new `_tags.find_tag_by_normalized_name` helper (built on `normalize_name` + `fetch_vocabulary`) and raises the same `TAG_EXISTS` envelope, while retaining the `tag_id != target` guard so a case/punctuation-only *self*-rename stays allowed; deliberately scoped to the normalized leg only (no embedding/similarity check — renaming a named tag isn't a fuzzy proposal); additive, no schema change; `merge_tags` remains the path to combine two distinct tags (issue #404)](GH-0404-rename-tag-normalized-dup-0001.md)
- [GH-0403 — shared `positive_int`/`nonneg_int` argparse validators now cover pagination + id flags uniformly: `scan`/`search` already routed `--limit`/`--offset` through them, but `read_chunks`/`list_documents`/`list_findings` still took bare `type=int` (accepting `0`/negatives that silently mis-paginate or surface opaquely) and `read_document --document` was bare `int` too; the fix is a narrow swap (no new helper) — `--offset`→`nonneg_int`, `--limit`→`positive_int`, the `--document` id→`positive_int` — so an out-of-range value raises `ArgumentTypeError`→`SystemExit`→the `USAGE_ERROR` envelope + exit 1 *before* any DB/session opens (the #402 seam); deliberately scoped down — id-only flags on scripts with no pagination were left on bare `int`, outside this issue's scope; no schema change (issue #403)](GH-0403-shared-pagination-validators-0001.md)
- [GH-0402 — argparse usage errors now emit the JSON error envelope instead of argparse's raw `usage:` dump + `SystemExit(2)` (the lone escape hatch breaking the skill contract that every output is the `{"error","code"}` shape on stdout): the fix is a single seam, not a per-script change — `parse_args(argv)` moves *inside* `skill_runner.run`'s existing `try`, and a new `except SystemExit` arm splits on exit code (falsy `None`/`0` from `--help`/`-h` re-raised untouched so help still exits 0; any non-zero → `{"error": "Invalid arguments. See --help for usage.", "code": "USAGE_ERROR"}` on stdout + exit 1), returning before any DB/session opens; argparse still writes its `usage:` line to *stderr* (a separate, harmless stream); the one runner chokepoint covers all ~18 scripts (no `ArgumentParser.error()` subclass spread); two existing exit-2 conflict tests updated to exit-1 + `USAGE_ERROR`; no schema change (issue #402)](GH-0402-argparse-json-envelope-0001.md)
- [GH-0401 — bare `bartleby skill` (no script name) now returns a JSON error envelope instead of breaking the contract: the dispatcher in `commands/skill.py` lumped empty argv in with `-h/--help` (`if not argv or argv[0] in ("-h","--help")`), printing usage prose to stdout with exit 0 — which an agent parsing stdout as JSON would choke on; the fix splits the two cases — `-h/--help` keeps its verbatim help-to-stdout/exit-0 behavior, while empty argv mirrors the unknown-skill path (usage prose to **stderr** via `_print_help(sys.stderr)`, a `{"error": "No skill script given. Available: …", "code": "MISSING_SKILL"}` envelope to stdout, `sys.exit(1)`); the new `MISSING_SKILL` code names the missing-script case distinctly from `UNKNOWN_SKILL` (a *named* script that isn't registered); smallest fix, no shared envelope abstraction; no schema change (issue #401)](GH-0401-bare-skill-json-envelope-0001.md)
- [GH-0371 — the embedding/model import stack is **lazy-imported** for the four FTS-only read scripts (`scan`, `list_documents`, `read_chunks`, `describe_corpus`), which previously paid it on every invocation despite never embedding: the cost lived in two shared modules they pull in (`_common.py` imported the chunker + `embed_texts` at top, dragging in `docling`+`filetype`; `_tags.py` imported `embed_texts` at top), so the fix moves those heavy imports to **function-local** inside the only functions that embed (`embed_body_chunks` in `_common`, `find_similar_tag` in `_tags` — placed *after* its empty-vocab/exact-match early returns so an exact-name hit never triggers it); importing `scan` in a fresh interpreter drops from 428→407 modules (all 15 `docling.*`/`filetype.*` gone; `numpy` still rides in via `sqlite-vec`, core read infra, out of scope) and ~71.5→~67ms; a fresh-interpreter regression test (`test_skill_read_import_cost.py`) asserts none of the four pull in `bartleby.ingest.embed`/`.chunk`/`sentence_transformers`/`torch`/`docling`/`filetype`, and monkeypatch targets were repointed to the source modules since the names are no longer module-level attributes; `skill batch` mode out of scope; no schema change (issue #371)](GH-0371-lazy-import-embedding-stack-0001.md)
- [GH-0364 — hoist `merge_findings`' embed out of the write-lock window, mirroring #340's fix and **correcting GH-0340's wrong "buys it nothing" claim**: `merge_findings` is `run(mutates=True)` so the runner wraps `work()` in one deferred apsw transaction (no write lock until the first write), but its first write is the `UPDATE findings` and it called the combined `rebuild_finding_chunks` (embed+write) *after* that `UPDATE`, holding the write lock across the ~5–10s lazy sentence-transformers model load while `busy_timeout` is only 5000ms — the identical `BusyError`/`INTERNAL_ERROR` exposure #340 fixed for the other three scripts (GH-0340 had assumed `merge_findings` already held a write txn across embedding, which it did not until the `UPDATE`); the fix calls `embed_body_chunks(body)` *before* the `UPDATE` and `write_finding_chunks` at the SQL tail so the lock spans only the millisecond tail, no behavior/output change; with this `rebuild_finding_chunks` (kept in #340 solely for `merge_findings` to import) has no callers and is deleted per no-dormant-code, and the GH-0340 note's claim is corrected in place; no schema change (issue #364)](GH-0364-merge-findings-hoist-embed-0001.md)
- [GH-0393 — `BARTLEBY_HOME` overrides the on-disk state-dir root (`projects/`, `config.yaml`, scratch, `serve/`), resolved **lazily** in `config.py` so a `BARTLEBY_HOME` set after import isn't a silent no-op; the *product* only honors the var (mechanism) while a worktree-scoped `bartleby-home-sandbox.sh` `SessionStart` hook + ultraship guidance *set* it (policy) — keeping dev/test/agent activity off the developer's live `~/.bartleby` corpora, the escape that wrote a corrupted `probe` corpus into the live namespace; tests inherit it via one autouse `conftest` fixture replacing ~9 copies of `PROJECTS_DIR` boilerplate (issue #393)](GH-0393-bartleby-home-override-0001.md)
- [GH-0363 — v0.9.0 assembly: `SCHEMA_VERSION` bumps **8 → 9** and the held-at-8 protocol resolves — `schema.py` and the dormant `_upgrade_v8_to_v9` step both already carried the v9 columns (#114's value-bearing-tag columns + #254's anchor-split bookkeeping) but the version stayed pinned at 8 so the upgrade loop never reached the step and the chain-walk equivalence gate (`test_upgrade_chain_walks_from_v4_through_current`) stayed `xfail`; this single assembly commit flips the version, turns the dormant step live, and drops the xfail — a purely-additive bump, so existing corpora run `bartleby project upgrade`, not re-ingest (omnibus v0.9.0)](GH-0363-v0.9.0-assembly-bump-to-9-0001.md)
- [GH-0363 — critic-loop fixes: three confirmed 🟡 correctness findings the `/ultraship` critic surfaced over #114's `merge_tags` value-tag safety and #370's `read_chunks` walled-chunk hint; all patch-level, no schema change, no `SCHEMA_VERSION` bump (omnibus v0.9.0 critic loop)](GH-0363-critic-merge-tags-readchunks-fixes-0001.md)
- [GH-0372 — SKILL.md documents that **FTS5 phrase queries span punctuation**: a quoted `"foo bar"` matches `foo bar`, `foo, bar`, and `foo. Bar` alike because FTS5 tokenizes on punctuation and a phrase matches a token *sequence*, so an agent can turn a semi-structured annotation (`Smith (Director of Operations)`) into an exact phrase filter by quoting just its words — previously undocumented; docs-only (one paragraph + the inverse caveat) on the scan row, no code or schema change (issue #372)](GH-0372-fts5-phrase-queries-span-punctuation-0001.md)
- [GH-0370 — `read_chunks` returns a **structured error for an unknown `--document`** with a cross-namespace hint: `chunk_id` and `document_id` are independent autoincrement sequences over one integer space and `read_chunks` accepts both kinds, so a wrong-namespace id (a chunk id passed as `--document`, or vice versa) used to read as an opaque empty/not-found; the new error names the namespace mismatch and suggests the likely other-kind lookup (issue #370)](GH-0370-read-chunks-structured-error-cross-namespace-hint-0001.md)
- [GH-0369 — new mutating skill script `save_date --document <id> --date YYYY-MM-DD` (or `--clear`) backfills/corrects a document's `authored_date` when the date is in the text but absent from metadata — a cite-don't-guess curation write in the `save_summary`/`assign_tag` mold, riding the existing runner mutation seam (`run(mutates=True)` → one `with conn:`, plus the rollback-surviving `log_call` audit row); **it writes `summaries.authored_date`, not `documents.authored_date`** because that is the only `authored_date` column the schema actually has and the column every read path reads (the issue's "`documents.authored_date` already exists" premise is false — reconciled in favor of the read column so the document immediately participates in `--authored-after/--before`); no schema change (issue #369)](GH-0369-save-date-writes-summary-row-0001.md)
- [GH-0368 — `authored_date` (nullable) now rides **every scan/search match in all modes including `--brief`**, sourced from the summary's `authored_date` (the `summaries` LEFT JOIN, the same source `list_documents` and the `--authored-after/--before` filters read) — so an agent triaging matches by time no longer has to re-look-up each document or parse dates out of filenames; no schema change, no re-ingest (issue #368)](GH-0368-authored-date-in-scan-search-matches-0001.md)
- [GH-0367 — `scan` surfaces `chunks.section_heading` in default-mode matches and gains a `--heading-like` chunk filter — two severable pieces making document structure visible and filterable at scan time; the column already exists and `read_chunks` already returned it, so no schema change and no re-ingest (issue #367)](GH-0367-section-heading-output-and-heading-like-filter-0001.md)
- [GH-0366 — `--file-like <pattern>` (SQL `LIKE`, repeatable for OR) lands on `scan`, `search`, and `list_documents`, routed through the shared `Scope` — so corpora that encode load-bearing metadata in `file_name` (dated exports, per-entity files, EDGAR accessions, `<BioguideID>__<date>__<slug>.md`) can be filtered server-side instead of corpus-wide-scanned and filtered client-side; no schema change (issue #366)](GH-0366-file-like-filter-via-shared-scope-0001.md)
- [GH-0362 — the schema-change versioning policy is written down: **minor = `SCHEMA_VERSION`**, and additive-vs-breaking is a *binary* call (additive → `_upgrade_vN_to_vN+1` + `project upgrade`; breaking → bump + re-ingest, no migration) — the convention lived in someone's head, and both the "held at 8" episode and the additive-vs-breaking call being re-derived at every bump were symptoms of that; docs-only (issue #362)](GH-0362-schema-change-versioning-policy-0001.md)
- [GH-0361 — `release.py` becomes **additive-aware in its messaging**: it no longer hard-codes "Existing corpora must be re-ingested" on *any* minor bump (in the GitHub Release notes, the dry-run summary, and the module docstring) — since an additive bump ships an `_upgrade_vN_to_vN+1` step and corpora run `bartleby project upgrade` instead; the messaging now distinguishes additive (upgrade) from breaking (re-ingest) (issue #361)](GH-0361-release-additive-aware-messaging-0001.md)
- [GH-0360 — `tests/test_chunks_chokepoint.py` pins the polymorphic-chunks invariant statically: it walks `bartleby/**/*.py` and fails on any `INSERT INTO` targeting `chunks`/`chunks_fts`/`chunks_vec` outside the allowlisted `bartleby/db/chunks.py` helper — previously the chokepoint was enforced only by code review; a static text scan (not AST), since the helper's own INSERTs live in SQL string literals; tests-only, no production or schema change (issue #360)](GH-0360-chunks-chokepoint-guard-test-0001.md)
- [GH-0359 — the `INSERT OR IGNORE INTO document_images` comment in `ingest/writer.py` is corrected: it claimed `OR IGNORE` dedupes replays of the `(doc, image, page, index)` tuple, but `document_images`' composite PK includes the *nullable* `page_number` and SQLite treats NULLs as distinct, so for docling/HTML/standalone images (`page_number = None`) that dedup is **inert**; the load-bearing replay guard is the document-exists early return (`document_id_for(file_hash)`), and `OR IGNORE` is only belt-and-braces over non-NULL tuples; comment-only, no behavior or schema change (issue #359)](GH-0359-document-images-replay-comment-0001.md)
- [GH-0358 — a test pins `persist_parse`'s **mid-unit rollback** (the "atomic parse" invariant): a `documents` row implies a *finished* parse, so resume re-runs only units that left no row — a property riding entirely on `Writer.persist_parse`'s single `with self.conn` transaction; the suite had no test forcing a failure *after* the `documents` INSERT, so the new test injects one and asserts the row (and all dependent writes) roll back as a unit; tests-only, no schema change (issue #358)](GH-0358-persist-parse-rollback-atomicity-0001.md)
- [GH-0357 — `test_schema_mismatch_points_at_upgrade_first` now stamps `SCHEMA_VERSION - 1` (an **older** DB) instead of `SCHEMA_VERSION + 1` (a newer DB) before reopening: `open_db`'s "run `bartleby project upgrade`, and if non-additive, re-ingest" remedy only applies to an *older* on-disk DB (the chain walks `vN → vN+1` forward and can only bring an older corpus up); asserting that message against a *newer* DB tested it in a direction where it doesn't apply — the correct remedy there is "update the code, not the DB" (#354's guard) (issue #357)](GH-0357-schema-mismatch-test-direction-0001.md)
- [GH-0356 — pins the **upgrade-chain refusal path**: a DB stamped below the first chain step is refused with `RuntimeError` ("non-additive bump; re-ingest is required") the moment `upgrade()` hits a version with no `_UPGRADES` step, and the CLI wrapper `sys.exit(1)`s — both previously uncovered, as was the load-bearing **"a refused upgrade leaves the DB unchanged"** property; that property matters because the chain now stamps `meta.schema_version` *inside each step's own transaction* (#353/#354), so a refused walk must write *nothing at all* — now explicitly guaranteed by test (issue #356)](GH-0356-upgrade-chain-refusal-leaves-db-unchanged-0001.md)
- [GH-0355 — the upgrade-chain equivalence gate (`test_upgrade_chain_walks_from_v4_through_current`) is strengthened to **full DDL**: its `_schema()` helper compared only table names plus sorted column names, so dropped indexes, column-type drift, and `NOT NULL`/`DEFAULT`/`CHECK`/FK-clause drift were all invisible (exactly the drift the gate exists to catch); it now compares the whitespace-normalized `sqlite_master.sql` of every `table`/`index` row between the chain-upgraded and freshly-created DBs — stripping `--` comments (`schema.py` annotates columns; the hand-built chain doesn't), collapsing whitespace, and uppercasing — so layout differs but structure can't (issue #355)](GH-0355-strengthen-chain-gate-full-ddl-0001.md)
- [GH-0354 — `upgrades.upgrade()` now **refuses a DB newer than the code** at the top, before any mutation: it walked `current_version` up to `SCHEMA_VERSION` but never guarded `current_version > SCHEMA_VERSION` — the `while v < SCHEMA_VERSION` loop simply never ran, fell through to the trailing `with conn:`, and stamped `upgraded_at` on a DB it can't reason about; the newer-DB guard lived only in the (untested) CLI wrapper, so the library itself had the hole — now a single early `RuntimeError` ("DB schema vN is newer than this code (vM). Update the code, not the DB.") closes it (issue #354)](GH-0354-guard-newer-db-0001.md)
- [GH-0353 — **crash-safe per-step upgrade stamping** + a widened CLI catch: `upgrade()` committed each `_upgrade_vN_to_vN+1` step in its own `with conn:` but stamped `meta.schema_version` only after the *whole* chain, so a crash mid-chain left the DB structurally at vN+1 while `meta` said vN — a re-run replayed the step and died on "table already exists" (an `apsw.SQLError`, which the CLI's `RuntimeError`-only catch surfaced as a raw traceback while `open_db`'s strict check wedged the corpus); the fix moves the `UPDATE meta SET schema_version = v+1` *inside* each step's transaction (DDL + bump commit atomically) and widens the CLI catch, so a mid-chain crash leaves the last completed step's version and a re-run resumes cleanly (issue #353)](GH-0353-crash-safe-upgrade-stamping-0001.md)
- [GH-0352 — resolve the stale "held at 8" `SCHEMA_VERSION` header comment + write down the v8→v9 authoring rule: the comment promised a "consolidated `_upgrade_v8_to_v9` ships once when #169 releases" and told v8 corpora to **re-ingest** for `ingests`/`ingest_run_id` — both wrong by v0.8.x (#169 closed shipping *staying at* v8; `_upgrade_v7_to_v8` was completed per #212/GH-0212 to add those additively, reachable via `project upgrade`); the stale text is replaced with the actual rule rather than just deleted, because two live hazards motivated recording it (issue #352)](GH-0352-stale-held-at-8-schema-comment-0001.md)
- [GH-0254 — TOC-excision data-loss + double-linked-TOC contiguity fix: the anchor-splitting rework's "every byte lands in exactly one section" invariant still failed in two edge shapes (plus one regression) the critic's empirical second pass found — chiefly that excising the TOC nav's **top-level ancestor** over-excised real content whenever the nav shared its page-`<div>` with cover prose (registrant name, CIK, period), a DATA LOSS; all three fixed in `ingest/sec2md.py`, held at SCHEMA_VERSION 9 (issue #254, final critic-loop rework under omnibus #363)](GH-0254-toc-excision-fix-0001.md)
- [GH-0254 — anchor-splitting rework — preamble section + document-order, TOC-only boundaries: the original #254 split shipped three confirmed production bugs in `ingest/sec2md.py` that broke its own "every byte of content lands in exactly one section, nothing dropped" invariant — chiefly that slicing started at the first anchor target so all pre-TOC body content (the EDGAR cover page: registrant name, CIK, period, ticker, shares) landed in no section and vanished from FTS/embeddings; the rework emits a synthetic **preamble** section for that content, orders sections by document position, and draws boundaries only at real TOC targets (issue #254, critic-loop rework under omnibus #363)](GH-0254-anchor-splitting-rework-0001.md)
- [GH-0254 — split TOC-anchored EDGAR/iXBRL HTML into a container + section documents: large SEC filings (S-1s, 10-Ks, hundreds of pages) ingested as one monolithic `documents` row, so the single summary truncated past `max_summarize_tokens` and `read_document` was a token bomb; the adopted "Option C1" makes the *unit* smaller — `ingest/sec2md.py::convert_sections` parses the raw HTML (BeautifulSoup, lazy-imported), collects `<a href="#id">` links whose targets exist in the body, slices at the top-level ancestors of each target, and emits independently chunked/embedded/summarized section documents; EDGAR/sec2md-only, and only when a real TOC is present (issue #254)](GH-0254-html-anchor-splitting-0001.md)
- [GH-0114 — **value-bearing tags**: a tag can now optionally carry a **per-document value** (a number/string/date pulled from each document's text by a deterministic `(?P<value>…)` regex), so a value-tag answers "what is X's value in each document?" rather than just "which documents are X?" — riding the existing tag vocabulary, not a new subsystem; **purely additive** schema (four nullable, NULL-truthful columns: `tags.value_type` CHECK'd discriminator + `tags.pattern`, `document_tags.value` stored canonically as text + `document_tags.chunk_id` as the citation anchor preserving "reached through a chunk, never cited directly"); a new `extract` skill script does the pulling; the columns land in `schema.py` and a `_upgrade_v8_to_v9` fragment held dormant until the #363 assembly bump to v9 (issue #114)](GH-0114-value-bearing-tags-0001.md)
- [GH-0342 — `merge_findings` and `edit_finding` now **reject upfront** any citation pointing at a finding-kind chunk owned by a finding *involved* in the operation, before any write — because both delete-and-rebuild the body chunks of the findings they touch, and `finding_citations.chunk_id REFERENCES chunks(chunk_id) ON DELETE CASCADE` would otherwise turn such a citation into one of two silent corruptions: a citation row that lands but is cascade-deleted when the owning source is consumed (leaving a `[^N]` marker with no row), or an FK violation surfacing as an opaque `INTERNAL_ERROR` when the chunk is deleted before `replace_finding_citations` runs; the new `reject_citations_to_involved_findings` in `_common.py` runs after `load_finding_body` and the not-found/memory-wall gates, queries the cited *chunk rows* by `source_kind='finding'` AND `source_id IN (involved ids)` (ownership decided by the chunk row's `source_id`, never by matching body text — text matching would miss renumbered ids and false-positive on prose), and raises with `offending_chunk_ids=[…]`; `merge_findings` uses involved set `sources + [target]` (code `CITES_MERGED_CHUNKS`), `edit_finding` uses `[finding_id]` only when `--body-file` is supplied (code `CITES_OWN_CHUNKS`); the two codes differ only for message ergonomics and share one helper; this is the involved-finding *write-refuse* half complementing #343's read-seam surfacing of dangling markers in *other* findings; no `mutates`/transaction change to `merge_findings` (stage-manager owns that per #339); no schema change (issue #342)](GH-0342-reject-self-citations-0001.md)
- [GH-0343 — dangling `[^N]` citation markers are now **surfaced at the read seam** rather than leaked illegibly: when a cited finding-kind chunk vanishes (the cited finding deleted, or edited/merged so its chunks rebuild under new ids), `ON DELETE CASCADE` strips the citation row but the *citing* finding's verbatim body keeps the `[^N]` marker pointing at nothing — real intel (a claim that *was* supported by a since-removed source, distinct from never-cited) that was previously emitted as raw `[^123]`; since write paths already reject dangling at save time (`load_finding_body` → `UNKNOWN_CITATIONS`), dangling is strictly a post-write cascade artifact, so the fix lives only at read time and needs no stored state or schema change — derivable as the body's `extract_citations` ids minus the *resolved* citation set (`resolve_citations` already drops vanished chunks); `read_finding` gains a computed `dangling_citations: [int,…]` field (documented in its Output block and SKILL.md, telling the agent to flag such a marker as a removed citation, not drop it); the web finding view's `+page.svelte` marker fallback now calls `renderTombstone(N)` to render a muted "cited source no longer available" affordance keeping the marker visible; and `delete_finding`'s docstring drops its false "no referential fallout for the corpus" claim, naming the incoming-cascade fallout and pointing at the read seam (its `removed_citations` counts only the deleted finding's own outgoing citations) — phrased "no longer available" never "deleted" since at read time we can't tell a truly-deleted source from an edited/merged one; no schema change (issue #343)](GH-0343-read-seam-dangling-citations-0001.md)
- [GH-0340 — six write scripts (`save_finding`, `edit_finding`, `save_summary`, `merge_tags`, `assign_tag`, `unassign_tag`) now declare `run(mutates=True)` (the #339 seam) so their multi-statement writes commit or roll back as a unit — previously each autocommitted its pieces, so a mid-write failure could orphan a finding with no chunks, leave an edited body with stale citations, or copy assignments onto a destination tag while the source lingered; **`tag.py` stays deliberately unwrapped** (a comment says why) because its per-document LLM classification loop against the user's busy local Ollama tolerates per-document failure and resumes via `_already_tagged` — one transaction would hold a write lock for minutes-to-hours and roll back every already-classified doc, destroying load-bearing resumability; **embedding is hoisted above the first write** in the three embedding scripts: `rebuild_finding_chunks` in `_common.py` splits into an EMBED phase (`embed_body_chunks(body) -> list[ChunkInput]`, no DB touch) and a WRITE phase (`write_finding_chunks(conn, finding_id, chunk_inputs)`), so the ~5–10s lazy sentence-transformers model load happens *before* the deferred apsw transaction takes the write lock (`busy_timeout` is only 5000ms, shorter than the load, so embedding-under-lock risked `BusyError` for a concurrent skill/web/ingest); `rebuild_finding_chunks` was kept as a thin composition so `merge_findings` could keep importing it — **on the since-corrected claim that `merge_findings` already held a write txn across embedding so a hoist would buy it nothing; #364 showed its deferred lock opens only at its `UPDATE findings` (embed sat after it, same exposure), hoisted that embed too, and deleted the now-unused `rebuild_finding_chunks`**; atomicity is pinned by one inject-failure test per wrapped script (break `chunks._pack_embedding` mid-insert for findings/summary, wrap per-doc helpers / install an apsw exec-trace for the tag scripts), each asserting every affected table is untouched incl. all three chunk tables via `assert_chunk_tables_consistent`; builds on #344's chunk-seeded `summary_a` fixture; no schema change (issue #340)](GH-0340-mark-mutating-hoist-embeds-0001.md)
- [GH-0344 — the skill mutation tests (`edit_finding`, `merge_findings`, `save_summary` replace, `delete_finding`) now assert `chunks`/`chunks_fts`/`chunks_vec` stay in sync via one shared `assert_chunk_tables_consistent(conn)` in `tests/_skill_fixtures.py` — **tests-only, no production change, no `SCHEMA_VERSION` bump** — closing gaps where the mirrors were only spot-checked or checked *vacuously*; the asymmetry is the point: the **FTS leg** must use FTS5's two-argument `INSERT INTO chunks_fts(chunks_fts, rank) VALUES('integrity-check', 1)` because `chunks_fts` is `content='chunks'` (stores no row text of its own), so any rowid/`COUNT` lookup reads *through* `chunks` and can never observe drift — the `rank` argument is what forces FTS5 to re-derive the index from the content table and compare (the one-argument form is inert on apsw 3.51/SQLite 3.51 and must not be used); the **vector leg** uses direct `{rowid FROM chunks_vec} == {chunk_id FROM chunks}` set comparison since `vec0` has its own storage; SQLite `INTEGER PRIMARY KEY` rowid reuse forced identity-**by-content** not by-id (the `save_summary` fixture's prior summary was seeded with zero chunks so its replace was unobserved — now seeded with two chunks identified by distinctive text, which collided on `(source_kind, source_id, chunk_index)` UNIQUE in `test_skill_search.py` and bumped that seed's `chunk_index` to 2, the only edit outside the mutation tests + fixtures); the assertions **pass on current HEAD — no production bug surfaced** (a failure would have been a real write-path bug to park and report, not fix here) (issue #344)](GH-0344-triple-table-sync-assertions-0001.md)
- [GH-0341 — `merge_tags` splits its single `moved_assignments` field into `inserted` + `already_present`, because the old field was a lie whenever source and destination tags overlapped: the copy is `INSERT OR IGNORE INTO document_tags … WHERE tag_id = src`, which silently drops any `(document_id, tag_id)` the destination already held, yet the report took the source tag's pre-merge `COUNT(*)` and called every row "moved" even when zero were written; `inserted` is now the true rows written, read from `conn.changes()` (apsw's per-connection rows-changed counter, as in `ingest/writer.py`) *before* the `DELETE FROM tags` so it reflects only the copy, and `already_present` is `pre_merge_count - inserted` — their sum is still the pre-merge count, so no information is lost while "merged a 40-doc tag; 12 already there, 28 new" becomes distinguishable from "28 brand-new"; the field was split rather than renamed per the run-wide director's note; no `with conn:` is added here (copy+delete atomicity is #340's runner-seam wrap) — purely count semantics; `moved_assignments` had no consumers outside the script (grep found only `merge_tags.py`; SKILL.md never named it), so the change is contained to the docstring, return, and tag tests; no schema change (issue #341)](GH-0341-merge-tags-count-semantics-0001.md)
- [GH-0339 — atomicity for mutating skill scripts moves to **one place, the runner seam**: `skill_runner.run` gains a `mutates: bool = False` keyword that, when `True`, wraps **only** the `work()` call in a single apsw `with conn:` transaction so the whole mutation commits or rolls back as a unit (read-only default opens no transaction, behavior identical to before) — replacing the per-script hand-rolled `with conn:` wrappers `merge_findings`/`delete_finding` carried since GH-0056; three properties are load-bearing: the transaction wraps `work()` *only* (project/session resolution commit their own bookkeeping before it), apsw `with conn:` is a **deferred** transaction (no write lock until the first write, so a mutating script that ends up read-only never locks), and the `with conn:` blocks inside the `bartleby.db.chunks` typed helpers **nest as savepoints** under it rather than committing independently; the `log_call` audit write stays **outside** the transaction on purpose — a rolled-back mutation must still leave an `audit_logs` row, since the audit log records *calls* not *successful* calls; **#339 flips no real script** — it ships only the seam, the removal of the two hand-wraps, and a guard test (`tests/test_skill_runner.py`) that drives `run` with inline `work` callbacks, so `merge_findings`/`delete_finding` are *momentarily un-wrapped* until #340 marks them mutating (intentional gap); `tag.py` stays unwrapped by design (per-document commit is load-bearing); no schema change (issue #339)](GH-0339-runner-seam-transaction-0001.md)
- [GH-0335 — `/ultraship` players are **isolated subagents** (the Agent tool with `isolation: worktree`), not headless `claude -p` processes — running in the stage-manager's own session under the user's login, so there's no separate auth, no pre-granted permission allowlist, and no `--dangerously-skip-permissions`, and the "hangs at 1 a.m." failure mode GH-0244 designed around can't occur (ToS-clean by construction, matches observed practice); the trade-off is that a subagent can't spawn another agent, so the per-player gate splits — players keep `uv run pytest` → commit, and the `simplify-refactor` half moves up to the **stage-manager**, run over each sub-PR's diff in the merge train (run step 4) where it has agent-spawn capability and is already the single serial writer; supersedes the player-mechanism part 2 of GH-0244 (part 1's autonomous sub-issue → omnibus merge stands) (issue #335)](GH-0335-ultraship-subagent-players-gate-at-stage-manager-0001.md)
- [GH-0334 — `bartleby/skill/SKILL.md` now names `bartleby config` (not the post-#137-stale `bartleby ready`) as where the summarizer model used by `tag --all` is configured — the deferred remainder of #329's sweep, surfaced by the first `/ultraship` run's critic; a tree-wide audit confirmed this was the *only* remaining model-config-sense reference, so every other `bartleby ready` occurrence (README install section, `skill/README.md`, `commands/ready.py`, `tests/test_ready.py`) is the install sense and was deliberately left untouched — the line is install (`bartleby ready`) vs configure-provider/model (`bartleby config`); docs-only, no schema change (issue #334)](GH-0334-skill-md-model-config-names-bartleby-config-0001.md)
- [GH-0329 — the skill's `NO_PROVIDER` error (raised by `resolve_classifier` in `skill_scripts/_tags.py` when the summarizer config has no provider/model) now tells the user to run `bartleby config`, not the post-#137-stale `bartleby ready` — since #137 split the two surfaces, `bartleby ready` only installs the skill while the provider-config wizard moved to `bartleby config`, so the remedy the error named no longer fixed it; a focused test asserts the code is `NO_PROVIDER`, the message contains `bartleby config` and not `bartleby ready` so it can't silently regress; auditing other stale `bartleby ready` copy elsewhere (e.g. the SKILL.md model-config wording) is a separate sweep item left untouched (issue #329)](GH-0329-no-provider-names-bartleby-config-0001.md)
- [GH-0330 — `_attach` in `db/connection.py` (apsw) now calls `conn.enable_load_extension(False)` immediately after `sqlite_vec.load(conn)`, so the SQL `load_extension()` function is no longer callable for the connection's lifetime on either call site (`open_db`, `init_db`) — defense-in-depth (all query text is already parameterized) following the standard enable→load→disable idiom; disabling further loading does not unload the already-registered sqlite_vec (`SELECT load_extension('x')` raises `apsw.SQLError` "not authorized" while `vec_version()` still returns, asserted by a new focused test and the green suite); chose the minimal one-liner over a `try/finally` wrapper since `_attach` does no failable work between load and disable; no schema change (issue #330)](GH-0330-disable-load-extension-after-attach-0001.md)
- [GH-0310 — the `failed_ingests` clear is folded into each `persist_*` transaction (parse/caption/summary) so a unit's payload write and its failure-row removal commit atomically — closing the crash window that once left a durably-complete unit reading as failed forever (resume derives from completeness and would never re-run it to clear the ghost); the delete runs before each method's early-return so a sibling-captioned replay still sheds a ghost; the redundant external `clear_failure` call sites are removed and the method privatized to `_clear_failure` (caller-holds-txn contract) per no-dormant-code, rather than left as no-ops (issue #310)](GH-0310-clear-failed-ingests-in-persist-transaction-0001.md)
- [GH-0311 — `bartleby scribe` exits non-zero when any ingest unit is left unresolved (parse failure in `failed_ingests`, or a document still owing captions/summary); the green "Done." is suppressed on failure so a nothing-ingested run can't read green to a `&& next-step` caller, while a no-op resume and the no-files paths still exit 0 — the signal is the union of `writer.failures()` (the only source that catches parse failures) and `incomplete_count`, and disabled vision/summary stages don't trip it (issue #311)](GH-0311-scribe-exits-nonzero-on-failure-0001.md)
- [GH-0314 — the Ollama summarize-worker clamp keys off the *effective* provider (`--provider` override folded in), not bare `config['provider']`: `_resolve_summarize_workers` takes a required `effective_provider` and `main()` threads in the same value it records in the provenance snapshot; fixes `scribe --provider ollama` over a cloud config bypassing the clamp and `--provider anthropic` over an ollama config clamping a cloud run to 1; refines GH-0243 (issue #314)](GH-0314-clamp-tracks-effective-provider-0001.md)
- [GH-0309 — a Tesseract failure during sparse-PDF-page classification now degrades that page to the VLM route instead of failing the whole document parse (it was unguarded, discarding every non-sparse page on a broken install — the #43 scenario); makes `pdfplumber.convert` obey the same "OCR is only a classifier, never a hard dependency" invariant `images.analyze` already did; `ocr.run` also catches `TesseractNotFoundError` (the missing-binary case) for the legible #43 re-wrap; warn-once consolidated into `console.warn_once(key, message)` so both surfaces warn once per process across surfaces, not once each (issue #309)](GH-0309-sparse-page-ocr-degrades-to-vlm-0001.md)
- [GH-0306 — split the ~1,600-line `commands/scribe.py` god module into `bartleby/ingest/` phase modules (`resolve`, `parsers`, `classify`, `caption`, `summary`, `parse` + `_ProgressTally`→`progress`), leaving a ~330-line `main()` entry point; behaviour-preserving Phase-0 enabling refactor for the v0.8.10 omnibus, five green commits, no schema touch — it makes the parse/caption/summarize asymmetry that hid the missing parse retry-cap (#307) self-evident and lets #307–#314 each land in a distinct module (issue #306)](GH-0306-split-scribe-into-ingest-phase-modules-0001.md)
- [GH-0244 — `/ultraship` assembles a whole omnibus bundle unattended (director interview → headless players → serialized merge train → bounded critic → director grade → 8am report); it bends two safety rules **deliberately and only here**: the stage-manager merges sub-issue → omnibus autonomously (recoverable branch, serialized train, gated suite, never force-pushed) and players run with pre-granted permissions (else they hang at 1am) — **a human still merges omnibus → `main`** and the guard hook is untouched; manifest parse/validate/DAG is the pure, unit-tested `scripts/ultraship.py`, orchestration is skill prose (issue #244)](GH-0244-ultraship-authority-boundary-0001.md)
- [GH-0303 — hotfix: leaderboard speed becomes **Input tokens/s** (source-document prompt tokens per load-corrected wall-clock second, mean of per-doc medians) — the planning number: divide a doc's token count by it to estimate its summarization time; **Tok/s** and **Wall s/tok** both drop (the latter normalized by *output* length, which you don't know up front); frontier axis and sort tiebreak ride on `input_tps`, CSV gains `input_tokens_per_second`; the live progress Tok/s is untouched](GH-0303-hotfix-input-tokens-per-second-0001.md)
- [GH-0303 — leaderboard speed becomes **Wall s/tok** (load-corrected wall-clock per output token, length-normalized and defined for cloud rows too); the length-confounded **Inference (s)** column is dropped; the **Frontier** header is renamed **Pareto optimal** (CSV `frontier`→`pareto_optimal`) since "frontier model" colloquially means a big-lab flagship — the very thing the column excludes; frontier axis still gates on `tps`, so cloud rows stay off it (issue #303)](GH-0303-wall-s-tok-over-inference-and-pareto-optimal-rename-0001.md)
- [GH-0295 — the `anthropic-cc` judge grades summaries via the local `claude -p` CLI under the user's own Claude subscription (no OpenAI key); headless-CLI-over-SDK so the subprocess env can strip `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` and never `--bare` — the load-bearing guard against silently billing a pay-as-you-go API account; `--json-schema` enum axes + `JudgeScore.model_validate`; sanctioned for individual use only, judge-only (issue #295)](GH-0295-anthropic-cc-judge-0001.md)
- [GH-0297 — omnibus tracking has two layers `/ship` keeps in step: native GitHub **sub-issues** (hierarchy panel, tracks closure — via REST `sub_issues`, typed-int `.id`) and a comment-anchored `omnibus-checklist` block in the body (one `- [ ] #<N>` line per sub-issue, tracks branch-landing); seeded on first ship, ticked/linked on sub-PR-open, block read as the promotion `Closes` manifest; the format layer #265's serialized reconciler builds on (issue #297)](GH-0297-checkbox-regime-omnibus-0001.md)
- [GH-0281 — `bartleby serve` takes an optional `--project`, overriding the active project for that server only via `BARTLEBY_PROJECT` (no config mutation) — closes the last project-scoped-command parity gap (issue #281)](GH-0281-serve-project-0001.md)
- [GH-0288 — the memory-off finding-ownership check is consolidated into one `_common` chokepoint (`assert_findings_accessible` over `owned_finding_ids`); all five gating sites route through it; supersedes the per-script-gate stance of GH-0056/0271/0272/0275 now that the omnibus conflict-avoidance reason is gone (issue #288)](GH-0288-consolidate-finding-ownership-check-0001.md)
- [GH-0275 — `delete_finding` (title echo + destroy) and `merge_findings` (consume foreign sources) gate on ownership under memory-off; supersedes GH-0056's "ungated write siblings" stance for these two (issue #275)](GH-0275-gate-delete-merge-on-memory-wall-0001.md)
- [GH-0272 — `edit_finding` gates on ownership under memory-off (its body echo made it a read-by-write bypass); refines GH-0056's "edit is an ungated write sibling" — gate iff the response discloses unsupplied content (issue #272)](GH-0272-edit-finding-memory-off-gate-0001.md)
- [GH-0271 — read_chunks honors the memory wall: a memory-off session can't read other sessions' finding chunks (foreign `--chunks` ids fall to `missing`, foreign `--around-chunk` raises `MEMORY_OFF`) (issue #271)](GH-0271-read-chunks-honors-the-memory-wall-0001.md)
- [GH-0268 — The summarizer benchmark is a `bartleby benchmark` command group over append-only per-cell stores: idempotent multi-pass judging, recorded provenance with loud drift, cloud reference rows off the frontier (issue #268)](GH-0268-benchmark-command-group-append-only-stores-0001.md)
- [GH-0248 — `scan --count-by` groups by a regex capture (per-match counts); a fold primitive with a stated not-a-query-engine boundary, `--extract` deferred, agent-proportionate runaway guard (issue #248)](GH-0248-count-by-regex-capture-0001.md)
- [GH-0246 — describe_corpus reports chunk-length stats (median/p90/max) so the agent self-sizes `--preview` instead of raising the 240-char default (issue #246)](GH-0246-describe-corpus-chunk-length-stats-0001.md)
- [GH-0234 — Graduate the local-summarizer benchmark into VC: local-only, temp-0.0 parity, blind OpenAI judge, schema-gate leaderboard; defer the real run (issue #234)](GH-0234-graduate-summarize-benchmark-to-vc-0001.md)
- [GH-0243 — A local Ollama provider auto-clamps the caption/summarize worker pools to 1 (issue #243)](GH-0243-clamp-ollama-worker-pools-0001.md)
- [GH-0250 — Temperature is dropped on Opus 4.7+ unconditionally (a property of the model, not of whether reasoning effort was sent), not only on the effort path; supersedes GH-0232 on that one point (issue #232 / PR #250)](GH-0250-unconditional-temperature-drop-opus-47plus-0001.md)
- [GH-0232 — Reasoning effort is a unified enum mapped per-provider, via the modern Anthropic effort API (issue #232)](GH-0232-reasoning-effort-knob-0001.md)
- [GH-0235 — An HTML page saved as `.pdf` is rejected with a clear reason, never rerouted into the HTML pipeline (issue #235)](GH-0235-reject-html-saved-as-pdf-not-reroute-0001.md)
- [GH-0236 — PER_WORKER_GB sized against measured pdfplumber peak RSS, not a model-load estimate (issue #236)](GH-0236-tune-per-worker-gb-to-measured-peak-0001.md)
- [GH-0227 — Parse workers route warnings to the parent; they never touch the console (issue #227)](GH-0227-route-worker-warnings-to-parent-0001.md)
- [GH-0225 — In-run duplicate files are dropped with a surfaced count, not aliased or provenance-tracked (issue #225)](GH-0225-in-run-duplicate-files-dropped-with-count-0001.md)
- [GH-0222 — Never send temperature to the OpenAI provider; GPT-5 rejects non-default values (issue #222)](GH-0222-omit-temperature-openai-0001.md)
- [GH-0212 — Complete the v7→v8 additive upgrade rather than force a re-ingest (issue #212)](GH-0212-complete-v7-to-v8-additive-upgrade-0001.md)
- [GH-0213 — Bound parse-worker memory so a long ingest doesn't OOM (issue #213)](GH-0213-bound-parse-worker-memory-0001.md)
- [GH-0170 — Scribe progress UX under the worker pool (issue #170)](GH-0170-scribe-progress-ux-worker-pool-0001.md)
- [GH-0188 — Parallelize the summarize pass (issue #188)](GH-0188-parallelize-summarize-pass-0001.md)
- [GH-0166 — Decouple captioning into its own concurrent phase (issue #166)](GH-0166-decouple-captioning-own-concurrent-phase-0001.md)
- [GH-0167 — Decouple summarization into its own pass (issue #167)](GH-0167-decouple-summarization-own-pass-0001.md)
- [GH-0181 — OCR failure falls back to the VLM instead of dropping the image (issue #181)](GH-0181-ocr-failure-falls-back-vlm-instead-dropping-0001.md)
- [GH-0162 — Opt-in per-stage ingest timing + benchmark mode (issue #162)](GH-0162-opt-stage-ingest-timing-benchmark-mode-0001.md)
- [GH-0165 — Parallel parse pool (issue #165)](GH-0165-parallel-parse-pool-0001.md)
- [GH-0156 — Per-chunk view + quick-links from every chunk reference (issue #156)](GH-0156-chunk-view-quick-links-from-every-chunk-0001.md)
- [GH-0137 — `bartleby ready` skill installer + `ready`→`config` rename (issue #137)](GH-0137-bartleby-ready-skill-installer-ready-config-rename-0001.md)
- [GH-0002 — Docling backend stops double-parsing for images (issue #2)](GH-0002-docling-backend-stops-double-parsing-images-0001.md)
- [GH-0171 — Per-unit ingest provenance + config-drift warnings (issue #171)](GH-0171-unit-ingest-provenance-config-drift-warnings-0001.md)
- [GH-0164 — Schema v8 — single-writer drain + per-unit restartable ingest (issue #164)](GH-0164-schema-v8-single-writer-drain-unit-restartable-0001.md)
- [GH-0123 — Sanitize `{@html}` markdown against XSS (issue #123)](GH-0123-sanitize-html-markdown-against-xss-0001.md)
- [GH-0127 — Two-tone-by-meaning surface system — the visual unify pass (issue #127)](GH-0127-two-tone-meaning-surface-system-visual-unify-0001.md)
- [GH-0118 — `scan --sort {document,date}` — chronological enumeration (issue #118)](GH-0118-scan-sort-document-date-chronological-enumeration-0001.md)
- [GH-0122 — Web style standardization — design tokens + extracted primitives (issue #122)](GH-0122-web-style-standardization-design-tokens-extracted-primitives-0001.md)
- [GH-0121 — Displayed HTML source can't run its scripts (issue #121)](GH-0121-displayed-html-source-can-t-run-scripts-0001.md)
- [GH-0104 — Aggregation/facet — `scan --count-by document` + filtered `describe_corpus` (issue #104)](GH-0104-aggregation-facet-scan-count-document-filtered-describe-0001.md)
- [GH-0108 — Memory-off reads scoped to own session, not blocked (issue #108)](GH-0108-memory-off-reads-scoped-own-session-blocked-0001.md)
- [GH-0110 — Web view delegates read parity to the skills — home + `/documents` (issue #110)](GH-0110-web-view-delegates-read-parity-skills-home-0001.md)
- [GH-0105 — `--brief` triage projection on the four enumeration commands (issue #105)](GH-0105-brief-triage-projection-four-enumeration-commands-0001.md)
- [GH-0100 — Semi-automated releases — tags as version pins, minor coupled to `SCHEMA_VERSION` (issue #100)](GH-0100-semi-automated-releases-tags-version-pins-minor-0001.md)
- [GH-0087 — Web UI packaged so `serve` works from an installed wheel (issue #87)](GH-0087-web-ui-packaged-so-serve-works-from-0001.md)
- [GH-0080 — No summary for zero-chunk documents + image-only summary input (issue #80)](GH-0080-summary-zero-chunk-documents-image-only-summary-0001.md)
- [GH-0056 — Findings curation — `delete_finding` + `merge_findings` (issue #56)](GH-0056-findings-curation-delete-finding-merge-findings-0001.md)
- [GH-0078 — Content-sniffed file-type resolution (issue #78)](GH-0078-content-sniffed-file-type-resolution-0001.md)
- [GH-0068 — Batch tag assignment — `assign_tag` / `unassign_tag --documents <id,id,...>` (issue #68)](GH-0068-batch-tag-assignment-assign-tag-unassign-tag-0001.md)
- [GH-0066 — Tags surfaced in the web UI (issue #66)](GH-0066-tags-surfaced-web-ui-0001.md)
- [GH-0065 — Web UI search reuses the skill scripts via subprocess (issue #65)](GH-0065-web-ui-search-reuses-skill-scripts-subprocess-0001.md)
- [GH-0051 — EDGAR full-submission `.txt` unwrap (issue #51, Phase 1)](GH-0051-edgar-full-submission-txt-unwrap-0001.md)
- [GH-0062 — Schema v7 — finding provenance (`sessions.model` / `sessions.harness`, issue #62)](GH-0062-schema-v7-finding-provenance-sessions-model-sessions-0001.md)
- [GH-0032 — `scan` — full-text enumeration primitive (issue #32)](GH-0032-scan-full-text-enumeration-primitive-0001.md)
- [GH-0047 — `--help` prints the response shape, not just flags (issue #47)](GH-0047-help-prints-response-shape-just-flags-0001.md)
- [GH-0040 — `list_findings` / `read_finding` — finding read paths (issue #40)](GH-0040-list-findings-read-finding-finding-read-paths-0001.md)
- [GH-0035 — Manual tag assignment — `assign_tag` / `unassign_tag` (issue #35)](GH-0035-manual-tag-assignment-assign-tag-unassign-tag-0001.md)
- [GH-0014 — Converter axis split + sec2md (issue #14)](GH-0014-converter-axis-split-sec2md-0001.md)
- [GH-XXXX — Schema v6 — `tags` + `document_tags`](GH-XXXX-schema-v6-tags-document-tags-0001.md)
- [GH-XXXX — Skill scratch lives in `~/.bartleby/tmp/`, not `/tmp`](GH-XXXX-skill-scratch-lives-bartleby-tmp-tmp-0001.md)
- [GH-XXXX — Schema v5 — `summaries.authored_date`](GH-XXXX-schema-v5-summaries-authored-date-0001.md)
- [GH-XXXX — Summarizer input includes image chunks](GH-XXXX-summarizer-input-includes-image-chunks-0001.md)
- [GH-XXXX — Additive-only schema upgrades allowed (relaxation of "no backwards compat")](GH-XXXX-additive-only-schema-upgrades-allowed-relaxation-backwards-0001.md)
- [GH-XXXX — No running citation tracker](GH-XXXX-running-citation-tracker-0001.md)
- [GH-XXXX — `read_chunks --chunks <ids>`](GH-XXXX-read-chunks-chunks-ids-0001.md)
- [GH-XXXX — `search` triage signals](GH-XXXX-search-triage-signals-0001.md)
- [GH-XXXX — `search --in-documents`](GH-XXXX-search-documents-0001.md)
- [GH-XXXX — Tesseract owns image transcription, VLM owns description.](GH-XXXX-tesseract-owns-image-transcription-vlm-owns-description-0001.md)
- [GH-XXXX — Schema v4 — `chunks.page_number`](GH-XXXX-schema-v4-chunks-page-number-0001.md)
- [GH-XXXX — Schema v3 — image pipeline](GH-XXXX-schema-v3-image-pipeline-0001.md)
- [GH-XXXX — Schema v2 — title/description on summaries and findings](GH-XXXX-schema-v2-title-description-summaries-findings-0001.md)
- [GH-XXXX — `documents.token_count`](GH-XXXX-documents-token-count-0001.md)
- [GH-XXXX — Query embedding](GH-XXXX-query-embedding-0001.md)
- [GH-XXXX — Sessions auto-create](GH-XXXX-sessions-auto-create-0001.md)
- [GH-XXXX — `finding_citations` is chunk-level](GH-XXXX-finding-citations-chunk-level-0001.md)
- [GH-XXXX — Reranker dropped](GH-XXXX-reranker-dropped-0001.md)
- [GH-XXXX — Docling is the only converter](GH-XXXX-docling-only-converter-0001.md)
- [GH-XXXX — Sequential ingestion](GH-XXXX-sequential-ingestion-0001.md)
- [GH-XXXX — CLI rename](GH-XXXX-cli-rename-0001.md)
