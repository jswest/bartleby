# No running citation tracker

an agent asked for `cite <chunk_id>` + `save_finding --use-tracked-citations`. Declined — `save_finding` already gives durable storage; SKILL.md nudges agents to write interim findings when context grows long.
